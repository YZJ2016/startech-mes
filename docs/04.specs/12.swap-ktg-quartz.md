# Development Unit Template

Use this template before each implementation unit.

## 1. Spec

### Capability

- Name: 用 springboot2 内容替换 `ktg-quartz`
- Module: `ktg-quartz`
- Priority: P0
- Related requirement/design documents:
  - `docs/04.specs/11.backend-shell-swap-program.md`
  - 抽出确认：`09`/`10` 已认定 quartz 无 MES 额外业务类型

### Business Goal

定时任务模块与若依 3.9.2 `springboot2` 的 `ruoyi-quartz` 文件集对齐，仍以 Maven 模块 `ktg-quartz`、包名 `com.ktg.quartz` 对外。不碰 `ktg-mes`，不做五项中的 admin/security 接线。

### Scope

In scope:

- 对照树：`RuoYi-Vue-springboot2/ruoyi-quartz/`（HEAD 必须是 `913524b1525a0e374774afbb54c452d8f68a5a8f`）
- 覆盖 nested：`startech-mes-basic/backend/ktg-quartz/` 的 `src/` 与该模块 `pom.xml`
- 机械替换：`com.ruoyi` → `com.ktg`；pom `ruoyi-quartz` → `ktg-quartz`；parent 保持 `com.ktg:ktg`（版本用 nested 现有 parent，当前 `3.8.2`）
- 含 Java 与 `src/main/resources/mapper/quartz/*.xml`

Out of scope:

- 改模块名为 `ruoyi-quartz` 或包名为 `com.ruoyi.quartz`
- 换 `ktg-common` / 其它模块
- Java 17、Boot 3/4、jakarta、前端
- 改调度业务语义、改 `sys_job` 表结构
- 五项接线（本块没有）

### Acceptance Criteria

- `ktg-quartz` 源码来自上述对照 commit 的 `ruoyi-quartz`，包名为 `com.ktg.quartz`
- 目录与 `artifactId` 仍为 `ktg-quartz`
- XML namespace 为 `com.ktg.quartz.mapper.*`
- JDK 8 下本模块（及 `-am` 依赖）`compiler:compile` 成功后再开始 `13`
- `ktg-mes` 无本单元 diff

### Rules and Constraints

Tenant isolation:

- 不涉及

Security and masking:

- 不在文档写作业调用的内部主机

Audit and lineage:

- 不改历史 SQL；不借机改 MES 任务

AI/rule-engine rules:

- 不涉及

Database rules:

- 不改 `sys_job` / `sys_job_log` 结构

## 2. Plan

### Implementation Approach

1. 确认 nested toplevel 是 `startech-mes-basic`；对照 worktree HEAD 等于总控 hash
2. 备份意识：覆盖前看 `git status`，不要盖掉用户未提交修改
3. 将对照 `ruoyi-quartz/src` 拷入 `ktg-quartz/src`（替换 Java + mapper XML）
4. 按对照 `ruoyi-quartz/pom.xml` 更新依赖声明，但 **artifactId/parent/groupId 按机械替换规则留在 ktg 坐标系**
5. 全模块替换 `com.ruoyi` → `com.ktg`
6. 若编译失败是因为引用了尚未换入的 3.9.2 common 新类型：**停止**，不要把 common 文件提前拷进本块；回报总控，必要时先做 `14`

对照与 nested 的 Java 文件名集合当前一致（18 个类型）。以对照树为准覆盖内容，不要只挑几个文件改。

### Affected Modules

- `ktg-quartz`
- 根 POM：仅当本块编译明确需要 2.5.15 / 其它 springboot2 属性时才改；能在现有 2.5.12 上编过则本块不碰根 POM

### Public Contracts

- HTTP：`SysJobController` / `SysJobLogController` 路径保持若依定时任务那套（换入后与 3.9.2 springboot2 一致）
- 不改管理端 Vue2 路由

### Persistence Changes

- 无

### Transaction Boundaries

- 沿用换入源码既有事务，不扩大

### Risks

- 对照用了 `master` 导致 jakarta 进 quartz
- parent 误写成 `com.ruoyi:ruoyi:3.9.2` 使聚合解析失败

### Verification Strategy

- `rg "com\\.ruoyi" backend/ktg-quartz` 应为 0
- `rg "com\\.ktg\\.mes" backend/ktg-quartz` 应为 0
- Maven compile（JDK 8 + antrun skip）

## 3. Task Breakdown

| Task | Description | Output | Verification |
| ---- | ----------- | ------ | ------------ |
| T1 | 确认对照 HEAD 与 nested 仓库根 | 命令输出 | hash = `913524b1525a0e374774afbb54c452d8f68a5a8f`；toplevel 含 `startech-mes-basic` |
| T2 | 覆盖 src + 调整 pom | `ktg-quartz` diff | 无 `com.ruoyi`；artifactId=`ktg-quartz` |
| T3 | JDK 8 编译 | 本文件第 4 节回填 | BUILD SUCCESS 后才能 `13` |

## 4. Verification Record

Commands run:

```text
# 对照 HEAD（外层只读 worktree）
git -C RuoYi-Vue-springboot2 rev-parse HEAD
# 913524b1525a0e374774afbb54c452d8f68a5a8f

# 实施在 nested 隔离 worktree，非用户 master 工作区
# worktree: .../startech-mes/.worktrees/spec12-ktg-quartz
# branch:   swap/spec12-ktg-quartz
# common dir 仍是 startech-mes-basic
git rev-parse --show-toplevel
# .../startech-mes/.worktrees/spec12-ktg-quartz

rg "com\\.ruoyi" ktg-quartz
# 0 matches (rg exit 1)
rg "com\\.ktg\\.mes" ktg-quartz
# 0 matches (rg exit 1)

export JAVA_HOME="$(/usr/libexec/java_home -v 1.8)"
# /Users/yuzaijiang/Library/Java/JavaVirtualMachines/corretto-1.8.0_502/Contents/Home
java -version
# openjdk version "1.8.0_502"
# OpenJDK Runtime Environment Corretto-8.502.07.1 (build 1.8.0_502-b07)

cd backend
mvn -pl ktg-quartz -am compiler:compile -DskipTests -Dmaven.antrun.skip=true
```

Result:

- `ktg-quartz` `src/` 已用对照 `ruoyi-quartz/src` 覆盖；全模块 `com.ruoyi` → `com.ktg`；目录与 `artifactId` 仍为 `ktg-quartz`；parent 仍为 `com.ktg:ktg:3.8.2`。模块 `pom.xml` 依赖已与上游同构，无需改坐标。
- `rg "com.ruoyi"` / `rg "com.ktg.mes"` 在 `ktg-quartz` 均为 0。
- `ktg-mes` 与根 `backend/pom.xml` 无本单元 diff。
- Maven：`ktg` SUCCESS，`ktg-common` SUCCESS，`ktg-quartz` FAILURE。**BUILD FAILURE**（JDK 8 Corretto 1.8.0_502）。未把 common 文件提前拷进本块。
- 未做运行时 Quartz 作业测试。

Missing 3.9.2 common APIs（停在本块；交给 `14` 换入 `ktg-common` 后再编 `ktg-quartz`）：

- `com.ktg.common.core.controller.BaseController.success(Object data)`（对照 ruoyi-common 有；现 ktg-common 仅 `success()` / `success(String)`）。导致：
  - `SysJobController.java:74` `success(SysJob)` → 不兼容的类型: SysJob 无法转换为 String
  - `SysJobLogController.java:66` `success(SysJobLog)` → 不兼容的类型: SysJobLog 无法转换为 String
- `com.ktg.common.utils.StringUtils.substringBetweenLast(String, String, String)`（对照 ruoyi-common 有；现 ktg-common 无）。导致：
  - `JobInvokeUtil.java:108` 找不到符号

按总控：本模块 compile 成功后再开始 `13`。当前 **不得** 据此声称 quartz 已编译通过。

Known gaps:

> 状态：`[ ]` 开放 · `[x]` 已收口（收口详情见 `docs/02.requirements/99.升级交付剩余缺口与收口清单.md`）

- `[x]` `ktg-quartz` 源码已按对照覆盖并完成 `com.ruoyi` → `com.ktg`
- `[x]` JDK 8 `compiler:compile`：integration `swap/spec12-16-integrate` 与合入 tip `swap/spec12-17-integrate` @ `f58079c`（Corretto 1.8.0_502）`ktg-quartz` SUCCESS。本块单独 worktree 仍因当时 common 未换入而失败。
- `[ ]` 未做运行时跑 Quartz 作业

## 5. Final Summary

Files changed:

- nested worktree `backend/ktg-quartz/src/**`（18 Java + 2 mapper XML）；`pom.xml` 未改（已与上游依赖对齐且保持 ktg GAV）
- 外层本 spec 第 4 节回填

Behavior delivered:

- `ktg-quartz` 内容与 springboot2 `ruoyi-quartz`（`913524b`）对齐，模块名/包名仍为 ktg。编译闭合阻塞在尚未换入的 `ktg-common` 3.9.2 API。

Next development unit:

- `docs/04.specs/13.swap-ktg-generator.md`（须等本模块 compile 成功；当前未通过）
