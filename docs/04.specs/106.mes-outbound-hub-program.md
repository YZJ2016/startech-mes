# Development Unit Template

Use this template before each implementation unit.

## 1. Spec

### Capability

- Name: MES 出库 Hub（切片总控）
- Module: 外层文档 `docs/04.specs/106`–`107`；实现将落 `startech-mes-basic/backend/ktg-mes`（wm）与 `startech-mes-front`（仅 Hub 页）。**禁止**改 Vue2。**本总控不改 Java。编码未授权。**
- Priority: P0（总控本身不改代码；编码单元 107 阻塞）
- Related requirement/design documents:
  - 对照总览：`docs/02.requirements/14.mes-kuaige-capability-overview.md`（KG-D；域行「出库」= 文档已开、编码未开）
  - 对照总控：`docs/04.specs/103.mes-kuaige-capability-program.md`
  - 缺口：`docs/02.requirements/15.mes-outbound-hub-gaps.md`（OH-*；指针 IH-G1）
  - 设计：`docs/03.design/15.mes-outbound-hub-plan.md`
  - 本总控：`docs/04.specs/106.mes-outbound-hub-program.md`
  - 编码单元：`docs/04.specs/107.outbound-hub-p0.md`（**未授权开工**）
  - 入库轨（只读，范围不变）：13 / 设计 14 / 104 / **105（仍是唯一进行中的编码切片）**
  - 租户前提（只读）：42（wm）；Flyway 05/75
  - 对照只读：`kuaigeyun-mes/`（不合并、不改写）

### Business Goal

在现有生产领料 / 销售出库单据上，提供一张出库 Hub：仓管一张表看两类单、抽屉看明细。旧菜单与旧 URL 保留。本文件只锁定原则、107 边界、编码门禁与决策表，不写 Java/SQL。

### Scope

In scope:

- 锁定本切片目标与非目标（对齐设计 15 第 1 节）
- 锁定已拍板 OH-D/I/F（不得在 107 重开，**OH-G1–G5 除外**）
- 锁定 106–107 文件名；规定缺口状态只回写 15（入库 IH-G1 只作指针）
- 规定 UI-A：107 只改出库 Hub 页及抽屉
- 规定 **编码门禁**（见下 TOC）

Out of scope:

- 本总控内改 Java、SQL、Vue
- 现在开始 107 编码
- 入库 104/105 范围变更（IQC 闸、入库 Hub、行三列等仍只在 105）
- CAT-W01 / W04 / W05 / W06 / W07
- 借料、送货单、委外、其它出库、采购退货、11 种类型
- 生产退料 / 销售退货进 Hub（默认；OH-G1）
- OQC 闸（OH-G2）、出库金额（OH-G5）、Hub 打印（OH-G4）
- Demand、改 Vue2、改若依壳、改 `/index`、改 `/ai/**`、改行业包
- 替换 `GET /mes/wm/issueheader/list` 等旧契约

### Acceptance Criteria

- 存在 106、107，五章齐全，Related 链到 15、设计 15、103、本文件、入库 105
- 已拍板项不得在 107 重开；OH-G1–G5 保持 `[ ]` 直到产品另开
- 对照清单无未处理待确认（冲突）；OUT-F02 范围待确认且 **默认不进 107**
- 本总控第 4 节不得写成「本文件已改 Java」或「107 已开工」
- 总览域地图出库行不是「进行中」；入库仍是唯一「进行中」

### Rules and Constraints

Tenant isolation:

- Hub SQL 永不跳过 `tenant_id`
- 只信 JWT/`TenantContext`；无客户端租户头

Security and masking:

- 新 GET 必 `@PreAuthorize`；不进 `MesPermitAllProvider`
- 日志不打印 token、JDBC、内部路径

Audit and lineage:

- 新 DDL 只进 Flyway `db/migration/{master,tenant}`
- 不改历史 `backend/sql/`

AI/rule-engine rules:

- 本切片不涉及 AI

Database rules:

- 菜单 9310–9319；不写 yml
- 不新建第三张出库头表；**不加金额列**（OH-G5）

### 已锁定开发单元 TOC

| 序号 | 文件 | 阶段 | 依赖 |
| ---- | ---- | ---- | ---- |
| 106 | `106.mes-outbound-hub-program.md` | 切片总控 | 15/设计 15；103；不改 105 范围 |
| 107 | `107.outbound-hub-p0.md` | Hub + 抽屉（仅领料+销售出库） | 106；**编码门禁** |

**门禁：**

| 门禁 | 硬条件 |
| --- | --- |
| 出库文档可当进度面 | 15 + 设计 15 + 106/107 已写 |
| **107 可编码** | （1）105 的 IH-X1/IH-X2 部署手验收口 **或** 用户明示「开工出库」/「105 已关闭」；（2）缺口 15 无未处理待确认（冲突）；（3）开工会话点名 107 |
| 旧领料/销售出库 URL 仍可用 | 107 不得删除或改 method |
| 105 仍是唯一编码切片 | 门禁未满足前禁止改 `startech-mes-basic` 出库 Hub 代码 |

当前：**门禁未满足。禁止开工 107。**

### 已拍板决策（禁止后续单元重开）

| 编号 | 选择（摘要） |
| ---- | ------------ |
| OH-D1–OH-D16 | 见缺口 15。Hub 两类型；附加一条 GET；UI-A；一步过账；无 OQC；无金额；9310 段。打印与金额见 G4/G5 |
| OH-I1–OH-I6 | JWT、Hub list 权限 |
| OH-F1–OH-F6 | 只 Vue3 Hub 页；不改旧 CRUD 皮肤 |

开放：OH-G1（退料/退货范围待确认，默认不进 107）、OH-G2、OH-G3、OH-G4、OH-G5（均不挡 105）。

## 2. Plan

### Implementation Approach

1. 本单元只维持出库 TOC 与门禁。不写逐步 SQL。
2. 107 一次交付（**仅当门禁满足**）：菜单 9310、Hub list（含仓库筛）、抽屉（现网明细/深链）、旧 PUT 执行。
3. 借料 / 送货 / OQC / 仓储其余块 / 金额 / 打印不进本 TOC。
4. 旧页不重排。

### Affected Modules

- 文档：15、设计 15、本文件、107；总览 14 号仅指针；入库 13 的 IH-G1 指针
- 编码（107，门禁后）：`ktg-mes` wm；Flyway master；`startech-mes-front` 出库 Hub 页
- 禁止：`frontend/`、`kuaigeyun-mes/`、`ktg-generator`、UReport、入库 105 范围内的 Java/Vue（除非另点名修入库）

### Public Contracts

- 保持：`/mes/wm/issueheader/**`、`/mes/wm/productsalse/**` 的 URL/method/信封
- 增量（门禁后）：`GET /mes/wm/outboundhub/list`

### Persistence Changes

- master：菜单 9310 段
- 无新业务头表；无金额列；本刀默认无 tenant 加列

### Transaction Boundaries

- Hub list 只读；执行事务仍在现网 execute

### Risks

- 门禁未开就改代码
- 把 CAT-W01/W04–W07 或借料/送货打进 107
- 把出库页一并改皮肤（违反 UI-A）
- 把 106 误当成授权开工

### Verification Strategy

- 见 107 第 2/3 节；本文件不跑命令
- 人工：04.specs 无同号；15/设计 15 链到 106/107；105 范围未写出库 Hub 实现

## 3. Task Breakdown

| Task | Description | Output | Verification |
| ---- | ----------- | ------ | ------------ |
| T1   | 锁定 106/107 与 OH 决策 | 本文件 | 不重开 OH-D；编码门禁写明 |
| T2   | 指向总览 14 与 103、入库 IH-G1 | Related 节 | 链接存在 |
| T3   | 把编码细节放到 107 | 107 五章 | 本文件无逐步 SQL；107 第 4 节未声称已改 Java |

## 4. Verification Record

Commands run:

```text
（总控不改代码，未跑 mvn / npm）
```

Result:

- 未改业务仓 `startech-mes-basic`
- 未改对照仓 `kuaigeyun-mes`
- 未声称构建成功
- 107 编码未开始

Known gaps:

> 状态：`[ ]` 开放 · `[x]` 已收口

- [x] 切片 TOC 已锁（文档）
- [ ] 107 编码门禁未开（待 105 手验或用户明示开工出库）
- [ ] OH-X1-code / OH-X2-code / OH-X1 / OH-X2
- [ ] OH-G1 / G2 / G3 / G4 / G5 仍开放（不挡 105）

## 5. Final Summary

Files changed:

- `docs/04.specs/106.mes-outbound-hub-program.md`

Behavior delivered:

- 出库切片原则锁定；无运行时变化；编码未开

Next development unit:

- `docs/04.specs/107.outbound-hub-p0.md`（**编码阻塞**；当前编码切片仍是 `105.inbound-hub-iqc-p0.md`）
