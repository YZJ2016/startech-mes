# Development Unit Template

Use this template before each implementation unit.

## 1. Spec

### Capability

- Name: MES 出库 Hub（切片总控）
- Module: 外层文档 `docs/04.specs/106`–`107`、`112`–`117`；实现落 `startech-mes-basic/backend/ktg-mes`（wm）与 `startech-mes-front`（仅 Hub 页）。**禁止**改 Vue2。**本总控不改 Java。**
- Priority: P0（总控本身不改代码；107 P0 代码已落，部署手验开放）
- Related requirement/design documents:
  - 对照总览：`docs/02.requirements/13.mes-kuaige-capability-overview.md`（KG-D；域行「出库」= 107 进行中，手验开放）
  - 对照总控：`docs/04.specs/103.mes-kuaige-capability-program.md`
  - 缺口：`docs/02.requirements/15.mes-outbound-hub-gaps.md`（OH-*；指针 IH-G1）
  - 设计：`docs/03.design/15.mes-outbound-hub-plan.md`
  - 本总控：`docs/04.specs/106.mes-outbound-hub-program.md`
  - 编码单元：`docs/04.specs/107.outbound-hub-p0.md`（P0 代码已落；部署手验仍开放；仅领料+销售出库）
  - 后续单元：`112.outbound-hub-other-borrow-p1.md`（**代码已落**；两套独立表）；`113.outbound-hub-delivery-p2.md`（代码已落；不是 `PRODUCT_SALSE`，默认不进 Hub UNION；菜单 9340–9346）；`114.outbound-hub-returns-gated.md`（OUT-F02 **已拍板不纳入出库**，归入库 IH-G10，不是出库 P3）
  - Hub 抛光：`115.outbound-hub-oqc.md`（**代码已落**）、`116.outbound-hub-print.md`（**代码已落**）、`117.outbound-hub-amounts.md`（**代码已落**）
  - 入库轨（只读，范围不变）：13 / 设计 14 / 104 / **105（已落地；本 TOC 不重开）**
  - 租户前提（只读）：42（wm）；Flyway 05/75
  - 对照只读：`kuaigeyun-mes/`（不合并、不改写）

### Business Goal

在现有生产领料 / 销售出库单据上，提供一张出库 Hub：仓管一张表看两类单、抽屉看明细。旧菜单与旧 URL 保留。本文件只锁定原则、107 边界、编码门禁与决策表，不写 Java/SQL。

### Scope

In scope:

- 锁定本切片目标与非目标（对齐设计 15 第 1 节）
- 锁定已拍板 OH-D/I/F（不得在 107 重开，**OH-G2/G4/G5 除外**，分别挂 115–117；OH-G1 已拍板不纳入）
- 锁定 106–107、112–117 文件名；规定缺口状态只回写 15（入库 IH-G1 只作指针）
- 规定 UI-A：107 只改出库 Hub 页及抽屉
- 规定 **编码门禁**（见下 TOC）；112–117 另须点名对应文件，**不得打进 107**

Out of scope:

- 本总控内改 Java、SQL、Vue
- 现在开始 112 / 113 / 114 / 115–117 编码（107 已在点名会话开工；112/113 代码已落，本总控不授权继续改）
- 入库 104/105 范围变更（IQC 闸、入库 Hub、行三列等仍只在 105）
- CAT-W01 / W04 / W05 / W06 / W07
- 委外发料、采购退货、拉单、撤回（OH-G3/G6/G7，无文件、不进本 TOC）
- 把借料/送货/退料退货打进 **107**（借料+其他出库→112；送货→113；退料/退货→114 已拍板不纳入出库，归入库 IH-G10，**不是出库 P3**）
- Demand、改 Vue2、改若依壳、改 `/index`、改 `/ai/**`、改行业包
- 替换 `GET /mes/wm/issueheader/list` 等旧契约

### Acceptance Criteria

- 存在 106、107、112–117，五章齐全，Related 链到 15、设计 15、103、本文件、入库 105
- 已拍板项不得在 107 重开；OH-G1 已拍板不纳入出库（114）；OH-G2/G4/G5 代码已落（115–117；部署手验仍开）
- 对照清单无未处理待确认（冲突）；OUT-F02 已拍板不纳入且 **不进 107 / 112**
- 本总控第 4 节不得写成「本文件已改 Java」
- 总览域地图出库行是「进行中」（107 手验）；入库 105 代码已落、不重开

### Rules and Constraints

Tenant isolation:

- Hub SQL 永不跳过 `tenant_id`
- 只信 JWT/`TenantContext`；无客户端租户头

Security and masking:

- 新 GET 必 `@PreAuthorize`；不进 `MesPermitAllProvider`
- 日志不打印 token、JDBC、内部路径

Audit and lineage:

- 新 DDL 只进 Flyway `db/migration/{master,tenant}`
- 不改历史 `backend/sql/`

AI/rule-engine rules:

- 本切片不涉及 AI

Database rules:

- 菜单 9310–9319；不写 yml
- 不新建第三张出库头表；**P0/本总控不在 107 加金额列**（OH-G5 / **117** 另开且**代码已落**）

### 已锁定开发单元 TOC

| 序号 | 文件 | 阶段 | 依赖 |
| ---- | ---- | ---- | ---- |
| 106 | `106.mes-outbound-hub-program.md` | 切片总控 | 15/设计 15；103；不改 105 范围 |
| 107 | `107.outbound-hub-p0.md` | P0 Hub + 抽屉（仅领料+销售出库） | 106；用户已点名开工；不含借料/送货/退料退货 |
| 112 | `112.outbound-hub-other-borrow-p1.md` | P1 其他出库 + 借料（OH-P1） | 106；**代码已落**；两套独立表/API/页；禁止打进 107 |
| 113 | `113.outbound-hub-delivery-p2.md` | P2 送货单（OH-P2） | 106；**代码已落**；不是 `PRODUCT_SALSE`；默认不进 Hub UNION |
| 114 | `114.outbound-hub-returns-gated.md` | 退料/退货**不进**出库 Hub（OH-G1；**不是出库 P3**） | 106；**已拍板不纳入**；归入库 Hub 余型（IH-G10）；负向单测锁排除 |
| 115 | `115.outbound-hub-oqc.md` | Hub 抛光：销售出库 OQC 闸（OH-G2） | 106；**代码已落**；不得打进 107 |
| 116 | `116.outbound-hub-print.md` | Hub 抛光：打印（OH-G4） | 106；**代码已落** |
| 117 | `117.outbound-hub-amounts.md` | Hub 抛光：金额（OH-G5） | 106；**代码已落**；不绑 110 |

**门禁：**

| 门禁 | 硬条件 |
| --- | --- |
| 出库文档可当进度面 | 15 + 设计 15 + 106/107 已写 |
| **107 可编码** | （1）105 的 IH-X1/IH-X2 部署手验收口 **或** 用户明示「开工出库」/「105 已关闭」；（2）缺口 15 无未处理待确认（冲突）；（3）开工会话点名 107 |
| **112 / 113 可编码** | 点名对应文件 + 107 完成或用户明示 + 表结构拍板；**不得并入 107** |
| **114 可编码** | 点名 114 + 走 **不纳入** 分支（已满足）：不改出库 UNION，负向单测 + 关 OH-G1。纳入出库 Hub 不再是本单元目标 |
| **115 / 116 / 117 可编码** | 点名对应文件 + 设计 §7.4 拍板项；**不得并入 107** |
| 旧领料/销售出库 URL 仍可用 | 107 不得删除或改 method |
| 105 不重开 | 107 只读入库 Hub 合并思路，不改入库 Java/Vue |

当前：**107 P0 代码已落**（OH-X1-code / OH-X2-code）。部署手验仍开放。112 其他出库+借料代码已落（两套独立表）。113 送货单代码已落（独立资源，不进 Hub UNION；菜单 9340–9346）。114 已按不纳入收口。**115 OQC / 116 打印 / 117 金额代码已落**（部署手验仍开）。不重开 105。

### 已拍板决策（禁止后续单元重开）

| 编号 | 选择（摘要） |
| ---- | ------------ |
| OH-D1–OH-D16 | 见缺口 15。P0 Hub 两类型；附加一条 GET；UI-A；一步过账；**P0** 无 OQC / 无金额 / 无打印；9310 段。抛光：OQC **115** / 打印 **116** / 金额 **117**（代码已落） |
| OH-I1–OH-I6 | JWT、Hub list 权限 |
| OH-F1–OH-F6 | 只 Vue3 Hub 页；不改旧 CRUD 皮肤 |

开放：OH-P1→112（**代码已落**）、OH-P2→113（**代码已落**）、OH-G2→115 / OH-G4→116 / OH-G5→117 **代码已落**、OH-G3/G6/G7 无文件。OH-G1→114 **已拍板不纳入出库**（归入库 IH-G10；不是出库 P3）。

## 2. Plan

### Implementation Approach

1. 本单元只维持出库 TOC 与门禁。不写逐步 SQL。
2. 107 一次交付（**仅当门禁满足**）：菜单 9310、Hub list（含仓库筛）、抽屉（现网明细/深链）、旧 PUT 执行。仅 `ISSUE` + `PRODUCT_SALSE`。
3. 其他出库+借料进 **112**；送货单进 **113**。退料/退货 **114** 已拍板不纳入出库（**归入库 Hub 余型 IH-G10；不是出库 P3**）。OQC / 打印 / 金额进 **115–117**（代码已落）。仓储其余块仍不进本 TOC。委外/拉单/撤回无文件。
4. 旧页不重排。

### Affected Modules

- 文档：15、设计 15、本文件、107、112–117；总览 13 号仅指针；入库 14 的 IH-G1 指针
- 编码（107，门禁后）：`ktg-mes` wm；Flyway master；`startech-mes-front` 出库 Hub 页
- 禁止：`frontend/`、`kuaigeyun-mes/`、`ktg-generator`、UReport、入库 105 范围内的 Java/Vue（除非另点名修入库）；现在禁止改 112/113 对应业务代码；114 仅负向单测、不改出库 UNION

### Public Contracts

- 保持：`/mes/wm/issueheader/**`、`/mes/wm/productsalse/**` 的 URL/method/信封
- 增量（门禁后）：`GET /mes/wm/outboundhub/list`

### Persistence Changes

- master：菜单 9310 段
- 无新业务头表；无金额列；本刀默认无 tenant 加列

### Transaction Boundaries

- Hub list 只读；执行事务仍在现网 execute

### Risks

- 门禁未开就改代码
- 把 CAT-W01/W04–W07 或借料/送货打进 107
- 把出库页一并改皮肤（违反 UI-A）
- 把 106 误当成授权开工

### Verification Strategy

- 见 107 第 2/3 节；本文件不跑命令
- 人工：04.specs 无同号；15/设计 15 链到 106/107；105 范围未写出库 Hub 实现

## 3. Task Breakdown

| Task | Description | Output | Verification |
| ---- | ----------- | ------ | ------------ |
| T1   | 锁定 106/107 与 OH 决策 | 本文件 | 不重开 OH-D；编码门禁写明 |
| T2   | 指向总览 13 与 103、入库 IH-G1 | Related 节 | 链接存在 |
| T3   | 把编码细节放到 107 | 107 五章 | 本文件无逐步 SQL；107 第 4 节未声称已改 Java |

## 4. Verification Record

Commands run:

```text
（总控不改代码，未跑 mvn / npm）
```

Result:

- 未改对照仓 `kuaigeyun-mes`
- 本总控仍不改 Java；107 编码见该 spec 第 4 节

Known gaps:

> 状态：`[ ]` 开放 · `[x]` 已收口

- [x] 切片 TOC 已锁（文档；含 112–117）
- [x] 107 编码已落（见 107 第 4 节；部署手验仍开放）
- [x] 114 已按不纳入收口（OUT-F02 / OH-G1；见 114 第 4 节）
- [x] 113 送货单编码已落（见 113 第 4 节；独立资源，不进 Hub UNION；菜单 9340–9346）
- [x] 112 其他出库+借料编码已落（见 112 第 4 节；两套独立表/页）
- [x] OH-X1-code / OH-X2-code（见 107 第 4 节）
- [ ] OH-X1 / OH-X2 部署手验
- [x] OH-G2-code 销售出库 OQC 闸（见 115 第 4 节；部署手验仍开）
- [x] OH-G4 打印代码已落（见 116 第 4 节；部署手验仍开）
- [x] OH-G5-code 出库金额（见 117 第 4 节；部署手验仍开）

## 5. Final Summary

Files changed:

- `docs/04.specs/106.mes-outbound-hub-program.md`

Behavior delivered:

- 出库切片原则锁定；107 P0 运行时行为见该 spec，本总控无 Java

Next development unit:

- `docs/04.specs/107.outbound-hub-p0.md`（P0 代码已落；手验开放）
- `docs/04.specs/112.outbound-hub-other-borrow-p1.md`（P1 代码已落）
- `docs/04.specs/113.outbound-hub-delivery-p2.md`（送货单代码已落；菜单 9340–9346）
- `docs/04.specs/114.outbound-hub-returns-gated.md`（已按不纳入收口）
- `docs/04.specs/115.outbound-hub-oqc.md`（OQC 代码已落；部署手验仍开）
- `docs/04.specs/116.outbound-hub-print.md`（打印代码已落；部署手验仍开）
- `docs/04.specs/117.outbound-hub-amounts.md`（金额代码已落；部署手验仍开）
