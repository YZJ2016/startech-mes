# Development Unit Template

Use this template before each implementation unit.

## 1. Spec

### Capability

- Name: 快格云对照纳入 — 能力总览总控
- Module: 外层文档 `docs/02.requirements/14.mes-kuaige-capability-overview.md` 与本文件。 **本文件不改 Java/SQL/Vue。** 第一刀编码单元是 105，不在本文件。
- Priority: P0（文档总控）
- Related requirement/design documents:
  - 活总览（原则 + 域地图 + 功能目录）：`docs/02.requirements/14.mes-kuaige-capability-overview.md`
  - 第一刀缺口/设计：`docs/02.requirements/13.mes-inbound-hub-iqc-gaps.md`、`docs/03.design/14.mes-inbound-hub-iqc-plan.md`
  - 第一刀 TOC：本文件 → `104.mes-inbound-hub-iqc-program.md` → `105.inbound-hub-iqc-p0.md`（已落地，不重开）→ 后续 108–111（Hub 抛光，代码已落、部署手验开放）→ `106`/`107`（出库 P0 代码已落、手验开放）→ 112/113（代码已落）→ 114（已拍板不纳入出库）→ 115–117（出库 Hub 抛光，编码未开）
  - 出库文档轨（拆自 IH-G1）：`docs/02.requirements/15.mes-outbound-hub-gaps.md`、`docs/03.design/15.mes-outbound-hub-plan.md`、`106.mes-outbound-hub-program.md` → `107.outbound-hub-p0.md` → `112`–`114` → 抛光 `115`–`117`（编码未开）；114 已拍板不纳入出库 Hub（归入库 IH-G10，不是出库 P3）
  - 他轨指针（不重开）：行业包 09/10/86；首页 07/08。**KU-AI 不在本对照目标范围**；主工程 AI 留在既有 03 等轨道
  - `AGENTS.md` 红线 2/3/5/8/11

### Business Goal

给对照纳入一条**可扫的进度面**：原则已锁、域状态可见、第一刀有独立缺口/设计/spec。禁止用一份总设计覆盖工单/仓储/质量/设备/销售。本文件只锁 TOC 与「不得在此编码」，不写入库闸算法（那是 13/14/105）。

### Scope

In scope:

- 锁定总览文件名与 KG-D 不得在切片重开
- 锁定当前对照 TOC：103（本文件）→ 104（入库总控）→ 105（入库 P0，已落地）→ 108–111（入库 Hub 抛光，代码已落、部署手验开放）→ 106（出库总控，无代码）→ 107（出库 P0，代码已落、手验开放）→ 112/113（出库后续，代码已落）；114 已拍板不纳入出库、归入库 IH-G10；115–117 出库抛光编码未开
- 规定后续域必须新开 `02.requirements` 缺口，并回写总览域地图；工单/质量其它闸/设备 **不预占 118+ 文件名**
- 入库余型（其他入库/还料/客供/Hub 余型/拉单/撤回）只在缺口 13 挂 **IH-G7–G12** 指针，**无** `04.specs` 文件；委外三类 **IH-G13** 默认后置，不进 104 TOC
- 规定切片编码前须有 KG-D9 对照清单，且无未处理「待确认」
- 规定本总控第 4 节不得写成已改 Java

Out of scope:

- 本文件内改 Java、SQL、Vue
- 规定 Demand / 销售 / 快财务 / 轻办公 的接口或表（出库接口只在 15/设计 15/106–107，不在本文件）
- 改 Vue2、`kuaigeyun-mes/`、行业包 86–88、首页 79–82、AI specs（KU-AI 不在本对照目标范围）
- 把所有域的 task 写进本章第 3 节

### Acceptance Criteria

- 存在 `docs/02.requirements/14.mes-kuaige-capability-overview.md`，含 KG-D1–D9、域地图与 **功能目录**（缺失/加深/不做）；禁止另开平行功能目录文件
- 域地图含 **轻办公**、**快财务** 行，状态为默认后置（不是进行中）
- 域地图含 **KU-AI** 行，状态为**明确不做本轨**（不在目标范围）；不是待吸收能力、不是后置切片
- 存在 104、105，且 13/14 的 Related 指向 104/105（不是把 103 当入库编码单元）
- 存在 15/设计 15/106/107/112–117；出库编码门禁写明；不预创建工单/质量/设备 spec
- 编码单元开工前：该域缺口有对照清单，且无未处理「待确认」（KG-D9）
- 域地图中「出库 Hub」为进行中（107 手验）；入库 105 代码已落、不重开；Demand 标明本轮不引入；壳层与 kiosk 为明确不做本轨
- 文档无真实地址、凭据
- 本总控第 4 节不声称测试/构建成功

### Rules and Constraints

Tenant isolation:

- 切片实现仍遵守 JWT/`TenantContext`；本文件无表

Security and masking:

- 不扩匿名面；不写密钥

Audit and lineage:

- 总览状态变更只改 14 号文件；不另开平行总览

AI/rule-engine rules:

- 本总控不改 `/ai/**`；KU-AI 不在本对照目标范围（总览 KG-D8）

Database rules:

- 本总控无 DDL

### 已锁定开发单元 TOC

序号接现有最大号，从 **103** 起已占用至 **117**。禁止改号、插空。工单/质量其它闸/设备等**不预占 118+ 文件名**，开域时按当时目录最大号顺延。

| 序号 | 文件 | 阶段 | 依赖 |
| ---- | ---- | ---- | ---- |
| 103 | `103.mes-kuaige-capability-program.md` | 对照总控（无代码） | 总览 14（含功能目录） |
| 104 | `104.mes-inbound-hub-iqc-program.md` | 入库切片总控（无代码） | 13/14；103 |
| 105 | `105.inbound-hub-iqc-p0.md` | 入库 Hub + IQC 闸编码（**已落地；手验开放**） | 104 |
| 106 | `106.mes-outbound-hub-program.md` | 出库切片总控（无代码） | 15/设计 15；103；不改 105 范围 |
| 107 | `107.outbound-hub-p0.md` | 出库 Hub P0 编码单元（代码已落；手验开放） | 106；用户已点名开工；不重开 105 |
| 108 | `108.inbound-hub-fqc-p1.md` | 入库 P1 FQC 闸（IH-G2；**代码已落**；半成品闸无新号） | 104；不改 105 范围 |
| 109 | `109.inbound-hub-iqc-history-p2.md` | 入库 P2 历史回填（IH-G3；**脚本已落**；**非对照菜单**，现网清洗） | 104 |
| 110 | `110.inbound-hub-amounts-p3.md` | 入库 P3 金额（IH-G5；**代码已落**；无价目） | 104 |
| 111 | `111.inbound-hub-print-p4.md` | 入库 P4 打印（IH-G6；**代码已落**；非 UReport） | 104 |
| 112 | `112.outbound-hub-other-borrow-p1.md` | 出库 P1 其他出库+借料（**代码已落**；两套独立表） | 106；禁止打进 107 |
| 113 | `113.outbound-hub-delivery-p2.md` | 出库 P2 送货单（**代码已落**；不是 `PRODUCT_SALSE`） | 106 |
| 114 | `114.outbound-hub-returns-gated.md` | 出库退料/退货**不进** Hub（**已拍板不纳入；不是出库 P3**） | 106；归入库 Hub 余型 IH-G10；负向单测锁排除 |
| 115 | `115.outbound-hub-oqc.md` | 出库抛光：OQC 闸（OH-G2；**编码未开**） | 106；不得打进 107 |
| 116 | `116.outbound-hub-print.md` | 出库抛光：打印（OH-G4；**编码未开**） | 106 |
| 117 | `117.outbound-hub-amounts.md` | 出库抛光：金额（OH-G5；**编码未开**） | 106；不绑 110 |

**门禁：**

| 门禁 | 硬条件 |
| --- | --- |
| 总览可当进度面 | 14 + 103 已写 |
| 切片可编码 | 该域缺口有对照清单；「待确认（冲突）」已用户拍板或书面停放 |
| 入库闭环可编码 | 上一条 + 105；本文件不授权把出库塞进 105 |
| 出库 107 可编码 | 105 IH-X1/IH-X2 部署手验收口 **或** 用户明示开工出库；本文件不授权开工 |
| 108–117 可编码 | 点名对应文件 + 各单元门禁；本文件不授权开工 |

**闸与金额不对称已收口占号：** 入库 IQC=105、FQC=108；出库 OQC=**115**。109 非对照菜单。110 入库金额；出库金额=**117**。打印：入库 111、出库 **116**。入库余型只有缺口 13 的 IH-G7–G13 指针、**无文件**。出库余型 OH-G3/G6/G7 同此。工单/质量其它闸/设备**不预占 118+**。

### 已拍板决策（禁止后续单元重开）

| 编号 | 选择（摘要） |
| ---- | ------------ |
| KG-D1–KG-D9 | 抄思路不抄仓；路径 A；UI-A；第一刀入库+IQC；快财务/轻办公默认后置；KU-AI 不在目标范围；对照清单制度 + 冲突升级用户；旧契约保留；Vue3+JWT |
| IH-D1–IH-D18 等 | 只在缺口 13；104/105 引用编号，不改选择 |

开放：总览域地图中「未开始」各域；入库 IH-G2/G3/G5/G6 已挂 108–111（Hub 抛光，代码已落、部署手验开放；**不是**其他入库/还料/客供）；IH-G7–G12 余型/拉单/撤回故意后开无文件；IH-G13 委外默认后置。出库 OH-P1/P2 已挂 112/113、OH-G1 已挂 114（**已拍板不纳入出库**，归入库 IH-G10）；OH-G2/G4/G5 已挂 115–117（编码未开）；OH-G3/G6/G7 无文件。IH-G1 已拆到缺口 15，仍不挡 105。入库待确认已清；出库待确认（冲突）无。

## 2. Plan

### Implementation Approach

- 本单元只维持总览与 TOC 指针。
- 编码从 105 开始，且须：用户审阅。对照清单待确认已清。
- 出库 106/107 文档已开且 107 P0 已编码；112/113 代码已落；114 已按不纳入收口；115–117 占号未编码。工单/质量/设备未选定前不新建 118。入库余型不另占 spec 号。

### Affected Modules

- 文档：14、13、15、03.design/14、03.design/15、103–117
- 代码：无（本文件）；105 入库已落地不重开；107 出库 P0 已编码；108–111 入库抛光已编码（部署手验开放）；112/113 已编码；114 已按不纳入收口（负向单测在 114）；115–117 编码未开

### Public Contracts

- 无新 HTTP（本文件）

### Persistence Changes

- 无

### Transaction Boundaries

- 无

### Risks

- 把 103 误当成「全 MES 重写」授权
- 在总览里写出入库以外的接口清单，绕过 02/03/04 分层

### Verification Strategy

- 人工：三目录无同号文件；13/14 链到 104/105；15 链到 106/107；总览链到 13 与 15

## 3. Task Breakdown

| Task | Description | Output | Verification |
| ---- | ----------- | ------ | ------------ |
| T1   | 写总览 14（KG-D + 域地图） | `14.mes-kuaige-capability-overview.md` | 含进行中入库行；轻办公/快财务为默认后置；KU-AI 为明确不做本轨；含 KG-D9 |
| T2   | 本总控锁定 103–117 | 本文件 | TOC 含入库后续 108–111 与出库 106/107/112–117；无工单/质量/设备预占 118+ |
| T3   | 入库 104/105 与 13/14 互链；出库 15/106 与 IH-G1 互链 | 104、105、106、107、修订 13/14 | 无悬空 103 当入库编码；107 未授权开工 |

## 4. Verification Record

Commands run:

```text
（本单元只写外层文档，未跑 mvn / npm）
```

Result:

- 未改 `startech-mes-basic` 与 `kuaigeyun-mes`
- 未声称构建成功

Known gaps:

> 状态：`[ ]` 开放 · `[x]` 已收口

- [x] 总览与 TOC 已落盘（本会话文档；含出库 106/107/112–117 与入库后续 108–111）
- [ ] 入库编码（105）手验未关
- [x] CAT-N1–N3：下一刀出库 Hub；仓储其余块各自开口；价目不绑 IH-G5
- [x] 出库 Hub 文档已开（15/设计 15/106/107 + 后续 112–117）；107/112/113 代码已落；115–117 编码未开
- [ ] 工单/质量其它闸/设备未开 spec
- [ ] 轻办公 / 快财务已上总览图，**未开缺口、未编码**
- [x] KU-AI **不在本对照目标范围**（KG-D8 / 明确不做本轨）；主工程 AI 留在既有轨道
- [x] KG-D9 对照清单制度已锁；入库 INV-G01/S02/X01 已纳入；**待确认无**；待补全已分类（F04/F08筛+深链/F09 纳入；F03→IH-G5；打印→IH-G6）

## 5. Final Summary

Files changed:

- `docs/02.requirements/14.mes-kuaige-capability-overview.md`
- `docs/04.specs/103.mes-kuaige-capability-program.md`

Behavior delivered:

- 对照纳入有进度面与原则锁；出库文档轨已挂 TOC；无运行时行为变化

Next development unit:

- 当前编码：`docs/04.specs/107.outbound-hub-p0.md`（手验）；入库 105 手验仍开放、不重开
- 入库后续 108–111：**代码已落盘**，部署手验仍开放。107 P0 / 112 P1 / 113 P2 已编码。114 已按不纳入收口。出库抛光 115–117 **编码未开**。余型无 118。
