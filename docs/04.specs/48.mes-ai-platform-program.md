# Development Unit Template

Use this template before each implementation unit.

## 1. Spec

### Capability

- Name: MES 多租户 AI 平台落地（总控）
- Module: 外层文档 `docs/04.specs/48`–`56`；实现落 `startech-mes-basic/backend`（新建 `ktg-ai`，以及 `ktg-admin` / `ktg-system` 许可接线）与 `startech-mes-basic/startech-mes-front`
- Priority: P0
- Related requirement/design documents:
  - 设计：`docs/03.design/06.mes-ai-platform-plan.md`
  - 缺口清单：`docs/02.requirements/03.mes-ai-platform-gaps.md`
  - Phase 1 内嵌助手（并行三单元，不含 RAG；不要用本总控替代）：`docs/02.requirements/04.mes-ai-phase1-gaps.md`，`docs/04.specs/57.ai-phase1-provider-model.md`，`58.ai-phase1-session-chat.md`，`59.ai-phase1-vue3-ui.md`
  - 双 LangChain4j BOM（仅 dependencyManagement，不改运行时）：`docs/04.specs/60.ai-langchain4j-bom.md`
  - ChatModel / Agent 决策（A24/A25）：`docs/03.design/06.mes-ai-platform-plan.md` §5.1 / §5.2（不占 spec 号）
  - LangChain4j 运行时改写（落实设计 A24+A25）：`docs/04.specs/61.ai-langchain4j-runtime.md`
  - 受控写 / MCP / 工作流决策（不占 spec 号）：`docs/03.design/06.mes-ai-platform-plan.md` §5.3（A26–A28）
  - A26 受控写 Tools（先做）：`docs/04.specs/62.ai-controlled-write-tools.md`
  - A27 白名单 MCP：`docs/04.specs/63.ai-mcp-whitelist.md`
  - A28 已登录工作流：`docs/04.specs/64.ai-logged-in-workflow.md`
  - MCP/工作流 Vue3 页：`docs/04.specs/65.ai-mcp-workflow-ui.md`
  - 租户总控（前提）：`docs/04.specs/35.mes-multi-tenant-program.md`
  - 本总控：`docs/04.specs/48.mes-ai-platform-program.md`
  - 下一编码单元：**65**（Vue3 MCP/工作流页）。64（A28）已编码（证据见该文件第 4 节）。不要从 49 重开

### Business Goal

在现行租户与认证之上自建 AI 平台：平台许可工厂开通 AI；工厂配置本租户模型与知识库；已登录管理端用户内嵌对话。不合并 `ruoyi-ai`。本文件只锁定原则、阶段与决策表，不写 Java/SQL。

### Scope

In scope:

- 锁定本期目标与非目标（对齐设计第 1 节）
- 锁定已拍板决策表（不得在后续单元重开）
- 锁定 48–56 文件名与序号
- 规定模块边界、HTTP 前缀、验证与安全底线
- 规定缺口状态只回写 `docs/02.requirements/03.mes-ai-platform-gaps.md`

Out of scope:

- 本总控内改 Java、SQL、Vue
- 改 Vue2 `startech-mes-basic/frontend/`
- 引入 Sa-Token、MyBatis-Plus、JPA、第二套 JWT / `SecurityFilterChain`
- 独立用户对话端、游客聊天
- 短剧 / Coding Harness / 匿名 `/workflow/run` / 内置 SQL MCP（Agent 见 A25 / 设计 06 §5.2；受控写/MCP/已登录工作流见 A26–A28 / 设计 06 §5.3，均不在本总控 49–56 内实现）
- 默认上传改 OSS
- 包/模块改名为 `com.ruoyi` / `ruoyi-*`

### Acceptance Criteria

- `docs/04.specs/` 存在 48–56 共 9 份单元，章节与 `docs/01.temlplates/01.development_unit_template.md` 一致
- 每份单元的 Related documents 链到设计、缺口清单、本总控、上一/下一单元
- 文档无真实地址、凭据
- 缺口产品决策已拍板；实现证据写在 49–56 第 4 节，不得在本总控第 4 节写成「本文件已改 Java」

### Rules and Constraints

Tenant isolation:

- AI 表 `tenant_id bigint NOT NULL`；加入 include 名单；无上下文失败关闭
- 登录后租户只信 JWT/`LoginUser`
- 未许可 `ai` 的工厂：`getRouters` 无 AI 菜单，`/ai/**` 403
- `DATABASE`：AI 表进专库 catalog；未就绪失败关闭，禁止回落主库
- 向量检索必须带 `tenant_id`（I-A3）

Security and masking:

- 不引入第二套认证；`/ai/**` 必须登录，不进 `MesPermitAllProvider`
- 模型 Key：AES-256-GCM，`AI_MODEL_CIPHER_KEY`；列表打码；VO 无明文/cipher（A20）
- 向量集群凭证只环境变量（A14）
- 日志不打印 Key / token / 内部路径（I-A5）
- 上传仍 `uploadMinio` + `tenant/{id}/`

Audit and lineage:

- 历史 SQL 不原地改写；AI 用新增量 `YYYYMMDD_NN_ai_*.sql`
- 许可变更走现有平台许可 API + `mes_module` 字典增量
- 操作日志可用现有 `@Log`

AI/rule-engine rules:

- 本期 49–56：OpenAI 兼容对话/embedding + 知识检索
- 当期改写（设计 A24+A25，代码单元 61）：Agent + 只读知识 Tool；仍禁止模型直连 MES 业务库与任意 SQL
- 61 之后（设计 06 §5.3）：受控写 Tools（B / **62 先做**）、白名单 MCP（63）、已登录工作流（64）、Vue 页（65）；仍禁止匿名 `/run` 与 `execute_sql_query`
- 禁止搬 ruoyi-ai 匿名 `/workflow/run` 与内置 SQL MCP

Database rules:

- 列约定与租户总控相同：`tenant_id`、复合唯一键
- 新表必须同时：DDL、include 名单、专库 catalog、单测
- 根键 `ruoyi:`；AI 配置占位符

### 已锁定开发单元 TOC

序号接现有最大号 `47`，从 **48** 起连续编号。新单元禁止插空；已落地的历史序号不得改号或挪作他用。已并回设计、磁盘上已无对应文件的空号可以回收。曾把旧决策壳并回设计 06。现行已落地最大实现单元为 **65**；**62–65** 已编码（证据见各文件第 4 节）。不要把 A26–A28 + Vue 塞进同一份。

| 序号 | 文件 | 阶段 | 依赖 |
| ---- | ---- | ---- | ---- |
| 48 | `48.mes-ai-platform-program.md` | 总控 | 租户 35 系列已存在 |
| 49 | `49.ai-foundation-p0.md` | P0 模块+许可 | 48 |
| 50 | `50.ai-model-p1.md` | P1 模型与密钥 | 49 |
| 51 | `51.ai-chat-p2.md` | P2 会话+SSE | 50 |
| 52 | `52.ai-frontend-p3.md` | P3 Vue3 内嵌 | 51 |
| 53 | `53.ai-knowledge-p4.md` | P4 知识库表/附件 | 49；前端可与 52 并行补页 |
| 54 | `54.ai-vector-oss-p5.md` | P5 三种开源向量 | 53 |
| 55 | `55.ai-rag-p6.md` | P6 RAG 挂对话 | 51 + 54 |
| 56 | `56.ai-dashvector-p7.md` | P7 DashVector | 54（建议 55 已能检索） |

Phase 1 内嵌助手不在上表：序号 **57–59**（`docs/02.requirements/04.mes-ai-phase1-gaps.md`）。**现行代码：** 49–56 已与 57–59 落在同一 `ktg-ai` / Vue3（许可闸、知识库、RAG、Weaviate/DashVector）。57–59 仍冻结对话 HTTP/表/三页契约，不得被后续单元改名。历史施工说明「Phase 1 只跑 57–59、不要执行 53–56」已过期，禁止再按「知识库未开工」实现。

依赖管理不在上表：序号 **60** `60.ai-langchain4j-bom.md`（父 POM 双 LangChain4j BOM）。运行时改写见 **61**（已落地）。

A24/A25 见设计 06 §5.1/§5.2，不占 spec 号。A26–A28 决策正文见设计 06 §5.3，不占 spec 号；**代码单元**为下表 62–64，Vue 为 65。

运行时改写不在上表：序号 **61** `61.ai-langchain4j-runtime.md`（`ktg-ai` 落实设计 A24 ChatModel 工厂 + A25 Agent；本单元改 Java。父 POM BOM 已由 60 导入，禁止再写 version。代码已落地，证据见该文件第 4 节）。

| 序号 | 文件 | 阶段 | 依赖 |
| ---- | ---- | ---- | ---- |
| 62 | `62.ai-controlled-write-tools.md` | A26 受控写 Tools（**已编码/验证中**） | 61；**禁止**塞进 61 |
| 63 | `63.ai-mcp-whitelist.md` | A27 租户加密白名单 MCP（**已编码**） | 62 之后 |
| 64 | `64.ai-logged-in-workflow.md` | A28 已登录 `/ai/**` 编排（**已编码**） | 不得抢 62；建议 63 之后（可与 63 解耦） |
| 65 | `65.ai-mcp-workflow-ui.md` | MCP/工作流 Vue3 页（非 Agent 页）（**已编码**） | 63+64 契约冻结后 |

**门禁：**

| 门禁 | 硬条件 |
| --- | --- |
| 工厂用户可见 AI 菜单 | 49 完成且该厂有效 `ai` 许可 |
| 可对话 | 50 + 51；52 完成后管理端可操作 |
| 对话可挂知识库 | 55 |
| `type=dashvector` | 56 |

### 已拍板决策（禁止后续单元重开）

| 编号 | 选择（摘要） |
| ---- | ------------ |
| A1–A28 | 见缺口清单：平台、OpenAI 兼容、加密落库、先三种向量再 DashVector、内嵌前端、`ktg-ai`+`/ai/**`、不合并 ruoyi-ai、父 POM 双 LangChain4j BOM（A23）、ChatModel 留 DB 并按 `(tenantId, modelId)` 懒缓存（A24）、Agent 进当期且一份定义+A24（A25）、受控写 B / 白名单 MCP / 已登录工作流（A26–A28）；匿名 `/run`/短剧/Harness/SQL MCP 仍不做（A22） |
| I-A1–I-A6 | include + 许可闸 + 向量带 tenant_id + TaskDecorator + 不打密钥日志 + `ruoyi:` 占位 |
| F-A1–F-A4 | 只 Vue3；不引入 x/UnoCSS/hook-fetch；对照 ruoyi-web 重做 |
| V1–V4 | 策略工厂；未知 type 失败关闭；一进程一种 |

## 2. Plan

### Implementation Approach

1. P0 建 `ktg-ai` 空模块、许可字典、`/ai/**` 鉴权骨架。
2. P1 模型 CRUD + 加密密钥 + OpenAI 兼容探活（可不对流式）。
3. P2 会话表 + `POST /ai/chat/send` SSE。
4. P3 Vue3 模型页与全宽对话页。
5. P4 知识库元数据与 MinIO 附件（尚不检索）。
6. P5 三种开源向量策略（配置激活一种）。
7. P6 对话可选 `knowledgeId` 检索拼上下文。
8. P7 DashVector 第四策略。

与租户并行：不改租户已拍板 D/F/S/I/R；AI 只追加表与许可码。

### Affected Modules

- 文档：`docs/02.requirements/03.mes-ai-platform-gaps.md`、`docs/03.design/06.mes-ai-platform-plan.md`、`docs/04.specs/48`–`56`
- 后端：新建 `ktg-ai`；`ktg-admin` 依赖与薄 Controller；`ktg-system` 许可字典/映射；`ktg-framework` include 名单；专库 catalog（`ktg-system` 既有 provisioner）
- 前端：仅 `startech-mes-front`
- 禁止：`frontend/`；`docs/superpowers/`；`ktg-generator` / UReport 除非后续单独立项

### Public Contracts

- 保持：现有登录/getInfo/getRouters/uploadMinio/信封
- 新增：仅 `/ai/**`（精确 path 分单元定）
- 不新增可伪造且登录后仍生效的租户 Header

### Persistence Changes

- 新建 `ai_*` 表（50/51/53）
- `mes_module` 字典增加 `ai`（49）
- include 名单与专库 catalog 随表扩大
- 不改 `sys_tenant` 结构

### Transaction Boundaries

- 模型保存：校验 + 加密 + insert/update 同一事务
- 会话发消息：先落 user 消息，再流式；assistant 落库在流结束（失败标错，不回滚用户消息）
- 知识解析：附件行与向量写入分步；解析失败保留附件、可重试，不删知识库登记

### Risks

- 把 ruoyi-ai 路径/认证抄进来撞 `/system/message`
- 向量只按 kid 隔离导致跨租户检索
- 密钥进日志或模型 VO
- 聊天口误进 permitAll
- 三种向量 SDK 同时初始化拖垮启动
- 未列入专库 catalog 的 `DATABASE` 租户写进主库

### Verification Strategy

- 总控只验收文档完整性与 TOC 锁定
- 各实现单元第 4 节：未执行前填「未执行 / 待本单元实现后填写」
- 后端：`cd startech-mes-basic/backend` 后受影响模块 `mvn test` 或 `-am compiler:compile`；写明是否跳过 antrun
- Vue3：`cd startech-mes-basic/startech-mes-front && npm run build:prod`（有前端改动的单元）
- 未实际跑通不得写「测试通过」

## 3. Task Breakdown

| Task | Description | Output | Verification |
| ---- | ----------- | ------ | ------------ |
| T1 | 锁定 TOC 并写本总控 | `docs/04.specs/48.mes-ai-platform-program.md` | 序号 48–56；门禁与决策表完整 |
| T2 | 拆 P0–P7 共 8 份单元 spec | `49`–`56` | 模板五章齐全；互相链接 |
| T3 | 写设计文 | `docs/03.design/06.mes-ai-platform-plan.md` | 决策表与形态齐全 |
| T4 | 写缺口清单 | `docs/02.requirements/03.mes-ai-platform-gaps.md` | 产品项已拍板；无敏感值 |

## 4. Verification Record

Commands run:

```text
（本总控为文档单元。实现验证见 49–56 各文件第 4 节。）
JAVA_HOME=Corretto-17.0.9  mvn -pl ktg-ai,ktg-admin -am compiler:compile -DskipTests  BUILD SUCCESS ~20:44
mvn -pl ktg-ai -am test  BUILD SUCCESS  33 tests
node v22.16.0  npm run build:prod  vite 6.4.1  exit 0
SQL 未应用到运行库；无 live Weaviate/DashVector/SSE
```

Result:

- 子单元 49–56 代码已落地（证据写在各文件第 4 节）。本总控仍不声称「本文件改了 Java」。
- 对账：对话下拉改为 `/ai/knowledge/options`；知识库 HTTP/Jackson、附件删除路径、SSE `{content, done}`、菜单 `ai/knowledge/index` + `ai:knowledge:*`（含 upload、parse）、知识库页调用 `POST /ai/knowledge/attach/{id}/parse`、Phase 1 `/ai/provider|model|session|chat`、上传 MinIO 均对齐。

Known gaps:

> 状态：`[ ]` 开放 · `[x]` 已收口（收口详情见 `docs/02.requirements/03.mes-ai-platform-gaps.md`）

- `[x]` A1–A28、I-A1–I-A6、F-A1–F-A4、V1–V4（产品决策；A23 见 `60`；A24 见设计 06 §5.1；A25 见设计 06 §5.2；A26–A28 见设计 06 §5.3）
- `[x]` 49–56 代码落地（各单元第 4 节；本文件不伪关闭剩余实现缺口）
- `[ ]` V2 完整 Milvus / Qdrant 适配（仅 weaviate；未知 type 失败关闭）
- `[x]` 知识解析 UI（Vue 知识库页调用 `POST /ai/knowledge/attach/{id}/parse`，权限 `ai:knowledge:parse`；未做浏览器点选）
- `[ ]` 运行时 live RAG / 向量库 / SSE（SQL 未执行）
- `[x]` `ktg-ai` LangChain4j 运行时改写（单元 61；Chat/Embedding 工厂 + Agent + 只读知识 Tool；证据见该文件第 4 节；无 live 模型）
- `[x]` 受控写 Tools（单元 62 / A26；证据见该文件第 4 节；无 live 报工）
- `[x]` 白名单 MCP（单元 63 / A27；证据见该文件第 4 节；无 live MCP）
- `[x]` 已登录工作流（单元 64 / A28；证据见该文件第 4 节；无 live 编排）
- `[x]` Vue MCP/工作流页（单元 65；证据见该文件第 4 节；`build:prod` exit 0；浏览器点选未做）

## 5. Final Summary

Files changed:

- `docs/02.requirements/03.mes-ai-platform-gaps.md`（本批新建）
- `docs/03.design/06.mes-ai-platform-plan.md`（本批新建）
- `docs/04.specs/48.mes-ai-platform-program.md`（本文件）
- `docs/04.specs/49`–`56`（本批新建）

Behavior delivered:

- 锁定 AI 平台分期与红线。子单元 49–56 与运行时 61 代码已落地（证据见各第 4 节）；live SQL / 向量 / SSE 仍待环境。

Next development unit:

- 62–65 已编码。不要把 Java 写进设计 06。不要从 49 重开。不单独立项 Agent 页。浏览器点选与 live 环境仍待本机后端。
