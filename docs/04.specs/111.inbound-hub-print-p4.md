# Development Unit Template

Use this template before each implementation unit.

## 1. Spec

### Capability

- Name: 入库 Hub P4 — Hub 打印（IH-G6）
- Module: 将来落 `startech-mes-front` 入库 Hub 打印入口，及若需则 `ktg-mes` 只读打印数据。**禁止**改 Vue2。**本单元编码未开。**
- Priority: P4（入库切片后续；**不**改 105 范围）
- Related requirement/design documents:
  - 缺口：`docs/02.requirements/13.mes-inbound-hub-iqc-gaps.md`（**IH-G6**、INV-F08 打印支、INV-A04）
  - 设计：`docs/03.design/14.mes-inbound-hub-iqc-plan.md`（§7）
  - 切片总控：`docs/04.specs/104.mes-inbound-hub-iqc-program.md`
  - P0：`docs/04.specs/105.inbound-hub-iqc-p0.md`（已规定无打印按钮，保持）
  - 出库打印不是本单元：缺口 15 **OH-G4**

### Business Goal

仓管在入库 Hub 能打印当前单据（或导出打印页）。105 明确无打印按钮。本单元才加 Hub 打印。不搬快格云打印框架 / `capabilities.print` 对象模型。

**现在不交付该行为。**

### Scope

In scope（**仅当编码门禁满足**）:

- Hub 页打印入口（浏览器打印或现网已有打印手段的薄封装）
- 权限与 Hub list / 单据 query 对齐；无权限不展示
- UI-A：只动 Hub；不重排旧入库 CRUD

Out of scope:

- **现在改 Java/Vue**；给 105 补打印按钮
- 快格云打印模板引擎、自定义字段附件管道（INV-F07）
- 出库 Hub 打印（OH-G4）、仓储其余块 CAT-W01/W04–W07
- 改 UReport 当本单元「库存报表」（CAT-W06 禁止混进）
- 改 Vue2、扩 permitAll

### Acceptance Criteria

- **编码门禁（当前未满足）：** 点名 111（建议 105 手验已关，非把打印塞进 105）。
- 门禁后：角色能进 Hub 时可打印本租户单据；他厂 id 403/当不存在；105 行为基线（无本单元时仍无打印）不变直至本单元合并
- **此刻不得写绿命令**

### Rules and Constraints

Tenant isolation:

- 打印数据查询带 `tenant_id`；禁止拼他厂单号
- 只信 JWT

Security and masking:

- 打印接口须登录 + `@PreAuthorize`（若新增 GET）
- 不进 `MesPermitAllProvider`；不打印内部路径

Audit and lineage:

- 若新增 API：旧 list/执行 URL 不变
- 模板若不进库则不要为打印新建业务头表

AI/rule-engine rules:

- 不涉及

Database rules:

- 默认无新业务列；禁止借打印加金额列（金额走 110）

## 2. Plan

### Implementation Approach

- **现在：** 只维持本 spec。
- **门禁后：** 优先浏览器 `print` + 现有 GET 头/行；确需专用 GET 再附加，不替换旧契约。禁止引入第二套组件库。

### Affected Modules

- Vue3 `inboundhub`；必要时 `ktg-mes` 只读 API
- 不改 `frontend/`、UReport 设计器、出库 Hub

### Public Contracts

- 保持入库/IQC 旧 URL
- 若新增：只读打印 GET（路径实现时定）

### Persistence Changes

- 默认无

### Transaction Boundaries

- 只读

### Risks

- 把 UReport / 库存报表当打印（CAT-W06）
- 打印接口匿名化
- 改旧 CRUD 加打印当本单元完成（UI-A 只允 Hub）

### Verification Strategy

- 门禁未开：第 4 节「未跑」
- 门禁后：本租户可打、跨租户拒绝；`npm run build:prod` 实跑后写第 4 节

## 3. Task Breakdown

| Task | Description | Output | Verification |
| ---- | ----------- | ------ | ------------ |
| T0   | 编码门禁 | 点名 111 | 未满足则 **停止** |
| T1   | Hub 打印入口 | inboundhub | 无权限隐藏；未改旧页布局 |
| T2   | 实跑验证 | 第 4 节 | 未跑不得写绿 |

## 4. Verification Record

Commands run:

```text
（编码门禁未开，未改 Java/Vue，未跑 mvn / npm）
```

Result:

- 未改业务仓；未声称测试/构建成功

Known gaps:

- [ ] 编码门禁未开
- [ ] IH-G6 仍开放

## 5. Final Summary

Files changed:

- `docs/04.specs/111.inbound-hub-print-p4.md`（仅文档）

Behavior delivered:

- 无运行时行为。入库 Hub 打印单元已落盘并显式阻塞

Next development unit:

- 入库 TOC 后续阶段至此。出库编码仍看 `107.outbound-hub-p0.md`（阻塞）
- 当前编码切片仍是 105
