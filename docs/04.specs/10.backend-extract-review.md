# 后端换壳前抽出：合入后二次审查

审查对象：`startech-mes-basic` nested repo（`git rev-parse --show-toplevel` = `.../startech-mes-basic`）。

| 项 | 值 |
| -- | -- |
| 分支 | `upgrade/ruoyi-framework` |
| HEAD | `0bdd2f4`（SQL 增量；其下 Java 树仍为 `100efd2` `把 Minio 入口和公告已读从平台模块迁出，避免换壳时丢掉对象存储上传和已读标记。`） |
| 基线 | `master` / `origin/master` = `f740339`（抽出前初始化点） |
| 对比 | `git diff master...upgrade/ruoyi-framework` |
| 相对 origin | `ahead 22`，**未 push** |
| 本轮操作 | **spec 09 纯度门确认**（2026-08-20）：nested 主 checkout 检索 + 对照根目录 `RuoYi-Vue` 3.9.2 文件差集 + JDK 8 编译。本任务 **无 nested 代码改动、无新 commit、未 push、未改 git config、未 `--no-verify`、未 amend**。未开始 RuoYi 换壳。确认期间并行任务将 SQL `20260820_00`–`08` 收成 `0bdd2f4`（仅 sql，Java 与 `100efd2` 相同）。 |

审查依据：`docs/04.specs/00.backend-extract-program.md` 至 `09.platform-purity-gate.md`、`AGENTS.md`。这是抽出分支全部合入之后的二次审查，不是再合一次。

合入记录（`master..HEAD`）：wave1 → 04 → 05 → 06 → 07 → 08 → 09，另有各 P0 修复 commit、合入后清理 `UserConstants` 残留心跳常量的 `f21ea86`、纯度清理 `4cfd619`、Minio/公告已读迁出 `100efd2`，以及并行 SQL 增量 `0bdd2f4`（不在本门禁改动内）。

---

## 1. 合入后结论（能否开始换壳）

**抽出合入完成；spec 09 纯度门已在 2026-08-20 按条款实测并通过（含书面豁免）。允许单独立项换壳；本轮确认任务不开始用 `RuoYi-Vue` 替换 `ktg-admin/common/framework/system`。**

平台四模块 Java 检索无 `import com.ktg.mes`、`PhoneUtils`、`FileCombine`、`cn.hutool`、`SysNoticeDetail`；framework 无 `com.ktg.mes` 类型。对照根目录 `RuoYi-Vue` 3.9.2（`com.ktg`≡`com.ruoyi`）后，平台 extra Java 仅 common `MesPermitAllProvider.java`。JDK 8 `compiler:compile` BUILD SUCCESS。未跑测试、未启动。

| 判定 | 说明 |
| ---- | ---- |
| 抽出方向 | 符合总控：MES 能力已进 `ktg-mes`，HTTP 路径保持，无第二套认证，无 `ruoyi-*` 模块替换，历史 SQL 未原地改写 |
| 上一轮 P0 是否仍阻塞 | **否。** FieldMeta bean、`/common/uploadMinio` 门面、常量收口、OAuth 占位均在合入结果中 |
| 二次审查五项 + 三项卡住遗留 | **已清。** `4cfd619` 五项，`100efd2` 三项（Minio 入口、公告已读字段、PhoneUtils） |
| 纯度门（spec 09） | **通过**（豁免见 09 第 4 节与本文件第 5 节） |
| 本轮开始换壳 | **否。** 门禁通过 ≠ 本确认任务实施换壳；换壳须新立项并处理第 7 节接线 |
| 运行时 / 测试 | 未做。**不宣称测试通过** |

故意留给换壳单元（已写入 09 豁免，不推翻纯度门）：

- 配置前缀仍为 `ktg-mes:`（换壳再映射 `ruoyi:`）
- Java 8 / javax / Spring Boot 2.5 与若依 3.9.2 的版本差
- `MesPermitAllProvider` 接口在 common，换壳改注册方式或保留接口

---

## 2. 上一轮阻塞复核

四个阻塞均按合入后的 `f21ea86` 树复核，不以单分支 worktree 为准。

### 2.1 FieldMetaObjectHandler 是否为 Spring bean 且被 `setPlugins` 注册 — **已修好**

- `com.ktg.mes.config.FieldMetaObjectHandler` 带 `@Component` 且 `@Intercepts` 实现 `Interceptor`。全仓仅此一个 `implements Interceptor`。
- `MyBatisConfig` 仍 `@Autowired(required = false) Interceptor[] interceptors`，非空时 `sessionFactory.setPlugins(interceptors)`。
- framework 不编译依赖 mes；admin 启动扫描 mes 后 bean 会注入。源码路径闭合。
- 未做运行时插入验证（`createBy` / `createTime` 是否真写入）。换壳前有环境应抽一条插入确认。

### 2.2 `POST /common/uploadMinio` 是否走 MES 门面；`/common/upload` 是否仍本地盘 — **已修好**

- 入口已迁到 mes `MesMinioUploadController`：仍 `@RequestMapping("/common")` + `@PostMapping("/uploadMinio")`，调用 `MesFileUploadUtils.uploadMinio(file)`；响应仍把对象键放进 `url` / `fileName` / `newFileName` / `originalFileName`（与迁前一致）。无权限注解（与迁前一致）。
- admin `CommonController` 只留本地盘 `/upload`、`/uploads`、下载。源码无 `import com.ktg.mes`。
- 头像 `SysProfileController` 仍本地盘。
- `WmBarcodeServiceImpl` 走同一门面。`FileUploadUtils` 无 Minio import/方法。common 无 `minio` 依赖。

### 2.3 UserConstants / MesConstants 收口 — **已修好**

- `UserConstants` 只剩若依通用段（`SYS_USER` / `NORMAL` / `YES`/`NO` / 菜单类型 / 用户名密码长度等）。检索无 `MESSAGE_STATUS_*`、`WEBSOCKET_HEARTBEAT`、`TRANSACTION_TYPE_`、`ITEM_TYPE_CODE`、`QC_TYPE_`、`BARCODE_TYPE_`、`CAL_SHIFT_`、`ORDER_STATUS_`、`PURCHASE_STATUS_`、`REPORT_JASPER_PATH`。
- `MesConstants` 同时含 wave1 业务常量 **和** `WEBSOCKET_HEARTBEAT`、`MESSAGE_STATUS_*`。`WebSocketServer` / 业务调用方引用 `MesConstants`。全仓无 `UserConstants.(ITEM_|TRANSACTION_|QC_|BARCODE_|CAL_|ORDER_STATUS_|PURCHASE_|MESSAGE_STATUS_|WEBSOCKET_)`。
- HEAD `f21ea86` 专门清掉 05 合入后留在 `UserConstants` 的心跳常量。字面量抽查未改。

### 2.4 TokenController OAuth 是否环境变量/占位、源码无硬编码 secret — **已修好**

- 实现已在 `ktg-mes.mobile.controller.TokenController`；admin 旧文件不存在。路径仍 `GET /loginOuth`。
- 五个字段均为 `@Value("${oauth.*:}")`；`application.yml` 仅 `${OAUTH_*}` 空占位。空配置走既有「配置有误」。
- **本文件与回复不复述任何旧密钥、地址或 AccessKey。** 抽出 diff 未向 Java 新写入明文 secret。`extract-06` 修复是从源码删除硬编码。既有债见第 4 节（不抄值）。

---

## 3. spec 01–09 通过表

图例：通过 = 相对该 spec 抽出目标已在合入树落地；缺口 = 未做、留给换壳、或纯度尾巴。

### 00 总控

| 项 | 结果 |
| -- | ---- |
| 波次未跳到换壳 | 通过。无 `ruoyi-*` 模块替换 |
| 只动后端、不升级栈 | 通过。Java 8 / Spring Boot 2.5 / Security `WebSecurityConfigurerAdapter` |
| 纯度门未过禁止换壳 | **已满足门槛。** 09 第 4 节已写明通过；本轮仍不实施换壳 |

### 01 MES 常量

| 项 | 结果 |
| -- | ---- |
| 新建 `MesConstants`，字面量与旧 MES 段一致 | 通过（含编码/仓储/质检/条码/班次/报表/转盘） |
| `ktg-mes` 不再引用 `UserConstants` 的 MES 符号 | 通过 |
| `UserConstants` 去掉 MES 业务段（含消息/心跳） | **通过**（合入树，含 `f21ea86`） |
| 保留若依通用常量 | 通过 |

### 02 自动编码

| 项 | 结果 |
| -- | ---- |
| AutoCode 实体/策略/Mapper/XML/三个 Controller 仅在 `ktg-mes` | 通过（包 `com.ktg.mes.autocode`）。平台模块无 `SysAutoCode*` / `AutoCodeUtil` |
| HTTP `/system/autocode`、`/get/{ruleCode}`、`/part`、`/rule` | 通过 |
| `PhoneUtils` | **已删。** 仅一处调用，手机号正则内联到 framework `UserDetailsServiceImpl` 私有方法；仍走既有 `selectUserByMobile` / `AuthenticationManager` / `TokenService`，无第二套认证。平台四模块无 `PhoneUtils` 类型 |
| hutool | **已从 common 移除。** `ktg-mes` 自声明；admin `SysLoginController` 未使用 hutool import 已删。平台四模块无 `cn.hutool` |
| GET 生成接口仍匿名 | 符合 02；08 保留 `/system/autocode/get/**` |

### 03 错层类型

| 项 | 结果 |
| -- | ---- |
| `ItemType` → `com.ktg.mes.md.domain`；Mapper XML 全名已改 | 通过 |
| `TreeSelect` 去掉 `ItemType` 构造 | 通过。common 无 `ItemType` 引用 |
| `BarcodeUtil` → `com.ktg.mes.wm.utils`；barcode4j/zxing 仅 mes | 通过。common pom 无 barcode/zxing/minio/ureport |
| `ExcelUtils` → `com.ktg.mes.common.utils` | 通过 |
| `BussinessException` 改为 `ServiceException` 并删除 | 通过 |

### 04 Minio / FileUploadUtils

| 项 | 结果 |
| -- | ---- |
| Minio 配置/工具/TaleUtils/UploadFilePathConfig 在 `ktg-mes.file` | 通过 |
| `MesFileUploadUtils` 门面 | 通过 |
| common `FileUploadUtils` 本地盘、无 Minio | 通过 |
| `/common/uploadMinio` 走门面；`/upload` 本地盘 | **通过。** 入口在 mes `MesMinioUploadController`，URL 未改 |
| `FileCombine` | **已删。** 全仓无调用方 |
| 配置前缀仍 `minio:`；抽出 yml 未写入新明文密钥 | 通过（OAuth 段仅为环境变量占位） |

### 05 消息 / WebSocket / 附件 / 公告已读

| 项 | 结果 |
| -- | ---- |
| WebSocket + MessageProvider + 消息/附件/NoticeDetail 在 `com.ktg.mes.msg` | 通过 |
| 路径 `/system/message`、`/mobile/system/message`、`/system/attachment`、`/system/notice/detail` | 通过 |
| WebSocket `/websocket/message/{token}` | 通过 |
| 增量 SQL `backend/sql/20260820_01_sys_notice_detail.sql`；未改 `mes_sys.sql` | 通过（含 `uk_notice_user`，注释说明历史拼写） |
| 平台 `SysNoticeMapper.xml` 无 `sys_notice_detail` join | 通过。列表 SQL 为 `sys_notice` + `sys_user`；已读由 mes `fillReadStatus` 填 |
| `MESSAGE_STATUS_*` / `WEBSOCKET_HEARTBEAT` 在 `MesConstants` | **通过** |
| `SysNoticeController` 仍在 admin 并依赖 mes | **已拆。** 已读列表迁到 mes `SysNoticeListController`，路径仍 `GET /system/notice/list`；admin 不再 import `com.ktg.mes.*`。换壳若依侧若自带 `/list` 会与 mes 映射冲突，须去掉若依 list 或本 mes mapping |
| `SysNotice.userId1` / `readStatus` | **已迁。** 平台实体与 Mapper resultMap 已去掉这两字段；mes `SysNoticeListVo` 承担 JSON `userId1`/`readStatus`，`fillReadStatus` 写入 VO。平台 `SysNotice` 仍有列表 join 用的 `userName`（非本轮范围） |

### 06 移动登录 KEEP

| 项 | 结果 |
| -- | ---- |
| 五类仅在 `com.ktg.mes.mobile`；路径不变 | 通过（`/mobile/login/**`、`/mobile/user/**`、`GET /loginOuth`） |
| 继续 `TokenService` / `AuthenticationManager` / `SysLoginService.loginOuth` | 通过。无第二套 JWT |
| OAuth 环境变量占位 | **通过** |
| 产品确认现场是否仍打 `/mobile/**` | 仍开放（spec 标明不阻塞抽出） |

### 07 UReport

| 项 | 结果 |
| -- | ---- |
| `RuoYiApplication` 无 `@ImportResource`、无 `UReportServlet` | 通过。启动类仅 `@SpringBootApplication` + Async/Scheduling |
| `UReportConfig` 在 `com.ktg.mes.report.config`，xml + `/ureport/*` Servlet | 通过 |
| `ureport2-console` 仅 `ktg-mes` | 通过 |
| `/ureportM` 未改 | 通过 |
| 运行时 Servlet / xml 从 jar 加载 | 未做 |

### 08 permitAll

| 项 | 结果 |
| -- | ---- |
| `SecurityConfig` 无 MES 路径字面量；读取 `MesPermitAllProvider` | 通过。检索 `/mes/`、`ureport`、`autocode`、`loginOuth`、`/mobile/` 为空 |
| 实现 `MesPermitAllProviderImpl` 在 mes | 通过。无笼统 `/mobile/**` |
| 未复制 JWT 过滤器 | 通过 |
| 接口在 framework 而非 common/mes | **已下沉 common。** `com.ktg.common.security.MesPermitAllProvider`；framework `SecurityConfig` 依赖该接口；实现仍在 ktg-mes。framework 源码无 `com.ktg.mes` 类型 |
| `/login` `/register` `/captchaImage` | 未改 |
| 未登录 401 探测 | 未做 |

路径 × 决策（合入树代码）：

| 原 matcher | 08 决策 |
| ---------- | ------- |
| `/mobile/login/**` | 保留匿名（显式含 `/mobile/login`） |
| `/mobile/**` | **收紧为需登录**（含 `/mobile/user/**`、`/mobile/system/message/**`） |
| `/ureport/**` | 保留匿名（原 `.anonymous()` → provider `.permitAll()`，已登录也可访问） |
| `/websocket/**` | 保留匿名（同样 anonymous → permitAll） |
| `/system/autocode/get/**` | 保留匿名 |
| `/uploadFiles/**` `/files/**` `/template/**` `/upload/**` | 保留匿名 |
| `/mes/tm/tool/importData` | **需登录** |
| `/mes/sys/camera/list` | **需登录** |
| `/loginOuth` | 保留匿名 |
| `/profile/**` | 留在平台 |

`/mobile/**` 收紧后，原先靠整段匿名才能打到的移动业务接口将 401。这是有意契约收紧。`sendCode` 仍在 `/mobile/login/**` 下匿名。未新增长期匿名面。

### 09 残留与纯度门

| 项 | 结果 |
| -- | ---- |
| Godex KEEP 迁入 `com.ktg.mes.godex`，`/godex`；admin 无 `controller/godex` | 通过。现场是否仍用未经确认 |
| FieldMeta 迁入 mes 且注册插件 | **通过**（源码；无运行时填充验证） |
| 删除 `SwaggerController`（`/tool/swagger`） | 通过。全仓无该类 |
| 删除 `ktg-admin/src/test/java/TestController.java` | 通过。`src/test` 无 Java |
| `ktg-admin/.../tool/TestController.java`（`/test/user` 示例 API） | 仍在 |
| `ktg-mes.md.controller.TestController` | 未动（本就在 mes） |
| 纯度检索 + 「纯度门通过」 | **通过。** 09 第 4 节已回填 Java 树 `100efd2` / 结束 HEAD `0bdd2f4`、逐条结果、豁免与 JDK 8 编译 |
| `ktg-mes:` 前缀 | **豁免**（故意保留到换壳） |

---

## 4. 新发现的必须修 / 可后续

合入本身不再被上一轮 P0 阻塞。下列按「换壳前是否会丢行为 / 违反 09」分类。

### 必须修（或先写入 09 豁免再换壳）

三项换壳前卡住的纯度遗留已在 `100efd2` 处理。09 第 4 节已回填并通过。下列是**换壳实施接线**，不是纯度失败：

| 项 | 说明 |
| -- | ---- |
| `MesPermitAllProvider` 在 common | 符合 08「接口在 common」，已写入 09 豁免。换壳替换 common 时需保留接口或改注册方式 |
| 09 第 4 节 | **已回填为通过**（2026-08-20；Java 树 `100efd2`，结束 HEAD `0bdd2f4`） |
| 手机号登录判断在 `UserDetailsServiceImpl` | 已无 `PhoneUtils` 类型。换壳替换 framework 时需并入 ruoyi 用户详情服务或放弃手机号登录 |
| mes `GET /system/notice/list` 与若依 admin `/list` | 换壳时须去掉一侧映射 |
| mes `POST /common/uploadMinio` 与若依 admin `/common` | 换壳时须保留 mes 入口，不要被若依 `CommonController` 覆盖或漏接 |

### 可后续（不阻塞宣布抽出合入完成）

| 项 | 说明 |
| -- | ---- |
| Godex 现场未确认 | KEEP 可逆性差；通知车间即可 |
| admin `tool/TestController`（`/test/user`） | 若依示例接口，换壳会自然消失或再删。含内存演示用户，非抽出引入 |
| `EnableScheduling` 仍在启动类 | 07 明确本单元不删 |
| 运行时回归 | Minio 上传、UReport 控制台、WebSocket、公告已读、Godex 打印、未登录 401、FieldMeta 插入填充 |
| 08 收紧后前端/旧 H5 必须带 JWT 访问 `/mobile/user/**` 等 | 产品/前端评估，不在本次编译范围 |
| `ktg-mes.md.controller.TestController` 等既有明文凭据 | **抽出 diff 未改这些文件。** 既有债：应从源码去掉明文、改环境变量并轮换。审查与文档不抄值 |

### 安全红线（抽出 diff）

- **新引入明文密钥：未发现。** Java diff 无新的硬编码 AccessKey/OAuth secret。`application.yml` 仅增加 OAuth 环境变量空占位。
- **既有债（不抄值，不在抽出 diff 内或仅为历史 yml）：** admin 多份 `application-druid*.yml` / `application-back.yml` 中的对象存储键；`ktg-mes` 调试 Controller 中的 RAM/OSS 明文。等级：高危既有债。处理：部署侧 Secret、轮换、禁止再写入文档/日志/commit message。
- 匿名面未扩大；08 收紧了工装导入、摄像头列表、整段 `/mobile/**`。
- `/system/autocode/get/**` 与 `/loginOuth` 仍匿名（与 02/06 一致，已知面）。
- 无第二套认证。

### 分层与循环依赖

Maven：`ktg-mes` → `ktg-framework` → `ktg-system` → `ktg-common`；`ktg-admin` → mes + framework。无 mes↔admin 循环。

换壳会丢的接线（非 Maven 环，是启动装配）：

- mes `SysNoticeListController` 承担 `GET /system/notice/list`（若依 admin 自带 `/list` 时需去掉一侧）
- mes `MesMinioUploadController` 承担 `POST /common/uploadMinio`
- common `MesPermitAllProvider` + framework `SecurityConfig` 注入；实现在 mes
- framework `UserDetailsServiceImpl` 内联手机号正则后走 `selectUserByMobile`（无 `PhoneUtils`、无 mes 类型）
- framework `MyBatisConfig` 运行时注入 mes 的 `FieldMetaObjectHandler`（无编译依赖，启动需要 mes 在 classpath）

---

## 5. 纯度门清单勾选

对照 `09.platform-purity-gate.md` Verification。检索范围：`ktg-admin` / `ktg-common` / `ktg-framework` / `ktg-system` 的 Java（类型应只在 `ktg-mes`）。`[x]` = 平台已清零或符合允许豁免；`[ ]` = 未过。

检索（只应出现在 `ktg-mes`，或写入豁免）：

- [x] `AutoCodeUtil` — 仅 mes
- [x] `MinioUtil` — 仅 mes
- [x] `MobileLoginService` — 仅 mes
- [x] `WebSocketServer` — 仅 mes
- [x] `MessageProvider` — 仅 mes
- [x] `SysAttachment` — 仅 mes（平台无）
- [x] `SysMessage` — 仅 mes（平台无）
- [x] `UReportConfig` / `UReportServlet` — 仅 mes；`RuoYiApplication` 无 UReport
- [x] 实体 `ItemType` — 仅 `com.ktg.mes.md.domain`；common 无
- [x] `BarcodeUtil` — 仅 mes
- [x] Godex — 仅 `com.ktg.mes.godex`；admin 无 `controller/godex`
- [x] `FieldMetaObjectHandler` — 仅 mes，且已 `@Component` + `setPlugins`
- [x] `SwaggerController` — 已删除
- [x] `UserConstants` 无 `ITEM_TYPE_CODE` / `TRANSACTION_TYPE_` / `QC_TYPE_` / `BARCODE_TYPE_` / `CAL_SHIFT_` / `ORDER_STATUS_` / `PURCHASE_STATUS_` / `REPORT_JASPER_PATH` / `MESSAGE_STATUS_` / `WEBSOCKET_HEARTBEAT`
- [x] `FileUploadUtils` 无 Minio
- [x] `SecurityConfig` 无 `/mes/`、`ureport`、`autocode`、`loginOuth`、`/mobile/`
- [x] `ktg-common/pom.xml` 无 minio / ureport / barcode4j / zxing / hutool
- [x] `ktg-mes/pom.xml` 有 minio / ureport / barcode4j / zxing / hutool
- [x] `ktg-quartz` / `ktg-generator` 无本次抽出额外业务类型
- [x] `FileCombine` — 已删除，平台与 mes 均无引用
- [x] `PhoneUtils` — **已删。** 手机号判断内联 `UserDetailsServiceImpl`；平台四模块无该类型
- [x] admin `SysNoticeController` — 已无 mes import；`GET /system/notice/list` 在 mes `SysNoticeListController`
- [x] `SysNotice.userId1` / `readStatus` — 已从平台实体与 Mapper resultMap 去掉；mes `SysNoticeListVo` 保持 JSON 字段名
- [x] `MesPermitAllProvider` — 接口在 `com.ktg.common.security`；framework 只依赖 common 接口；实现仍在 mes。framework 无 `com.ktg.mes` 类型
- [x] `hutool-all` — 已从 `ktg-common/pom.xml` 移除；平台四模块无 `cn.hutool`
- [x] admin `CommonController` — 已无 `import com.ktg.mes`；`POST /common/uploadMinio` 在 mes `MesMinioUploadController`
- [x] `RuoYiConfig` 前缀 `ktg-mes:` — **豁免**，已写入 09 第 4 节
- [x] 对照根目录 `RuoYi-Vue` 的平台文件差集 — **已跑**（3.9.2；平台 extra Java 仅 `MesPermitAllProvider.java`）
- [x] 09 第 4 节 Result 写明「纯度门通过」— **通过**（含豁免；2026-08-20；Java=`100efd2`）

`100efd2` 后平台四模块检索摘要：

- `import com.ktg.mes` / `PhoneUtils` / `FileCombine` / `cn.hutool` / `SysNoticeDetail`：无
- `userId1` / `readStatus`：平台 `SysNotice` 与 `SysNoticeMapper.xml` 无
- `MesPermitAllProvider`：仅 common 接口 + framework `SecurityConfig` 引用（允许）
- framework 无 `com.ktg.mes`

允许的既有豁免（**已写入 09 第 4 节**）：

- `RuoYiConfig` 前缀 `ktg-mes:`（换壳再映射 `ruoyi:`）
- admin 的 `application*.yml`（对照时不复制密钥）
- Java 8 / javax / fastjson1 / springfox 版本差
- `MesPermitAllProvider` 接口在 common，换壳改注册方式
- 平台 `SysNotice.userName`（spec 未要求去掉）
- admin `tool/TestController`（若依示例，非 MES 残留）

08 收紧后的运行时回归（有环境再做，不写内网主机）：

- 未登录：`/mes/tm/tool/importData`、`/mes/sys/camera/list`、`/mobile/user/**` → 401
- 未登录：`/mobile/login/loginByPassword`、`/loginOuth`、`/system/autocode/get/**`、`/ureport/**`、`/websocket/**` 仍可达
- 标准 `/login` 不受影响

---

## 6. 编译记录

本机默认 `java` 为 JDK 21。联编使用 `JAVA_HOME=C:\Program Files\Java\jdk1.8.0_492`（Corretto 8）。PowerShell 下 `-Dmaven.antrun.skip=true` 必须加引号，否则会被拆成非法 lifecycle phase。

09 确认实测（`100efd2`，未 `clean`；framework / quartz / mes 因时间戳重编，其余 up to date）：

```text
mvn -pl ktg-common,ktg-framework,ktg-system,ktg-mes,ktg-admin -am compiler:compile "-DskipTests" "-Dmaven.antrun.skip=true"
```

摘要（真实输出）：

```text
ktg ................................................ SUCCESS
ktg-common ......................................... SUCCESS   (up to date)
ktg-system ......................................... SUCCESS   (up to date)
ktg-framework ...................................... SUCCESS   (40 source files)
ktg-quartz ......................................... SUCCESS   (18 source files)
ktg-generator ...................................... SUCCESS   (up to date)
ktg-mes ............................................ SUCCESS   (721 source files；deprecation/unchecked warning：UploadFilePathConfig、UReportConfig)
ktg-admin .......................................... SUCCESS   (up to date)
BUILD SUCCESS
Total time:  52.172 s
Finished at: 2026-08-20T16:17:52+08:00
openjdk version "1.8.0_492"
```

`-am` 顺带编了 `ktg-quartz`、`ktg-generator`（admin 依赖）。未跑 `mvn test`。未启动应用。因此不得声称测试通过。

`ktg-admin` `generate-resources` 的 antrun 依赖前端 dist；本命令使用 `-Dmaven.antrun.skip=true`，只验证 Java 编译。

---

## 7. 换壳立项仍须处理（检查单）

纯度门已通过。下列是换壳**实施**接线，不是 09 失败项：

1. 映射 `ktg-mes:` → `ruoyi:`；带走 `MesPermitAllProvider` 接口或改注册方式。
2. 换壳若依 `SysNoticeController` 时处理与 mes `GET /system/notice/list` 的映射冲突；保留 mes `POST /common/uploadMinio`。
3. 有环境时做 FieldMeta 插入、`/common/uploadMinio`、UReport、WebSocket、公告已读、08 的 401 探测（不写内网主机）。
4. 对照 `RuoYi-Vue` 平台文件差集：**已做**（见第 5 节）；换壳时再对内容级 diff（版本差文件、手机号登录内联）。
5. 既有明文凭据在部署侧轮换，不要写进换壳 PR 描述。
6. **单独立项**替换四个平台模块；本确认任务不开始换壳。
