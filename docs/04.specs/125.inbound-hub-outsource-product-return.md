# Development Unit Template

Use this template before each implementation unit.

## 1. Spec

### Capability

- Name: 入库 Hub — 委外退货（IH-G13 臂 C：`OUTSOURCE_PRODUCT_RETURN`）
- Module: **`startech-mes-basic/backend/ktg-mes`**（新建委外退货头/行 + 过账；扩入库 Hub UNION）+ Flyway + Vue3。**禁止**改 Vue2；**禁止**合并快格源码。**本文件仅文档**；编码须点名 **125**。
- Priority: **默认后置**（**CAT-O01**）。硬依赖委外工单；强烈建议 **123 委外收货已落地**（退货关联原收货、可退数量来自已收合格量）。
- Related requirement/design documents:
  - 缺口 14 **IH-G13**；总览 **CAT-O01**；设计 14 §7.5；总控 104
  - 兄弟：`123` 收货、`124` 退料
  - 对照只读：`OutsourceProductReturn` / Hub `outsource_product_return`

### 拆分决策

见 **123** —— 三独立表/服务 → **123/124/125**。本文件 **只** 交付成品退回供应商（库存 **−1**）臂。

### Business Goal

委外收货后不合格成品退回供应商：仓管建 **委外退货单**（关联委外工单，可选关联委外收货单），校验可退数量后过账 **扣减库存**，并在入库 Hub 以 `OUTSOURCE_PRODUCT_RETURN` 列表可见（与快格一致：该类型出现在 **入库 Hub** 类型枚举，尽管库存方向为出）。

**现网事实（2026-09-17）：**

| 项 | 现网 | 125 假设 |
| --- | --- | --- |
| 委外退货表 | **无** | 新建 `wm_outsource_product_return*` |
| 123 收货 | 文档已开、代码未开 | 编码 125 时若 123 未落：仅允许工单级退货上限 **或** 停止（T0 默认：**要求 123 已落**） |
| 快格 capabilities | preview-only、不可 withdraw | 同 124：本单元不做撤回；127 矩阵默认 OUT |

**对照只读：** `OutsourceProductReturnService`：按收货汇总已退、校验数量；关联 `outsource_material_receipt_id` 可空。

### Scope

In scope:

- tenant：`wm_outsource_product_return` / `_line`
- master：菜单 **9410–9414**
- REST：`/mes/wm/outsourceproductreturn`；权限 `mes:wm:outsourceproductreturn:*`
- 规则：工单必填；可选 `outsource_receipt_id`；超量拒绝；一步过账 **`transactionFlag=-1`**
- Hub 臂 + Vue3 UI-A；抽屉执行调本 REST（**不是** 销售退货 `rtsalse`）

Out of scope:

- 123/124 实现并入；销售退货 118；采购退货（出库 OH-G3 残留）
- 122/126/127；结算/审核流

### Acceptance Criteria

1. 点名 **125** + WO +（默认）123 收货可读；否则停止。
2. 超量拒绝；执行后库存减少；Hub 可筛本类型。
3. 不与 `RT_SALSE` 混表；出库 Hub **仍不纳入** 本类型（114 精神：退货类展示在入库 Hub）。
4. §4 **已实跑（代码已落）**；部署手验仍开。

### 编码前拍板（T0）

| 项 | 会话默认 |
| --- | --- |
| 点名 **125** | **已满足**（本会话点名） |
| 依赖 123 | **硬依赖**（123 已落） |
| Hub 类型 | `OUTSOURCE_PRODUCT_RETURN` |
| 库存 | `TRANSACTION_TYPE_OUTSOURCE_PRODUCT_RETURN`，`-1` |
| 状态机 | 一步过账 |
| 菜单 | 9410–9414 |
| 撤回 | 默认不支持（127 OUT） |
| IQC/FQC | 无 |

### Rules and Constraints

- 租户失败关闭；JWT；Flyway；默认后置。
- 库存方向 **−1**；与 124 **+1** 严格区分。

### 附录 — HTTP

| 用途 | 路径 |
| --- | --- |
| CRUD/执行 | `/mes/wm/outsourceproductreturn/**` |
| 可退预览 | `GET …/preview?outsourceWorkOrderId=` |
| Hub | `receiptType=OUTSOURCE_PRODUCT_RETURN` |

---

## 2. Plan

### Implementation Approach

1. 确认 123 + WO → 点名 125。
2. DDL/REST/扣账/Hub/Vue。
3. 单测超量与库存方向。

### Affected Modules

- `ktg-mes` wm；Flyway；Vue3；只读 123 收货与 WO。

### Public Contracts

- 增量退货 REST + Hub 类型；不改 `rtsalse` 契约。

### Persistence Changes

- 退货头行表；菜单 9410 段。

### Transaction Boundaries

- 扣账与校验同事务；失败不回写工单已退量。

### Risks

| 风险 | 缓解 |
| --- | --- |
| 与销售退货混淆 | 独立类型；禁止改 118 |
| 库存方向写反 | 单测 −1 |
| 无收货可退 | T0 硬依赖 123 |
| Hub「入库」展示出库方向单据 | 文档与抽屉文案标明「委外退货」；对齐快格枚举 |

### Verification Strategy

- 单测 + build；手验 Hub。

---

## 3. Task Breakdown

| Task | Description | Output | Verification |
| ---- | ----------- | ------ | ------------ |
| T0 | 点名 125 + 123/WO | 门禁 | 停止条件 |
| T1 | DDL/菜单 | Flyway | 路径 |
| T2 | REST/−1 过账 | Service | 单测 |
| T3 | Hub + Vue | 臂/抽屉 | UI-A |
| T4 | §4 | 命令 | 未跑不绿 |

---

## 4. Verification Record

Commands run:

```text
cd startech-mes-basic/backend
mvn -pl ktg-mes -am test "-Dtest=WmOutsourceProductReturnP125Test,InboundHubOutsourceProductReturnVueTest,WmInboundHubMapperXmlTest,WmInboundHubTenantRewriteTest" -DfailIfNoTests=false "-Dsurefire.failIfNoSpecifiedTests=false"

cd startech-mes-basic/startech-mes-front
npm run build:prod
```

Result:

- Focused tests: **20 run, 0 failures** (`WmOutsourceProductReturnP125Test` 11 + Vue 3 + Hub mapper/tenant rewrite).
- `npm run build:prod`: **EXIT=0** (chunks include `outsourceproductreturn`).
- **Menus:** **9410–9414**.
- **Hub arms after this unit:** **11**（prior 9 via 123 + sibling **124** `OUTSOURCE_MATERIAL_RETURN` + this `OUTSOURCE_PRODUCT_RETURN`）.
- **Flyway:** tenant `V20260916.44__tenant_outsource_product_return.sql`；master `V20260916.44__master_outsource_product_return_menu.sql`（**.44**；**.43** 留给 124）.
- **REST:** `/mes/wm/outsourceproductreturn` (+ line + preview)；库存 `processOutsourceProductReturn` → `transactionFlag=-1`.
- 124 已独立落地（**代码已落**；部署手验仍开）；did not add Hub withdraw for OUTSOURCE（127 owns）；did not touch Vue2.

Known gaps:

- [x] 125 代码（退货 + Hub 臂 + Vue3）；依赖 123 收货/WO 已可读
- [ ] 部署手验：建 WO → 收货 → 退货过账扣库存 → Hub 筛 `OUTSOURCE_PRODUCT_RETURN`
- [x] 124 独立跟踪（**代码已落**；部署手验仍开）

---

## 5. Final Summary

Files changed:

- `docs/04.specs/125.inbound-hub-outsource-product-return.md`（本文件）
- `startech-mes-basic/backend/ktg-system/.../V20260916.44__tenant_outsource_product_return.sql`
- `startech-mes-basic/backend/ktg-system/.../V20260916.44__master_outsource_product_return_menu.sql`
- `startech-mes-basic/backend/ktg-mes` OutsourceProductReturn domain/mapper/service/controller + Hub UNION 臂 + StorageCore −1
- `startech-mes-basic/startech-mes-front` `outsourceproductreturn` + Hub 分支（`<!-- spec 125 -->`）

Behavior delivered:

- 委外退货一步 `PREPARE`→`FINISHED`；库存 −1；Hub `OUTSOURCE_PRODUCT_RETURN`；可退校验（工单或可选收货）；无 IQC/FQC；无撤回

Next development unit:

- 部署手验本单元；124 退料（若未落）可并行。
