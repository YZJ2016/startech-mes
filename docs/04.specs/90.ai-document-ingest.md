# Development Unit Template

Use this template before each implementation unit.

## 1. Spec

### Capability

- Name: 知识附件生产级解析与 ingest
- Module: **`startech-mes-basic/backend/ktg-ai`**（Parser / Splitter / fragment / 向量写入；切块列与 `sys_config` 回落）。**禁止**改 Vue2。知识库表单见 **92**。
- Priority: P1（本轨编码第一刀）
- Related requirement/design documents:
  - 缺口：`docs/02.requirements/10.mes-ai-production-rag-gaps.md`（**PR-D3–D5、D15、D17、D18、D19 后端回落**）
  - 设计：`docs/03.design/11.mes-ai-production-rag-plan.md` §4、§6.1
  - 总控：`docs/04.specs/89.ai-production-rag-program.md`
  - 现网解析：`docs/04.specs/54.ai-vector-oss-p5.md`、`53.ai-knowledge-p4.md`
  - 切块基线：`docs/04.specs/69.ai-rag-pipeline.md`（overlap/分隔符语义保留）
  - BOM：`docs/04.specs/60.ai-langchain4j-bom.md`
  - 上一单元：`docs/04.specs/89.ai-production-rag-program.md`
  - 下一单元：`docs/04.specs/91.ai-advanced-rag.md`（检索）；管理页 `docs/04.specs/92.ai-knowledge-admin-ui.md`（可并行）

### Business Goal

`POST /ai/knowledge/attach/{id}/parse` 对 PDF / Office / txt / md 抽出可读正文，按 Ingestor 四段切块写入 fragment 与现网向量库。不再把整文件当 UTF-8 字符串视为成功。

### Scope

In scope:

- `ktg-ai` 增加无 version：`langchain4j-document-parser-apache-pdfbox`、`langchain4j-document-parser-apache-poi`、`langchain4j-document-parser-apache-tika`；BOM 含 markdown 解析器则加，否则 md 走 Text
- 从 MinIO 流解析；按扩展名路由；冲突时 Tika 探测；空文本失败
- `DocumentTransformer` / `DocumentSplitters.recursive`（overlap>0）+ 知识库 `chunk_separator` / `chunk_size` / `chunk_overlap`（空则 `sys_config`） / `TextSegmentTransformer`
- 写 `ai_knowledge_fragment`（可选 Flyway `source_ref`）；向量元数据仍含 `tenant_id`、kid、docId、fid；写入的集群按当前 `TenantContext` 解析（93：DEDICATED 否则 yml/env）
- Flyway **tenant**：`ai_knowledge` 增 `chunk_size` / `chunk_overlap`（int NULL，空=回落）。实体/Mapper/XML 同步；JSON camel `chunkSize` / `chunkOverlap`
- Flyway **master**：`sys_config` 全局种子 `ai.rag.chunk-size=500`、`ai.rag.chunk-overlap=200`、`ai.rag.chunk-separator=\n\n`、`ai.rag.top-k=5`、`ai.rag.min-score=0`（`tenant_id` NULL，对齐 `mes.index.layout`；无既有 `ai.*` 键）。91 的展开/再排序键可另脚本，前缀同 `ai.rag.`
- `AiKnowledgeChunker` / `parseAttach` ingest：切块参数 **知识库列 → `ISysConfigService.selectConfigByKey` → 与种子相同的硬编码**。拆除 `AiProperties` rag `@Value` 与 yml `ruoyi.ai.rag.*` / `AI_RAG_*`（向量集群 `@Value` **本单元不拆**；SHARED 产品面仍是 yml，专绑定见 **93 / PR-D20**）
- embedding 走 A24；支持则 `DOCUMENT` input type
- 单测：夹具 PDF 或 DOCX 抽出非乱码关键词；纯文本仍通过；空/损坏文件 `parse_status=2`；跨租户 objectKey 仍 403；**禁止**断言 Easy RAG

Out of scope:

- RetrievalAugmentor（91）
- 知识库管理页 Vue（92：embedding 下拉、chunkSize/overlap 表单、片段浏览、浏览器点选）
- 向量集群平台页与每厂绑定（**93** 无默认页；SHARED=yml；禁止把 host/type 写进本单元或知识库表单）
- 把 RAG 默认写回 yml / 新 `@Value`；双源（yml + sys_config）
- OCR、加密 PDF 破解
- 换 `VectorStoreService` 实现、连 live 集群、改向量集群产品面（**93**）与开通向导（**94**）
- `langchain4j-easy-rag`、`InMemoryEmbeddingStore`
- 改 parse URL、改 Vue2

### Acceptance Criteria

- 允许类型主路径不是 `new String(bytes, UTF-8)`
- overlap>0 且 < chunk_size（现网夹取：overlap≥size → `size-1`）；分隔符仍优先于纯窗口
- 空列不读 yml：单测断言走 `sys_config`（或硬编码与种子相同的 500/200/`\n\n`）
- 无 embedding 模型 → 失败关闭，不标解析成功
- 第 4 节未跑通不得写「测试通过」
- 文档无真实主机/凭据

### Rules and Constraints

Tenant isolation:

- 读对象键前缀仍 `tenant/{id}/`
- fragment INSERT 填当前租户
- 向量写入走当前 `TenantContext` 解析的 store（有 DEDICATED 绑定用该厂客户端，否则平台默认）；禁止空上下文

Security and masking:

- parse_msg 短句；日志不打印存储凭据与绝对路径

Audit and lineage:

- `ai_knowledge` 新列只进 `db/migration/tenant/`；`sys_config` 种子只进 `db/migration/master/`（I8 全局表，不进专库）
- 不改已发布 `backend/sql/20260823_24*` / `20260825_29*`；不把密钥写入种子

AI/rule-engine rules:

- 切块用 LC4j DocumentSplitter，不把 `AiKnowledgeChunker` 当 PDF 主路径（纯文本可作 splitter 底层）；splitter 的 size/overlap/separator 解析规则与本单元 ingest 相同（列 → sys_config）

Database rules:

- include 已含三表则不扩匿名表

## 2. Plan

### Implementation Approach

1. POM 加 parser 坐标，无 version。
2. 解析器注册表：扩展名 → Parser；失败再 Tika 探测一次。
3. `parseAttach` 改为 InputStream → Document → ingest 四段 → 原 `storeVectors`。
4. 不改知识库 Vue 表单（92）。本单元负责列、Mapper、ingest 读路径与拆除 yml rag 键。

### Affected Modules

- `ktg-ai/pom.xml`、knowledge service / Mapper、`AiProperties` rag 字段拆除、Flyway tenant + master、`application.yml` 删除 `ruoyi.ai.rag.*`

### Public Contracts

- **冻结：** `POST /ai/knowledge/attach/{id}/parse`（URL / method / 认证不变）
- 知识库 POST/PUT 增量可选 `chunkSize` / `chunkOverlap`（camel）；不新 path。`embeddingModelId` 后端语义不变（空则第一条 vector）；下拉属 92

### Persistence Changes

- tenant：`ai_knowledge.chunk_size` / `chunk_overlap` int NULL；可选 `ai_knowledge_fragment.source_ref varchar(128)`
- master：`sys_config` 五键全局种子（见 Scope）。`config_type='Y'`、无主机/口令。不插默认厂 overlay

### Transaction Boundaries

- 先删旧 fragment 再插入；向量失败标 2 可重试（现网）

### Risks

- POI/PDFBox 版本与 Boot 4
- 大 xlsx 内存；可按现网 20MB 上限拒绝

### Verification Strategy

- `mvn -pl ktg-ai -am test`（Java 17）
- 不连 live MinIO/向量也可：mock ObjectStore + Fake VectorStore + classpath 夹具

## 3. Task Breakdown

| Task | Description | Output | Verification |
| ---- | ----------- | ------ | ------------ |
| T1 | parser 依赖 + 路由 | ktg-ai | 编译；无 easy-rag |
| T2 | parseAttach ingest 四段 | Service | PDF/DOCX 夹具；空文件失败 |
| T3 | tenant 列 chunk_size/overlap + Mapper | Flyway tenant + XML | 空列回落 sys_config；不改历史 sql/ |
| T4 | master 种子 ai.rag.* 五键；拆除 yml rag `@Value` | Flyway master + AiProperties | 不读 `AI_RAG_*`；向量 yml 键本单元不动（93） |
| T5 | 可选 source_ref Flyway | tenant SQL | 无密钥 |
| T6 | 回填第 4 节 | 命令 | 未跑通不得写通过 |

## 4. Verification Record

Commands run:

```text
cd /Users/yuzaijiang/workspaces-all/llm-workspace/startech-mes/startech-mes-basic/backend
# JAVA_HOME=Corretto 17.0.20.1（x86_64）
mvn -pl ktg-ai,ktg-admin -am test
```

Result:

- `BUILD SUCCESS`（2026-09-08T20:52:30+08:00）。`ktg-ai` Tests run: 336, Failures: 0；`ktg-system` Tests run: 117, Failures: 0（含 `FlywayMigrationNamingTest` 3）；`ktg-admin` Tests run: 16, Failures: 0。
- `AiKnowledgeParseTest` 5 条：PDF 夹具抽出 `SOP-STEP-THREE`；空文件 / 损坏 PDF → `parse_status=2`；无 embedding 不标成功；切块空列走 `selectConfigByKey`。跨租户 objectKey 仍 403（既有 `AiKnowledgeIsolationTest`）。
- **未连** live MinIO / 向量集群。**未**浏览器上传 PDF。日志与测试无凭据。

Known gaps:

> 状态：`[ ]` 开放 · `[x]` 已收口（收口详情见 `docs/02.requirements/10.mes-ai-production-rag-gaps.md`）

- `[x]` PR-X1 编码（本命令；点选/live 未做）
- `[x]` PR-D17 / PR-D19 切块回落改 `sys_config`（本单元；证据回缺口 `10`）
- `[ ]` PR-G1 OCR
- `[ ]` live MinIO / 向量；浏览器 PDF 点选
- 父 POM 钉 PDFBox 2.0.x，LangChain4j `ApachePdfBoxDocumentParser` 需要 3.x `Loader`。生产路径用 PDFBox 2 `PDDocument.load` 包装器（`AiDocumentParsers.PdfBox2DocumentParser`），POM 仍保留无 version 的 `langchain4j-document-parser-apache-pdfbox`。
- `ktg-ai` 对 `ktg-mes` 排除 easyexcel 拉入的 `poi-ooxml-schemas`，以免挡住 POI 5 `DocumentFactory`。Office 解析仍依赖运行时 POI 5 胜出；若其他模块把 POI 4 schemas 再打进 classpath，xlsx/docx 可能失败。未做 live Office 文件验收。
- Tika：仅扩展名与 MIME 冲突时先试；MIME 一致时命名 Parser 失败不再回退 Tika（相对 PR-D3「未知允许类型回退」的实现收窄）。

## 5. Final Summary

Files changed:

- `startech-mes-basic/backend/ktg-ai/pom.xml`（parser 坐标；排除 easyexcel 的 `poi-ooxml-schemas` 以免挡住 POI 5）
- `ktg-ai/.../runtime/rag/AiDocumentParsers.java`、`AiKnowledgeDocumentSplitter.java`、`AiEmbeddings.java`、`AiRagSettings.java`
- `ktg-ai/.../AiKnowledgeServiceImpl.java`、`AiKnowledge.java`、`AiKnowledgeFragment.java`、Mapper XML
- `ktg-ai/.../config/AiProperties.java`（拆除 rag `@Value`）；`ktg-admin/.../application.yml`（删除 `ruoyi.ai.rag.*`，保留向量集群键）
- Flyway tenant `V20260908.34__tenant_ai_knowledge_chunk.sql`；master `V20260908.35__master_ai_rag_sys_config.sql`
- 单测 `AiKnowledgeParseTest.java`；`FlywayBaselineBehaviorTest` / `FlywayMigrationNamingTest` 适配切线后增量（历史 `V20260902.01__ai_session_agent_id.sql` 未改名）

Behavior delivered:

- `POST /ai/knowledge/attach/{id}/parse` 按扩展名走 PDFBox 2 包装器 / POI / Markdown|Text；从 MinIO 字节流解析，不再把整文件 UTF-8 当成功。
- 切块：知识库列 → `sys_config` `ai.rag.*` → 硬编码 500/200/`\n\n`。fragment 保留 tenant/kid/docId/fid，可选 `source_ref`。无 embedding 失败关闭。

Next development unit:

- 检索：`docs/04.specs/91.ai-advanced-rag.md`。管理页：`docs/04.specs/92.ai-knowledge-admin-ui.md`（可与本单元并行）。共享 yml + 专绑定：`docs/04.specs/93.ai-vector-cluster-ui.md`（可并行，不挡本单元）。开通向导：`docs/04.specs/94.tenant-create-wizard.md`。
