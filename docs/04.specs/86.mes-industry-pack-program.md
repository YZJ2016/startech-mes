# Development Unit Template

Use this template before each implementation unit.

## 1. Spec

### Capability

- Name: MES 行业包落地（总控）
- Module: 外层文档 `docs/04.specs/86`–`88`；实现落 `startech-mes-basic/backend`（`ktg-system` 许可/映射/菜单种子，必要时 `ktg-mes` / `ktg-admin`）与 `startech-mes-basic/startech-mes-front`。**禁止**改 Vue2。
- Priority: P0（总控本身不改 Java）
- Related requirement/design documents:
  - 缺口：`docs/02.requirements/09.mes-industry-pack-gaps.md`
  - 设计：`docs/03.design/10.mes-industry-pack-plan.md`
  - 租户前提（只读）：`docs/04.specs/35.mes-multi-tenant-program.md`、`docs/04.specs/39.tenant-catalog-p3.md`
  - Flyway：`docs/03.design/05.mes-flyway-integration.md`
  - 本总控：`docs/04.specs/86.mes-industry-pack-program.md`
  - 下一编码单元：`docs/04.specs/87.industry-pack-shell-p0.md` → `88.industry-pack-extensions-p1.md`
  - 对照只读：`kuaigeyun-mes/`（不合并）

### Business Goal

在现行租户许可与共享菜单之上，提供「行业包」侧栏容器：平台给工厂开通行业模块后，该厂管理端出现对应菜单；关掉则消失。不引入第二套应用市场或插件扫描。本文件只锁定原则、阶段与决策表，不写 Java/SQL。

### Scope

In scope:

- 锁定本期目标与非目标（对齐设计第 1 节）
- 锁定已拍板决策表（不得在后续单元重开，**IP-G1–IP-G3 除外**）
- 锁定 86–88 文件名与序号
- 规定模块边界、菜单号段、验证与安全底线
- 规定缺口状态只回写 `docs/02.requirements/09.mes-industry-pack-gaps.md`

Out of scope:

- 本总控内改 Java、SQL、Vue
- 改 Vue2 `frontend/`
- 引入 Sa-Token、MyBatis-Plus、JPA、第二套 JWT / `SecurityFilterChain`
- 扫描插件、按租户重建 `sys_menu`、快格云套餐 / PRO Key
- 真实行业垂直（IP-G1）、单据换页 / profile 接线（IP-G3）
- `/ai/**`、首页工作台、pad `/platform/**`

### Acceptance Criteria

- `docs/04.specs/` 存在 86–88，章节与 `docs/01.temlplates/01.development_unit_template.md` 一致
- 每份单元 Related documents 链到设计、缺口清单、本总控、上一/下一单元
- 文档无真实地址、凭据
- 已拍板项不得在 87/88 重开；开放项保持 `[ ]` 直到产品回复
- 本总控第 4 节不得写成「本文件已改 Java」

### Rules and Constraints

Tenant isolation:

- 菜单/字典全局，不按租户克隆
- 登录后只信 JWT/`LoginUser`/`TenantContext`；忽略客户端租户头
- 工厂可见 = 角色菜单 ∩ 有效许可；`platform`/`job` 永不许可给工厂
- P0 无行业租户表；将来表必须 `tenant_id` + include；`DATABASE` 失败关闭、禁回落主库
- SQL 永不跳过 `tenant_id`（含现网 admin）

Security and masking:

- 许可写接口仅平台
- 不扩 `MesPermitAllProvider`
- 日志不打印 token、口令、JDBC、内部路径

Audit and lineage:

- 新 DDL 只进 Flyway `db/migration/master`（P0）
- 不改历史 `backend/sql/` 已发布文件
- 许可变更可用现有 `@Log`

AI/rule-engine rules:

- 本轨不涉及 AI

Database rules:

- 菜单号段 `9200–9299`
- 不把行业开关写入 yml

### 已锁定开发单元 TOC

序号接现有最大号 `85`，从 **86** 起连续。禁止改号、插空。

| 序号 | 文件 | 阶段 | 依赖 |
| ---- | ---- | ---- | ---- |
| 86 | `86.mes-industry-pack-program.md` | 总控 | 租户 35/39 已存在 |
| 87 | `87.industry-pack-shell-p0.md` | P0 壳 + 桩模块 | 86；**IP-G2 须拍板才能验收侧栏** |
| 88 | `88.industry-pack-extensions-p1.md` | P1 声明 + 槽位互斥 | 87 |

**门禁：**

| 门禁 | 硬条件 |
| --- | --- |
| 工厂侧栏出现行业包 | 87 完成 + 该厂有效 `ip.demo` + **IP-G2 所选授权路径已落地** |
| 两模块不得抢同一宿主 path | 88 完成；真实换页另依赖 IP-G3 |

### 已拍板决策（禁止后续单元重开）

| 编号 | 选择（摘要） |
| ---- | ------------ |
| IP-D1–IP-D19 | 见缺口 09：共享菜单壳、平台许可、`ip`/`ip.demo`、不扫描插件、P1 不换页、无套餐 |
| IP-I1–IP-I6 | JWT 上下文、两闸、不扩匿名面 |
| IP-F1–IP-F5 | 只 Vue3；`loadView`；隐藏壳码 |

开放：IP-G1、IP-G2、IP-G3。

## 2. Plan

### Implementation Approach

1. 先 87：字典、映射、master 菜单、桩页、`saveLicenses` 派生 `ip`、许可 UI 隐藏壳码。
2. 再 88：Java 登记 + 许可写入互斥。
3. 真实行业与换页不进本 TOC。

与租户并行：不改已拍板 D/F/I；只追加 `mes_module` 码与 9200 段菜单。

### Affected Modules

- 文档：缺口 09、设计 10、本总控 86–88；租户 02/35/39 仅指针
- 后端（编码单元）：`ktg-system`（必改）、可选 `ktg-mes` / `ktg-admin`
- 前端：仅 `startech-mes-front`
- 禁止：`frontend/`、`kuaigeyun-mes/` 写入、`ktg-generator`、UReport

### Public Contracts

- 保持：`GET /getRouters`、`PUT /platform/tenant/{id}/licenses` 的 URL / method / 认证 / `code/msg/data`
- 增量：许可 body 可含 `ip.demo`；服务端可插入派生 `ip`（对平台读回列表可见，对勾选 UI 隐藏）
- 不新增登录后仍生效的租户 Header

### Persistence Changes

- `sys_dict_data` 增加 `ip`、`ip.demo`（master Flyway）
- `sys_menu` 9200 段（master）
- `sys_tenant_license` 无新列
- P0 无新业务表

### Transaction Boundaries

- `saveLicenses` 保持现网：删后插同一 `masterTransactionManager` 事务；P1 互斥失败则整单回滚

### Risks

- 未拍板 IP-G2 却宣称「许可即见菜单」
- 按租户写 `sys_menu` 破坏 I8 / 39
- `mes/ip` 映射不到 `ip` 导致父目录被许可过滤丢掉
- 把 `ip` 当成 `platform` 禁止许可给工厂
- 引入插件扫描或第二套路由加载器

### Verification Strategy

- 本总控：文档链完整、无密钥
- 编码证据只写在 87/88 第 4 节

## 3. Task Breakdown

| Task | Description | Output | Verification |
| ---- | ----------- | ------ | ------------ |
| T1 | 锁定 TOC 与红线 | 本文件 | 86–88 文件名存在；无业务代码 |
| T2 | 指针不重开租户史诗 | 02/35/39 一行 | 不改 D/F/I 条文 |

## 4. Verification Record

Commands run:

```text
（本单元文档已落盘，代码未执行。实现后不得把 Java 证据写在本总控第 4 节。）
```

Result:

- 文档总控。Java / Vue / 浏览器均未在本文件编写时运行。

Known gaps:

> 状态：`[ ]` 开放 · `[x]` 已收口（收口详情见 `docs/02.requirements/09.mes-industry-pack-gaps.md`）

- `[x]` **IP-D1–IP-D19、IP-I1–IP-I6、IP-F1–IP-F5** 产品决策（缺口 09）
- `[ ]` **IP-X1 / IP-X2** 编码
- `[ ]` **IP-G1 / IP-G2 / IP-G3** 产品分叉

## 5. Final Summary

Files changed:

- `docs/04.specs/86.mes-industry-pack-program.md`（本文件，文档单元）
- 业务代码：无（本文件落盘时）

Behavior delivered:

- 锁定行业包两期：87 壳+桩，88 声明+互斥；不扫描插件、不按租户重建菜单。

Next development unit:

- `docs/04.specs/87.industry-pack-shell-p0.md`
