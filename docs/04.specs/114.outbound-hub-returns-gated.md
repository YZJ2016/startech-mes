# Development Unit Template

Use this template before each implementation unit.

## 1. Spec

### Capability

- Name: 出库 Hub — 生产退料 / 销售退货进 Hub（OH-G1，**待确认门控**）
- Module: 将来落 `startech-mes-basic/backend/ktg-mes` Hub UNION 加法 + Vue3 出库 Hub 类型筛。**禁止**改 Vue2。**未拍板禁止编码。禁止默默纳入 107 或 112。**
- Priority: 门控后续（CAT-W03 相关；**不是** P0；**默认不进 P1**）
- Related requirement/design documents:
  - 缺口：`docs/02.requirements/15.mes-outbound-hub-gaps.md`（**OH-G1**、OUT-F02 **待确认（范围）**）
  - 设计：`docs/03.design/15.mes-outbound-hub-plan.md`（§7.3）
  - 总控：`docs/04.specs/106.mes-outbound-hub-program.md`
  - P0：`docs/04.specs/107.outbound-hub-p0.md`（默认不含 `rtissue`/`rtsalse`）
  - P1：`docs/04.specs/112.outbound-hub-other-borrow-p1.md`（不得把本范围塞进 P1）
  - 现网页（已存在，只读核对）：`wm/rtissue`、`wm/rtsalse`

### Business Goal

总览 CAT-W03 列过退料/退货散页。是否进入出库 Hub **仍待用户确认**。确认「纳入」之后，Hub 一张表增加这两类（服务端合并现网 `wm_rt_issue` / `wm_rt_salse`），旧页保留。确认「不纳入」或未拍板：Hub 永不默默出现这两类。

**现在不交付该行为。待确认保持待确认。**

### Scope

In scope（**仅当用户书面确认 OUT-F02 纳入 Hub**）:

- `GET /mes/wm/outboundhub/list` 增加 `RT_ISSUE` / `RT_SALSE`（名称实现时与现网对齐）
- 抽屉走现网退料/退货 GET；执行仍走现网 `PUT /mes/wm/rtissue/{rtId}`、`PUT /mes/wm/rtsalse/{rtId}`
- UI-A：不重排 `rtissue`/`rtsalse` 布局

Out of scope:

- **未拍板即编码**；在 107/112 中 silently include
- 把退货当成入库方向改 105（对照 OUT-F02：快格云 Hub 无这两类；更接近入库方向——若拍板「不进出库 Hub」则关闭本单元为不做）
- 借料/送货/其他出库（112/113）、CAT-W01/W04–W07
- 改退料/退货过账语义

### Acceptance Criteria

- **编码门禁（当前未满足）：** 用户对 OUT-F02 书面拍板为 **纳入出库 Hub**，且开工会话点名 114。拍板为 **不纳入** 则本文件改为不做并勾 OH-G1 为关闭（不编码）。**未拍板 = 禁止编码。**
- 另须：107 已完成或用户明示可改 Hub list SQL。
- 门禁后（仅纳入分支）：Hub 可筛到本租户退料/退货；旧 rtissue/rtsalse URL 不变
- **此刻不得写绿命令**

### Rules and Constraints

Tenant isolation:

- 新 UNION 臂两侧/`tenant_id`；无上下文不得访问退料/退货表
- 只信 JWT

Security and masking:

- 仍用 Hub `mes:wm:outboundhub:list`；行上执行用现网退料/退货权限字
- 不进 `MesPermitAllProvider`

Audit and lineage:

- 本单元默认无新表（现网已有头表）
- 不改历史 `backend/sql/`

AI/rule-engine rules:

- 不涉及

Database rules:

- 不加金额列；不新建第三张混合头表

## 2. Plan

### Implementation Approach

- **现在：** 文档门控。缺口 15 的 OUT-F02 / OH-G1 保持待确认。
- **纳入拍板后：** 扩 107 的 Hub 合并 SQL + 类型筛；对照入库 Hub UNION 先例。
- **不纳入拍板后：** 不改代码；回写 OH-G1 关闭原因。

### Affected Modules

- 仅纳入分支：`ktg-mes` outboundhub Mapper；Vue3 出库 Hub 类型选项
- 不改：退料/退货旧页布局、入库 Hub

### Public Contracts

- 保持 rtissue/rtsalse CRUD URL
- Hub list 类型枚举加法（仅纳入后）

### Persistence Changes

- 无新表

### Transaction Boundaries

- list 只读；执行仍在现网 rtissue/rtsalse execute

### Risks

- 未拍板并进 P0/P1（本文件存在就是为挡住这条）
- 前端拉四源再切片

### Verification Strategy

- 未拍板：第 4 节「未跑」
- 纳入且编码后：四臂租户/分页单测；实跑后写第 4 节

## 3. Task Breakdown

| Task | Description | Output | Verification |
| ---- | ----------- | ------ | ------------ |
| T0   | OUT-F02 用户拍板 | 缺口 15 | **纳入**才继续；**不纳入**则关缺口不编码 |
| T1   | Hub 类型臂 | outboundhub | 旧 rt 页布局未改 |
| T2   | 实跑验证 | 第 4 节 | 未跑不得写绿 |

## 4. Verification Record

Commands run:

```text
（OUT-F02 待确认，未改 Java/Vue，未跑 mvn / npm）
```

Result:

- 未改业务仓；未声称测试/构建成功
- 未把 `rtissue`/`rtsalse` 写入 107/112 范围

Known gaps:

- [ ] OUT-F02 / OH-G1 仍为待确认（范围）
- [ ] 未拍板禁止编码

## 5. Final Summary

Files changed:

- `docs/04.specs/114.outbound-hub-returns-gated.md`（仅文档）

Behavior delivered:

- 无运行时行为。退料/退货进 Hub 已单列并门控；默认仍不进 P0/P1

Next development unit:

- 无预占工单/质量其它闸/设备文件
- 出库 OQC / 打印 / 金额仍为缺口 15 的 OH-G2/G4/G5，**本会话不另开 program**
- 当前编码切片仍是 105；107 仍阻塞
