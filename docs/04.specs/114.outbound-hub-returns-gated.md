# Development Unit Template

Use this template before each implementation unit.

## 1. Spec

### Capability

- Name: 出库 Hub — 生产退料 / 销售退货**不纳入**（OH-G1 已拍板走入库 IH-G10）
- Module: `startech-mes-basic/backend/ktg-mes` 负向单测锁出库 Hub UNION；**禁止**改 Vue2。**禁止**把 `RT_ISSUE` / `RT_SALSE` 加进出库 Hub。**禁止**默默纳入 107 或 112。入库余型仍只在缺口 13 **IH-G10**（本文件不描述入库 UNION 加法，故本单元不改入库 Hub）。
- Priority: 门控已关闭（**不是**出库 P3；按对照仓归 **入库 Hub 余型**；出库不纳入）
- Related requirement/design documents:
  - 缺口：`docs/02.requirements/15.mes-outbound-hub-gaps.md`（**OH-G1**、OUT-F02 **本刀不做 / 已拍板不纳入出库**）
  - 入库余型指针：`docs/02.requirements/13.mes-inbound-hub-iqc-gaps.md` **IH-G10**（归宿；仍故意后开、无独立 spec）
  - 设计：`docs/03.design/15.mes-outbound-hub-plan.md`（§7.3）
  - 总控：`docs/04.specs/106.mes-outbound-hub-program.md`
  - P0：`docs/04.specs/107.outbound-hub-p0.md`（不含 `rtissue`/`rtsalse`）
  - P1：`docs/04.specs/112.outbound-hub-other-borrow-p1.md`（不得把本范围塞进 P1）
  - 现网页（已存在，只读核对）：`wm/rtissue`、`wm/rtsalse`

### Business Goal

总览 CAT-W03 **不要**把退料/退货当出库组成员。对照仓这两类是 **入库** Hub 类型（`production_return` / `sales_return`）。开工会话点名 114，且锁定产品倾向：生产退料 / 销售退货属 **入库 Hub 余型（IH-G10）**，出库默认不纳入。走 **不纳入** 分支：出库 Hub UNION 永不出现这两类；本文件不描述入库 UNION 加法，因此 **不在本单元实现入库余型**。旧 `rtissue` / `rtsalse` 散页与 URL 保留。

### Scope

In scope:

- 拍板 OUT-F02 / OH-G1 为 **不纳入出库 Hub**（归宿 IH-G10）
- 负向单测：出库 Hub SQL / Vue 类型筛不含 `RT_ISSUE` / `RT_SALSE` / `wm_rt_issue` / `wm_rt_salse`
- 负向单测：本单元不把退料/退货臂加进入库 Hub
- 现网 `GET/PUT /mes/wm/rtissue/**`、`/mes/wm/rtsalse/**` 契约保持
- 不进 `MesPermitAllProvider`

Out of scope:

- 把 `RT_ISSUE` / `RT_SALSE` 加进出库 Hub UNION 或类型筛
- 实现 IH-G10 入库 Hub 余型（半成品 / 生产退料 / 销售退货进入库 Hub）
- 改 107 出库 P0 行为、改 112/113 文件
- 重排 `rtissue` / `rtsalse` 旧页布局
- 借料/送货/其他出库（112/113）、CAT-W01/W04–W07
- 改退料/退货过账语义

### Acceptance Criteria

- **编码门禁（已满足不纳入分支）：** 用户点名 114，且产品倾向锁定为走入库 IH-G10、出库不纳入。
- 出库 Hub 列表 SQL **排除** `RT_ISSUE` / `RT_SALSE`（不把这两类加进 UNION 臂）；筛不到本租户退料/退货。运行时四臂为 ISSUE / PRODUCT_SALSE / OTHER_ISSUE / MATERIAL_BORROW（112）；本单元不得把 P0 两类型重新冻结为 Hub 全集
- Vue3 出库 Hub 类型选项无退料/退货
- 旧 rtissue/rtsalse URL / method 不变
- 入库 Hub 本单元不新增退料/退货臂（IH-G10 仍开放）
- `/mes/wm/outboundhub/**`、`rtissue`、`rtsalse` 不进 `MesPermitAllProvider`

### Rules and Constraints

Tenant isolation:

- 不新增退料/退货 UNION 臂；现网退料/退货表仍须 `tenant_id`（既有拦截器 include）
- 只信 JWT

Security and masking:

- Hub 仍用 `mes:wm:outboundhub:list`；退料/退货执行仍走旧页权限字
- 不进 `MesPermitAllProvider`

Audit and lineage:

- 本单元无新表
- 不改历史 `backend/sql/`

AI/rule-engine rules:

- 不涉及

Database rules:

- 不加金额列；不新建第三张混合头表；不改出库 Hub UNION 表集

## 2. Plan

### Implementation Approach

- **已拍板不纳入（含确认走入库 IH-G10）：** 不改出库 Hub UNION / 类型筛；不加入库余型臂（114 未描述该加法）；负向单测锁排除；回写 OH-G1 关闭原因。
- 不走「纳入出库」分支。

### Affected Modules

- `ktg-mes`：新增 `WmOutboundHubReturnsGatedTest`（只读核对 XML / Vue / 旧 Controller）
- 不改：出库 Hub Mapper、入库 Hub、退料/退货旧页布局、112/113

### Public Contracts

- 保持 rtissue/rtsalse CRUD URL
- 出库 Hub list 类型枚举不加退料/退货

### Persistence Changes

- 无新表

### Transaction Boundaries

- 本单元无写路径；执行仍在现网 rtissue/rtsalse execute

### Risks

- 把退料/退货默默并进 107/112（负向单测挡住）
- 误把 IH-G10 当本单元实现（114 未描述入库 UNION）

### Verification Strategy

- 负向单测：出库 / 入库 Mapper XML、Vue 类型筛、旧 URL、permitAll
- 实跑后写第 4 节

## 3. Task Breakdown

| Task | Description | Output | Verification |
| ---- | ----------- | ------ | ------------ |
| T0   | OUT-F02 拍板为不纳入出库（走 IH-G10） | 缺口 15 OH-G1 `[x]` | 对照清单不再待确认（范围） |
| T1   | 负向锁：出库 UNION 不含 RT 臂 | `WmOutboundHubReturnsGatedTest` | 旧 rt 页布局未改 |
| T2   | 实跑验证 | 第 4 节 | 未跑不得写绿 |

## 4. Verification Record

Commands run:

```text
cd startech-mes-basic/backend
java -version
# OpenJDK 21.0.9 Corretto-21.0.9.10.1（javac target 仍为 17）

mvn -pl ktg-mes -am "-Dtest=WmOutboundHubReturnsGatedTest" "-Dsurefire.failIfNoSpecifiedTests=false" test
# BUILD SUCCESS 2026-09-16T12:46:11+08:00 与 12:50:32+08:00
# ktg-mes Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
# com.ktg.mes.wm.WmOutboundHubReturnsGatedTest

mvn -pl ktg-mes surefire:test "-Dtest=WmOutboundHub*Test"
# WmOutboundHubReturnsGatedTest 5/5
# WmOutboundHubTenantRewriteTest 3/3
# WmOutboundHubMapperXmlTest 通过，tenant_id 计数 4（四臂 UNION；不再是 2 vs 4）

# leftover review 2026-09-16T13:33:19+08:00（四臂 rewrite fixture + AC 对齐 OH-G1）
# OpenJDK 21.0.9 Corretto-21.0.9.10.1（javac target 仍为 17）
mvn -pl ktg-mes -am "-Dtest=WmOutboundHubTenantRewriteTest,WmOutboundHubMapperXmlTest,WmOutboundHubReturnsGatedTest,WmOtherIssueMaterialBorrowP1Test" "-Dsurefire.failIfNoSpecifiedTests=false" test
# BUILD SUCCESS；ktg-mes Tests run: 20, Failures: 0, Errors: 0, Skipped: 0
# WmOtherIssueMaterialBorrowP1Test 8/8
# WmOutboundHubMapperXmlTest 4/4
# WmOutboundHubReturnsGatedTest 5/5
# WmOutboundHubTenantRewriteTest 3/3（COUNT 包四臂）
```

Result:

- 业务仓仅新增负向单测；未改出库 Hub UNION、未改入库 Hub、未改 `rtissue`/`rtsalse` 布局
- 未把 `RT_ISSUE` / `RT_SALSE` 写入出库 / 入库 Hub
- 未起应用、未登录、未对真实库跑 Flyway；未跑 npm（未改 Vue）

Known gaps:

> 状态：`[ ]` 开放 · `[x]` 已收口（收口详情见缺口 15）

- [x] OUT-F02 / OH-G1 已拍板 **不纳入出库 Hub**（归宿 IH-G10）
- [x] 负向单测锁出库 UNION 排除退料/退货
- [ ] IH-G10 入库余型（半成品 / 生产退料 / 销售退货进 **入库** Hub）仍故意后开、无独立 spec
- [ ] OH-G2 / G4 / G5 仍开放（本单元不做）

## 5. Final Summary

Files changed:

业务仓 `startech-mes-basic`（`feature/mes-kuaige-capability`，未提交）:

- 单测：`WmOutboundHubReturnsGatedTest`

外层文档：`docs/04.specs/114.outbound-hub-returns-gated.md`；缺口 15 / 13 IH-G10 / 总览 14 CAT-W03 / 设计 15 / 106 / 103

Behavior delivered:

- 出库 Hub **不出现** 生产退料 / 销售退货；旧散页 URL 不变
- 归宿记录在入库 **IH-G10**；本单元不实现入库余型臂

Next development unit:

- 无预占工单/质量其它闸/设备文件
- 出库 OQC / 打印 / 金额 → 115 / 116 / 117（编码未开）
- 入库余型（含退料/退货进入库 Hub）仍走 IH-G10，无新 spec 号
