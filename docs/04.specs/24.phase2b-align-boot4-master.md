# Development Unit Template

Use this template before each implementation unit.

## 1. Spec

### Capability

- Name: 阶段 2b 对齐 `origin/master`（Spring Boot 4.1.0）
- Module: 根 POM + 六个平台 `ktg-*`（一次一块）；`ktg-mes` 仅编译闭合
- Priority: P0
- Related requirement/design documents:
  - `docs/04.specs/19.backend-shell-swap-phase2-program.md`
  - 2a 必须先完成：`20`–`23`（JDK 17 / Boot **3.5.16** / jakarta / 五项已挂）
  - 对照：`RuoYi-Vue` `origin/master` @ `a6ea55ecf74ae6cdb063f2b413d66a33f29bf828`

### Business Goal

在 2a 已是 Java 17 + jakarta + Boot 3.5.16 之后，再把平台内容对齐若依 **当前 master（Boot 4.1.0）**。禁止从 Boot 2.5 或未做完的 2a 直接跳到本单元。模块名/包名仍 `ktg` / `com.ktg`。

### Scope

In scope:

- 对照 HEAD 必须是 `a6ea55ecf74ae6cdb063f2b413d66a33f29bf828`（实测与本地 `master` 相同）
- 根 POM 属性按对照对齐（已实测）：
  - `java.version` 仍 `17`
  - `spring-boot.version` = **`4.1.0`**
  - `mybatis-spring-boot.version` = `4.1.0`
  - `pagehelper.boot.version` = `4.1.1`
  - `springdoc.version` = `3.1.0`
  - `druid-spring-boot-4-starter`（替换 2a 的 `druid-spring-boot-3-starter`）
- 一次一个模块覆盖/合并 **2a 树与 master 的实际差集**（不要整仓无 diff 再拷一遍）。实测 `origin/springboot3...origin/master` 触及：
  - `pom.xml`（根）
  - `ruoyi-admin`：`RuoYiApplication.java`（**仅** `DataSourceAutoConfiguration` import）
  - `ruoyi-common`：`pom.xml`、`Sensitive.java`、`SensitiveJsonSerializer.java`、`LoginUser.java`、`RedisCache.java`、`HttpMethod.java`、`StringUtils.java`、`ReflectUtils.java`
  - `ruoyi-framework`：`pom.xml`、`ApplicationConfig.java`、`DruidConfig.java`、`ThreadPoolConfig.java`、`PermitAllUrlProperties.java`
  - `ruoyi-generator/pom.xml`、`ruoyi-quartz/pom.xml`
- 机械替换 `com.ruoyi` → `com.ktg`；GAV 留 ktg
- **启动类 exclude 包路径（2b 必改）**：
  - 2a / springboot3：`org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration`
  - master / Boot 4.1：`org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration`
- 对照 framework pom 关键更名（必须跟 master，不要留 Boot 3 starter 名）：
  - `spring-boot-starter-web` → `spring-boot-starter-webmvc`
  - `spring-boot-starter-aop` → `spring-boot-starter-aspectj`
- 对照 common pom：`com.fasterxml.jackson.core:jackson-databind` → `tools.jackson.core:jackson-databind`；并增加 `spring-boot-starter-cache`
- `ApplicationConfig`：master 删除了 `Jackson2ObjectMapperBuilderCustomizer` 时区 Bean——**跟对照删**，不要在 Boot 4 强留 Jackson2 API
- 覆盖/合并后立刻确认 `21`/`23` 接线仍在：Provider 注入、手机号、notice `/list` 已删、uploadMinio 仅 mes、yml 根键 `ruoyi`、`@EnableAsync`/`@EnableScheduling`、`ktg-mes` 依赖、antrun
- `ktg-mes`：仅当 Boot 4 编译失败时做最小修复（import/GAV），并写入第 4 节；不借机改业务

Out of scope:

- 跳过 2a
- 把 2a 未完成的 jakarta/springfox 放到本单元「一起做」
- `25` 改名；前端 Vue3
- 用 springboot2 树当对照
- 整文件覆盖 yml 密钥
- 无输出宣称 Boot 4 已通过

### Acceptance Criteria

- 根 POM `spring-boot.version=4.1.0`，**没有**残留 3.5.16 作为正在使用的 BOM（注释历史除外）
- `RuoYiApplication` import 为 `org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration`
- framework 使用 `spring-boot-starter-webmvc` 与 `druid-spring-boot-4-starter`
- 模块名仍 `ktg-*`；`rg "com\\.ruoyi" backend/ktg-*` 平台模块为 0
- 每改一块 JDK 17 编译该块 `-am` 通过后再下一块
- 五项在 Boot 4 树上再勾一次（表同 `23`，结果写入本文件第 4 节或 `26`）
- mes 若有修，仅编译闭合

### Rules and Constraints

Tenant isolation:

- 不涉及

Security and masking:

- 五项契约不变；不扩大匿名面
- 不粘贴密钥

Audit and lineage:

- 对照 hash 漂移则先改 `19` 再开工

AI/rule-engine rules:

- 不涉及

Database rules:

- 不执行 master 的 `sql/` 整包

## 2. Plan

### Implementation Approach

1. 确认 2a：`java.version=17`、`spring-boot.version=3.5.16`、`22` 聚合编过、`23` 五项静态已勾
2. 确认对照 `git rev-parse origin/master` = `a6ea55ecf74ae6cdb063f2b413d66a33f29bf828`
3. **一次一块**，建议顺序与差集一致：根 POM → `ktg-common` → `ktg-framework` → `ktg-admin`（启动类）→ `ktg-quartz` pom → `ktg-generator` pom。`ktg-system` 若 name-only diff 为空则 **不要**为「仪式感」整模块覆盖（避免再次冲掉 `selectUserByMobile`）
4. 每块：`git diff origin/springboot3 origin/master -- ruoyi-<mod>` 为清单，只合并这些文件，再机械替换包名
5. framework 覆盖 `SecurityConfig` 时若文件无 diff（实测 SecurityConfig **不在** springboot3…master 文件列表），**不要重拷**，以免误删 Provider
6. admin 只改 `RuoYiApplication` 的 DataSource import 时，核对启动注解与 exclude 数组仍在
7. mes 编译失败：记录 GAV/API，最小改；未知则先编再记

Boot 4 已知 API 差（来自实测 diff，实施时仍以对照文件为准）：

| 点 | 2a (springboot3) | 2b (master) |
| -- | ---------------- | ----------- |
| Boot | 3.5.16 | 4.1.0 |
| DataSourceAutoConfiguration | `org.springframework.boot.autoconfigure.jdbc` | `org.springframework.boot.jdbc.autoconfigure` |
| Web starter | `spring-boot-starter-web` | `spring-boot-starter-webmvc` |
| AOP starter | `spring-boot-starter-aop` | `spring-boot-starter-aspectj` |
| Druid | `druid-spring-boot-3-starter` | `druid-spring-boot-4-starter` |
| Jackson | `com.fasterxml.jackson.core` | `tools.jackson.core`（`SensitiveJsonSerializer` 等） |
| SpringDoc | 2.9.0 | 3.1.0 |
| MyBatis starter | 3.0.5 | 4.1.0 |

`StringUtils` 在 master 有大段增量：以对照覆盖 common 后跑编译，不要手工挑着抄。

### Affected Modules

- 改：根 POM、common、framework、admin 启动类、quartz/generator pom（以 diff 为准）
- 可能改：`ktg-mes` 最小编译修复
- 不改：`frontend/`、`RuoYi-Vue3-front`

### Public Contracts

- HTTP 契约与五项不变

### Persistence Changes

- 无

### Transaction Boundaries

- 无新边界

### Risks

- 未做 2a 就拷 master，mes 与平台同时炸
- 整模块覆盖 system/framework 冲掉手机号与 Provider（而这两文件在 Boot3→4 **可能无 diff**）
- 启动类仍 exclude 旧包名，Boot 4 编译或启动失败
- Jackson2 API 残留与 `tools.jackson` 混用
- 把 Vue3 或改名塞进来

### Verification Strategy

每块 JDK 17：

```text
mvn -pl ktg-<mod> -am compiler:compile "-DskipTests" "-Dmaven.antrun.skip=true"
```

全量：

```text
mvn -pl ktg-admin -am compiler:compile "-DskipTests" "-Dmaven.antrun.skip=true"
```

五项 `rg` 同 `23`。

## 3. Task Breakdown

| Task | Description | Output | Verification |
| ---- | ----------- | ------ | ------------ |
| T0 | 确认 2a 收口与 master hash | 第 4 节 | hash=`a6ea55ec…`；未跳 2a |
| T1 | 根 POM → Boot 4.1.0 BOM | `backend/pom.xml` | 无 3.5.16 BOM |
| T2 | common 差集（Jackson/Redis/StringUtils 等） | `ktg-common` | 本模块编译过 |
| T3 | framework 差集（starter 更名、ApplicationConfig） | `ktg-framework` | webmvc/aspectj/druid4；Provider 仍在 |
| T4 | admin 启动类 DataSource 包 | `RuoYiApplication` | 新包名；Async/Scheduling 仍在 |
| T5 | quartz/generator pom | 模块 pom | 编译过 |
| T6 | mes 若失败则最小修 | 第 4 节 | 不猜大升级 |
| T7 | 五项再勾 | 表 | 全勾或书面豁免 |

## 4. Verification Record

Commands run:

```text
尚未实施。2b 开工时记录：

git -C <RuoYi-Vue> rev-parse origin/master
# a6ea55ecf74ae6cdb063f2b413d66a33f29bf828
java -version   # 17
# 以及每模块 mvn 摘要
```

Result:

- 未实施；**不得**声称已对齐 Boot 4.1 或测试通过

Known gaps:

> 状态：`[ ]` 开放 · `[x]` 已收口（收口详情见 `docs/02.requirements/99.升级交付剩余缺口与收口清单.md`）

- `[ ]` 2b 代码未做
- `[ ]` mes × mybatis-spring-boot 4.1.0 / FieldMeta：**先编再记**
- `[ ]` 运行时见 `26`

## 5. Final Summary

Files changed:

- 实施后为 POM + 上表差集文件；本文件为 2b 契约

Behavior delivered:

- （实施后）平台 BOM 与源码对齐 master Boot 4.1.0，五项仍在，ktg 坐标系不变

Next development unit:

- `docs/04.specs/26.phase2-verify.md`。若产品明确要改名，先 `25`，默认跳过
