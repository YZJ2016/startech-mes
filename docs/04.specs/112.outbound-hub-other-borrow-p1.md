# Development Unit Template

Use this template before each implementation unit.

## 1. Spec

### Capability

- Name: 出库 Hub P1 — 其他出库 + 借料（CAT-W03 余型）
- Module: 将来落 `startech-mes-basic/backend/ktg-mes`（wm）与 `startech-mes-front`（出库 Hub 类型加法，及若需新 CRUD 页）。**禁止**改 Vue2。**本单元编码未开。禁止本会话改 Java/Vue。禁止打进 107。**
- Priority: P1（出库切片后续；同一域 CAT-W03）
- Related requirement/design documents:
  - 缺口：`docs/02.requirements/15.mes-outbound-hub-gaps.md`（**OH-P1**、OH-G3 余型、OUT-F01）
  - 设计：`docs/03.design/15.mes-outbound-hub-plan.md`（§7）
  - 切片总控：`docs/04.specs/106.mes-outbound-hub-program.md`
  - P0（范围不变）：`docs/04.specs/107.outbound-hub-p0.md`（仅 `ISSUE` + `PRODUCT_SALSE`）
  - 总览：`docs/02.requirements/14.mes-kuaige-capability-overview.md` CAT-W03；**CAT-N2** 禁止打包 W01/W04–W07
  - 退料/退货不是本单元：`114.outbound-hub-returns-gated.md`（待确认）
  - 送货单不是本单元：`113.outbound-hub-delivery-p2.md`

### Business Goal

P0 只聚合现网领料 + 销售出库。本单元把 CAT-W03 剩余的**其他出库、借料**接到同一出库 Hub（类型筛可切）。现网 **无** 对等头表/页：须先拍板表与 API，再编码。旧领料/销售出库契约不变。

**现在不交付该行为。**

### Scope

In scope（**仅当编码门禁满足**）:

- 产品拍板后的其他出库、借料单据（现网模型，不拷快格云 6 种枚举名当表名）
- `GET /mes/wm/outboundhub/list` **加法**类型臂（仍服务端合并 + 每侧 `tenant_id`）；不替换旧 issue/productsalse list
- 旧菜单保留；新类型若有独立 CRUD 则为加法页（UI-A：不重排 107 将改的 Hub 以外的旧领料/销售出库布局）

Out of scope:

- **现在改 Java/Vue**；把其他出库/借料塞进 107
- 送货单（113）、退料/退货（114，待确认）、委外发料、采购退货（OH-G3 残留，默认后置/另开）
- CAT-W01 看板、CAT-W04 功能业务、CAT-W05 库存管理、CAT-W06 报表、CAT-W07 配料
- OQC（OH-G2）、出库金额（OH-G5）、Hub 打印（OH-G4）
- 工单/质量其它闸/设备 program（CAT-N1：出库之后才开，**不预占**下一文件）
- 改 Vue2、改入库 105、扩 permitAll、前端假分页

### Acceptance Criteria

- **编码门禁（当前未满足）：**（1）点名 112；（2）107 编码完成 **或** 用户明示开工本单元；（3）**其他出库/借料的头表与 HTTP 已书面拍板**（现网无表，禁止未拍板建表）；（4）缺口 15 无未处理待确认（冲突）。OUT-F02 未拍板则本单元仍不得纳入 `rtissue`/`rtsalse`。
- 门禁后：Hub 可按新类型筛选；UNION/合并每侧租户条件；107 的 ISSUE/PRODUCT_SALSE 行为不回退
- **此刻第 4 节不得写绿；不得声称已改 Java**

### Rules and Constraints

Tenant isolation:

- 无 `TenantContext` 不得访问新 wm 表；Hub 每臂 `tenant_id`
- 只信 JWT；无客户端租户头

Security and masking:

- 新路径 `@PreAuthorize`；不进 `MesPermitAllProvider`

Audit and lineage:

- 新 DDL 只进 Flyway；不改历史 `backend/sql/`
- 菜单号禁止占用 9300–9319（入库 Hub / 出库 P0）；新号实现时按当时 `sys_menu` 顺延

AI/rule-engine rules:

- 不涉及

Database rules:

- 未拍板前 **零新头表**；拍板后不加金额列（OH-G5）
- 不写 yml

## 2. Plan

### Implementation Approach

- **现在：** 只维持本 spec。
- **门禁后：** 先落拍板后的单据 CRUD（若需要），再扩 Hub UNION。对照 107 服务端合并，禁止拷 `outboundListAggregate.ts`。

### Affected Modules

- 门禁后：`ktg-mes` wm；Flyway；Vue3 出库 Hub 类型筛
- 禁止：`frontend/`、`kuaigeyun-mes/`、107 范围外的入库代码

### Public Contracts

- 保持：`/mes/wm/issueheader/**`、`/mes/wm/productsalse/**`
- 增量：Hub list 类型枚举加法；新单据 REST 仅在拍板后写入设计 15 §7 再实现（本文件不锁未拍板路径）

### Persistence Changes

- 未拍板：无
- 拍板后：新头/行表走 tenant Flyway；Hub 不物化第三张「混合出库」表

### Transaction Boundaries

- Hub list 只读；过账走各类型自己的 execute

### Risks

- 未拍板建表或照搬快格云类型码
- 把退料/送货/委外/盘点默默并入
- 前端多源假分页

### Verification Strategy

- 门禁未开：第 4 节「未跑」
- 门禁后：租户/403/分页单测；实跑命令后写第 4 节

## 3. Task Breakdown

| Task | Description | Output | Verification |
| ---- | ----------- | ------ | ------------ |
| T0   | 编码门禁 + 表结构拍板 | 缺口 15 OH-P1 | 未满足则 **停止** |
| T1   | 单据 + Hub 类型臂 | wm + outboundhub | 未纳入 rtissue；无 W01/W04–W07 |
| T2   | 实跑验证 | 第 4 节 | 未跑不得写绿 |

## 4. Verification Record

Commands run:

```text
（编码门禁未开，未改 Java/Vue，未跑 mvn / npm）
```

Result:

- 未改 `startech-mes-basic` 与 `kuaigeyun-mes`
- 未声称测试/构建成功
- 107 仍未授权开工；本单元更未授权

Known gaps:

- [ ] 编码门禁未开
- [ ] 其他出库/借料头表未拍板（现网无对等页）
- [ ] OH-G3 残留：委外发料、采购退货仍不进本单元

## 5. Final Summary

Files changed:

- `docs/04.specs/112.outbound-hub-other-borrow-p1.md`（仅文档）

Behavior delivered:

- 无运行时行为。出库 P1（其他出库+借料）已落盘并显式阻塞

Next development unit:

- `docs/04.specs/113.outbound-hub-delivery-p2.md`（编码未开）
- 107 P0 仍阻塞；当前编码切片仍是 105
