# Development Unit Template

Use this template before each implementation unit.

## 1. Spec

### Capability

- Name: Weaviate 原生多租户手册
- Module: 外层 `docs/05.manuals/02.ai-assistant-user-manual.md`（及缺口 12 的 WM-X3 回写）。**禁止**改 Vue2、禁止改 Java（编码在 100/101）。
- Priority: P1（可与 101 并行；依赖 99 已拍板文案）
- Related requirement/design documents:
  - 缺口：`docs/02.requirements/12.mes-ai-weaviate-mt-gaps.md`（**WM-I3、WM-F1、WM-D1**）
  - 设计：`docs/03.design/13.mes-ai-weaviate-mt-plan.md` §2
  - 总控：`docs/04.specs/99.weaviate-native-mt-program.md`
  - 现网手册：`docs/05.manuals/02.ai-assistant-user-manual.md` §3.4 / §4.4
  - 上一单元：`docs/04.specs/99.weaviate-native-mt-program.md`（并行于 101）
  - 下一单元：无（本轨收尾）

### Business Goal

运维与平台运营能从手册区分：**集群两档（A14）** 与 **Weaviate 库内 tenant shard**；明白 SHARED Key 仍是集群级，不能出厂仍绑 DEDICATED。

### Scope

In scope:

- §3.4 / §4.4：写明 `type=weaviate` 时库内是 `MesAiD{dimension}` + tenant `t{tenantId}`，**不再**「一知识库一个 class」
- 写明 I-A3 属性过滤仍在；SHARED API Key 不能当成专库
- 写明旧 `MesAiK*` 不自动迁移，若曾写入须重解析（WM-G2）
- 缺口 12 WM-X3 在第 4 节有手册 diff 证据后回写
- 指针：54「未连 live」仍开放；不把本手册写成 live 已验

Out of scope:

- Java / Vue / yml 真值
- 新造 `docs/05.manuals/` 序号文件（改现有 `02`）
- DashVector 操作步骤重写（可一句「DashVector 仍 collection+partition，本轨未改」）
- 真实 host / Key

Non-goals:

- 不把原生 MT 写成可替代 DEDICATED
- 不纠正 `01.temlplates` 拼写、不新建顶层目录

### Acceptance Criteria

- 手册 §3.4 能独立读懂三层：yml/专绑定 → Weaviate tenant → 属性 `tenant_id`/`kid`
- 无真实地址、凭据
- 未连 live 的句子保留
- 第 4 节未改手册前不得写「手册已对齐」

### Rules and Constraints

Tenant isolation:

- 文案不得教工厂在知识库页填集群 / tenant 名

Security and masking:

- 不出现真实 Weaviate URL、API Key、内网主机

Audit and lineage:

- 只改手册与缺口回写，不改 AGENTS.md 红线

AI/rule-engine rules:

- 不把 embedding 说成 Weaviate vectorizer

Database rules:

- 不把向量 blob 说成进 MySQL

## 2. Plan

### Implementation Approach

在现有 §3.4「两档」之后补一小节「Weaviate 库内切分」，不拆掉 SHARED/DEDICATED 表。§4.4 环境变量表不变（仍 `WEAVIATE_HOST` / `WEAVIATE_API_KEY`），加一句 class/tenant 由程序计算。

### Affected Modules

- `docs/05.manuals/02.ai-assistant-user-manual.md`
- 回写 `docs/02.requirements/12.mes-ai-weaviate-mt-gaps.md` WM-X3（第 4 节通过后）

### Public Contracts

- 无 HTTP 变更

### Persistence Changes

- 无

### Transaction Boundaries

- 无

### Risks

- 手册写成「开了 MT 就等于专库」误导出厂厂关掉 DEDICATED
- 删掉「live 未做」造成已上线假象

### Verification Strategy

- 人工读 §3.4 / §4.4：无凭据、三层隔离清楚、live 未验仍在
- 本单元不跑 Maven / npm

## 3. Task Breakdown

| Task | Description | Output | Verification |
| ---- | ----------- | ------ | ------------ |
| T1 | §3.4 补 Weaviate 库内 MT / 维度 class | 手册 | 无 MesAiK 作为现行切法（可写「历史/已废弃」） |
| T2 | §4.4 标明程序计算 tenant/class；Key 仍集群级 | 手册 | SHARED Key ≠ 专库 |
| T3 | WM-G2 重解析一句 | 手册 | 不写自动迁库 |
| T4 | 回填第 4 节与缺口 WM-X3 | 本文 + 缺口 12 | 未改手册不得勾 X3 |

## 4. Verification Record

Commands run:

```text
（待本单元改手册后填写；无 Maven）
```

Result:

- 未执行。

Known gaps:

> 状态：`[ ]` 开放 · `[x]` 已收口（收口详情见 `docs/02.requirements/12.mes-ai-weaviate-mt-gaps.md`）

- `[ ]` WM-X3 手册
- `[ ]` WM-G1 live（手册必须保留未验）

## 5. Final Summary

Files changed:

- （改手册后回写）

Behavior delivered:

- （改手册后回写）

Next development unit:

- 无。编码在 100/101；live 为 WM-G1，另开。
