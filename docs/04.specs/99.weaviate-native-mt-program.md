# Development Unit Template

Use this template before each implementation unit.

## 1. Spec

### Capability

- Name: Weaviate 原生多租户（总控）
- Module: 外层文档 `docs/04.specs/99`–`102`；实现落 `startech-mes-basic/backend/ktg-ai`。**禁止**改 Vue2。手册见 **102**。
- Priority: P0（总控本身不改 Java）
- Related requirement/design documents:
  - 缺口：`docs/02.requirements/12.mes-ai-weaviate-mt-gaps.md`
  - 设计：`docs/03.design/13.mes-ai-weaviate-mt-plan.md`
  - 只读基线：`docs/04.specs/54.ai-vector-oss-p5.md`、`56.ai-dashvector-p7.md`、`93.ai-vector-cluster-ui.md`、I-A3 / A14（`docs/02.requirements/03.mes-ai-platform-gaps.md`）
  - 平台总控索引：`docs/04.specs/48.mes-ai-platform-program.md`
  - 下一编码单元：`docs/04.specs/100.weaviate-mt-adapter.md` → `101.weaviate-mt-isolation.md`；手册 `102.weaviate-mt-manual.md`（可与 101 并行）

### Business Goal

把 Weaviate 库内切分换成官方 multi-tenancy（`MesAiD{dimension}` + tenant `t{tenantId}`），同时保留 A14 两档与 I-A3 属性过滤。本文件只锁定原则、阶段与决策表，不写 Java/SQL。

### Scope

In scope:

- 锁定本期目标与非目标（对齐设计第 1 节）
- 锁定已拍板决策表（不得在后续单元重开，**WM-G1、WM-G2、WM-G3 除外**）
- 锁定 99–102 文件名与序号（99 之后目录以三位数续号，避免与 99 撞号）
- 规定模块边界、契约冻结、验证与安全底线
- 规定缺口状态只回写 `docs/02.requirements/12.mes-ai-weaviate-mt-gaps.md`

Out of scope:

- 本总控内改 Java、SQL、Vue
- 旧 `MesAiK*` 自动迁移、按 tenant 的 Weaviate API Key、hybrid BM25
- 改 parse / chat/send / 93 绑定 HTTP
- 改 Vue2、`frontend/`、pad `/platform/**`
- Pinecone / Milvus / Qdrant

### Acceptance Criteria

- `docs/04.specs/` 存在 99–102，章节与 `docs/01.temlplates/01.development_unit_template.md` 一致
- 每份单元 Related documents 链到设计、缺口清单、本总控、上一/下一单元
- 文档无真实地址、凭据
- 已拍板项不得在 100/101/102 重开；开放项保持 `[ ]` 直到产品另开轨
- 本总控第 4 节不得写成「本文件已改 Java」

### Rules and Constraints

Tenant isolation:

- Weaviate tenant 与 `search` 的 `tenantId` 只信 JWT / `TenantContext`
- 属性 `tenant_id` + `kid` 过滤保留（I-A3）
- A14 专行仍 ignore 在 master，本轨不改绑定表

Security and masking:

- 不扩 `MesPermitAllProvider`
- 日志不打印 Weaviate Key / host 口令
- SHARED Key 仍是集群级（WM-I3），不得写成「原生 MT = 密钥隔离」

Audit and lineage:

- 本轨无新业务表；不改 `ai_platform_vector_store` 形态
- 不改历史 `backend/sql/`

AI/rule-engine rules:

- embedding 仍 A24；向量仍现网 `VectorStoreService`，不换官方 EmbeddingStore
- 禁止 SQL MES 业务库

Database rules:

- 根键仍 `ruoyi:`
- SHARED 向量 yml 占位不变；DEDICATED 仍 93

## 2. Plan

### Implementation Approach

先 100 改 `WeaviateHttpVectorStore` 与 Weaviate 专用命名函数（DashVector 的 `MesAiK*` 不得被误改）；再 101 把 ingest / 删除 / 检索接到维度 class，并回归 Factory 两档与 DashVector；102 改手册，可与 101 并行。

### Affected Modules

- 文档：本文件与 100/101/102；缺口 12；设计 13；总控 48；平台缺口 03 索引；设计 06 §6/§10 指针
- 编码：仅 `ktg-ai` 向量适配与调用方 dimension（100/101）；手册 05（102）

### Public Contracts

- **冻结：** 对外 HTTP（parse / chat/send / agent/send / 93 vector-store）
- **内部增量：** `removeByDoc` 增加 `dimension`；Weaviate class / tenant 命名见 WM-D2/D3

### Persistence Changes

- 无新表。向量数据形态在 Weaviate 内变化；MySQL fragment 不变

### Transaction Boundaries

- 与现网一致：fragment 短事务；向量失败标附件失败可重试

### Risks

- 误改 `VectorStoreSupport.collectionName` 导致 DashVector 串名
- `removeByDoc` 漏传 dimension → Weaviate 删错 class 或失败关闭被当成回归
- 把原生 MT 宣传成可替代 DEDICATED

### Verification Strategy

- 以 100/101/102 第 4 节为准；本总控不跑 Maven

## 3. Task Breakdown

| Task | Description | Output | Verification |
| ---- | ----------- | ------ | ------------ |
| T1 | 拍板缺口 12 / 设计 13 / TOC 99–102 | 四层文档 | 编号连续；100 适配器、101 接线、102 手册 |
| T2 | 索引 48 / 03 / 06 / 54 | 指针 | I-A3/A14 标明库内切分由本轨覆盖，不重开字段 |
| T3 | 回填第 4 节 | 本文 | 不写 Java 已改 |

## 4. Verification Record

Commands run:

```text
（总控不改代码；无 Maven。实现验证写在 100–102 第 4 节）
```

Result:

- 缺口 `12`、设计 `13`、单元 `99`–`102` 已落文档。本文件未改 Java/SQL/Vue。

Known gaps:

> 状态：`[ ]` 开放 · `[x]` 已收口（收口详情见 `docs/02.requirements/12.mes-ai-weaviate-mt-gaps.md`）

- `[x]` 产品决策 WM-D1–WM-D10、WM-I1–WM-I4、WM-F1（本轨文档）
- `[ ]` WM-X1 / WM-X2 / WM-X3 编码（100 / 101 / 102）
- `[ ]` WM-G1 live、WM-G2 旧 class 迁移、WM-G3 按 tenant Key

## 5. Final Summary

Files changed:

- `docs/02.requirements/12.mes-ai-weaviate-mt-gaps.md`
- `docs/03.design/13.mes-ai-weaviate-mt-plan.md`
- `docs/04.specs/99.weaviate-native-mt-program.md`（本文件）
- 编码/手册单元：`100`、`101`、`102`

Behavior delivered:

- 锁定 Weaviate 原生多租户分期与红线。本总控未改运行时。

Next development unit:

- `docs/04.specs/100.weaviate-mt-adapter.md`
