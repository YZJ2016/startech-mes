# Development Unit Template

Use this template before each implementation unit.

## 1. Spec

### Capability

- Name: 换壳阶段 1 全量门禁（编译、纯度回归、五项、未验证运行时）
- Module: `startech-mes-basic/backend` 聚合
- Priority: P0
- Related requirement/design documents:
  - `docs/04.specs/11.backend-shell-swap-program.md`
  - `docs/04.specs/09.platform-purity-gate.md`
  - `docs/04.specs/10.backend-extract-review.md` 第 5、7 节
  - `12`–`17` 各单元第 4 节须先有编译记录

### Business Goal

在六个平台模块都按 springboot2 换入并接好线之后，做一次 **可重复的静态门禁**：全量 JDK 8 编译、纯度检索未回潮、五项接线齐全。运行时单独列为未验证，避免把「编过」写成「测过」。

### Scope

In scope:

- 聚合编译 `ktg-admin -am`
- 纯度回归：抽出迁入 `ktg-mes` 的类型不得回到四平台模块（清单对齐 `09`）
- 五项接线勾选（对齐 `10` 第 7 节与 `11`）
- 记录对照 commit 仍为 `913524b1525a0e374774afbb54c452d8f68a5a8f`
- 确认模块名仍为 `ktg-*`、包名仍为 `com.ktg`、`ktg-mes` 仍约 721 个 Java 且非本批改动主体

Out of scope:

- 阶段 2：Java 17、Boot 4.1、jakarta、模块改 `ruoyi-*`、包名 `com.ruoyi`、前端 Vue3
- 本单元内再换一块模块
- 启动应用、连真实库/Minio（若有人做了，把命令摘要写入第 4 节，**未做则明确未验证**）

### Acceptance Criteria

- `12`–`17` 均已实施且各文件第 4 节有成功编译摘要（本文件不得替它们「补勾」）
- 本文件第 4 节有一次全量 `compiler:compile` 的真实输出摘要后，才能写「阶段 1 编译通过」
- 纯度检索与五项表全部 `[x]` 或书面豁免
- 未跑的测试/启动不得写通过

### Rules and Constraints

Tenant isolation:

- 不涉及

Security and masking:

- 第 4 节不粘贴密钥、连接串、Token
- 401 探测不写内网主机

Audit and lineage:

- 对照 hash 漂移则本门禁失败，先改 `11`

AI/rule-engine rules:

- 不涉及

Database rules:

- 不执行 DROP；不把上游 `sql/` 整包打进现网

## 2. Plan

### Implementation Approach

1. 确认 nested toplevel、分支、对照 worktree hash
2. 跑全量编译
3. 跑纯度 `rg`
4. 勾五项
5. 列出运行时未验证项（固定清单，不要删条）

### Affected Modules

- 只读检查为主；本单元不应再改 Java。若检查失败，回到对应 `12`–`17` 修，不要在 `18` 开新范围

### Public Contracts

- 无新 API

### Persistence Changes

- 无

### Transaction Boundaries

- 无

### Risks

- 用 JDK 21 编过 Lombok/mes 却当成阶段 1 成功
- 忘记 antrun skip 把失败算在换壳上
- 把阶段 2 目标写进本门禁失败项

### Verification Strategy

见第 3 节命令。检索根目录：`startech-mes-basic/backend`。

## 3. Task Breakdown

| Task | Description | Output | Verification |
| ---- | ----------- | ------ | ------------ |
| T1 | 全量 JDK 8 编译 | 第 4 节命令摘要 | BUILD SUCCESS |
| T2 | 纯度回归 | 下表 | 平台四模块无 MES 回潮 |
| T3 | 五项接线 | 下表 | 全勾 |
| T4 | 未验证运行时清单 | Known gaps | 条数不减少 |

纯度回归（平台四模块 `ktg-admin/common/framework/system` 的 `src`，类型应只在 `ktg-mes`）：

- `AutoCodeUtil` `MinioUtil` `MobileLoginService` `WebSocketServer` `MessageProvider` `SysAttachment` `UReportConfig` `ItemType`（实体）`BarcodeUtil` `FieldMetaObjectHandler` `godex`
- `UserConstants` 无 MES 业务常量前缀
- `FileUploadUtils` 无 Minio
- `SecurityConfig` 无 `/mes/`、`ureport`、`autocode`、`loginOuth`、`/mobile/` 字面量
- 平台四模块 pom 无 minio / ureport / barcode4j / zxing / hutool（hutool 仅 mes）
- `import com.ktg.mes` 在 admin Controller 应为 0（抽出目标）
- `MesPermitAllProvider` **允许**仅出现在 common 接口 + framework 注入；实现在 mes

五项：

1. `RuoYiConfig` + yml 根键为 `ruoyi`（不再是 `ktg-mes:` 根键）
2. `MesPermitAllProvider` 接口在 common，framework 注入，实现在 mes
3. mes 有 `GET /system/notice/list`；admin `SysNoticeController` 无 `/list`
4. mes 有 `POST /common/uploadMinio`；admin `CommonController` 无该映射
5. `UserDetailsServiceImpl` 含 `selectUserByMobile` + 手机号正则

模块名/包名：

- `rg "artifactId>ruoyi-" backend --glob pom.xml` 应为 0
- 业务 Java 包仍 `com.ktg`；`ktg-mes` 仍在 `<modules>`

## 4. Verification Record

合入树（nested `startech-mes-basic` worktree；**不是** nested `master`）：

```text
nested worktree: /Users/yuzaijiang/workspaces-all/llm-workspace/startech-mes/.worktrees/spec12-17-integrate
branch: swap/spec12-17-integrate
HEAD: f58079cc52b31ce3edb563a1d8b5246ca6b07728
      stop notice DELETE from touching sys_notice_read until a migration exists.
# JDK 8 全量 compiler:compile SUCCESS 证据在 18a056e（本轮未重跑 JDK 8）。
# tip f58079c 只改 notice DELETE，不调用 sys_notice_read。
base: swap/spec12-16-integrate @ 6f1e374
对照: RuoYi-Vue-springboot2 @ 913524b1525a0e374774afbb54c452d8f68a5a8f
JAVA_HOME: Corretto 1.8.0_502
```

Commands run (合入树 `backend/`，`JAVA_HOME` = JDK 8；2026-08-20 18:10):

```text
git rev-parse --show-toplevel
# /Users/yuzaijiang/workspaces-all/llm-workspace/startech-mes/.worktrees/spec12-17-integrate

git -C /Users/yuzaijiang/workspaces-all/llm-workspace/startech-mes/RuoYi-Vue-springboot2 rev-parse HEAD
# 913524b1525a0e374774afbb54c452d8f68a5a8f

"$JAVA_HOME/bin/java" -version
# openjdk version "1.8.0_502"  Corretto-8.502.07.1

mvn -pl ktg-admin -am compiler:compile -DskipTests -Dmaven.antrun.skip=true
# BUILD SUCCESS  5.768 s
# ktg SUCCESS; ktg-common SUCCESS (111);
# ktg-system SUCCESS (56); ktg-framework SUCCESS (45);
# ktg-quartz SUCCESS (18); ktg-generator SUCCESS (13);
# ktg-mes SUCCESS (721); ktg-admin SUCCESS (24)

# 纯度（平台四模块 src：admin/common/framework/system）
# AutoCodeUtil / MinioUtil / MobileLoginService / WebSocketServer /
# SysAttachment / UReportConfig / BarcodeUtil / FieldMetaObjectHandler /
# class ItemType / godex：平台四模块 0；类型在 ktg-mes
# UserConstants 无 MES 业务常量前缀；FileUploadUtils 无 minio
# SecurityConfig 无 /mes/、ureport、autocode、loginOuth、/mobile/ 字面量
# 平台四模块 pom 无 minio / ureport / barcode4j / zxing / hutool（hutool 仅 mes）
# admin Controller import com.ktg.mes = 0
# MesPermitAllProvider：common 接口 + framework @Autowired(required = false) + mes 实现

# 五项
# 1 RuoYiConfig prefix = "ruoyi"；application.yml / application-back.yml 根键 ruoyi:（无 ktg-mes: 根键）；port 仍 8085
# 2 MesPermitAllProvider 接口 common、framework 注入、实现 mes
# 3 mes SysNoticeListController GET /list；admin SysNoticeController 无 /list
# 4 mes MesMinioUploadController POST /uploadMinio；admin 无 uploadMinio
# 5 UserDetailsServiceImpl selectUserByMobile + REGEX_MOBILE

rg "artifactId>ruoyi-" --glob pom.xml
# 0
# ktg-mes 仍在 <modules>；ktg-mes Java 721；package com.ruoyi = 0
```

Result:

- 阶段 1 **静态门禁通过**（JDK 8 全量 `compiler:compile` + 纯度回归 + 五项接线）。**不是**运行时/接口测试通过；未启动应用。
- 合入 tip：`swap/spec12-17-integrate` @ `f58079c`（notice DELETE 不碰 `sys_notice_read`）。全量 compile SUCCESS 记在 `18a056e`；本收口未重跑 JDK 8。
- `12`–`16` 复用 `swap/spec12-16-integrate` @ `6f1e374`；本树 overlay `ktg-admin`（springboot2 `ruoyi-admin` + keep-list），未 wholesale-merge spec17 `5411c97`。
- mes UNIQUE 编译闭合：`UNIQUE`/`NOT_UNIQUE` 保持 boolean；133 个 mes 文件把 `String` 校验签名与 `.equals` 改为 boolean。
- nested `master`（`f740339`）未改、未 push。

五项表：

| # | 接线 | 静态 |
| - | ---- | ---- |
| 1 | `RuoYiConfig` + yml 根键 `ruoyi` | `[x]` |
| 2 | `MesPermitAllProvider` common / framework / mes | `[x]` |
| 3 | mes `GET /system/notice/list`；admin 无 `/list` | `[x]` |
| 4 | mes `POST /common/uploadMinio`；admin 无该映射 | `[x]` |
| 5 | `UserDetailsServiceImpl` 手机号 + `selectUserByMobile` | `[x]` |

先前 PLAN/库存（已执行，仅留作基线分叉记录）：

**先前性质曾是 PLAN / 库存。** 当时未合入用户 nested `master`，未跑全量 `mvn -pl ktg-admin -am`。下文库存仍有效，作为隔离树分叉记录；门禁结果以上方合入树为准。

对照（只读，本轮再测）：

```text
# 外层 2026-08-20 17:50–18:02
git rev-parse --show-toplevel
# /Users/yuzaijiang/workspaces-all/llm-workspace/startech-mes

git -C RuoYi-Vue-springboot2 rev-parse HEAD
# 913524b1525a0e374774afbb54c452d8f68a5a8f

git -C startech-mes-basic rev-parse --abbrev-ref HEAD
# master
git -C startech-mes-basic rev-parse HEAD
# f740339d7162fca8c42717b647b4f1ebde34dfe2
# 用户当前 nested 工作区干净；本轮未 merge、未改 Java
```

### 隔离 worktree 库存（poll ~10 min，终态 18:01）

| 单元 | worktree | 分支 | 相对 nested `master` | overlay commit | dirty | 该 spec 第 4 节编译 |
| ---- | -------- | ---- | ------------------- | -------------- | ----- | ------------------- |
| 12 | `.worktrees/spec12-ktg-quartz` | `swap/spec12-ktg-quartz` | +1，基线 `f740339`（nested `master`） | `87cefba` Overlay ktg-quartz | 0（idle） | **BUILD FAILURE**（缺 3.9.2 common API） |
| 13 | `.worktrees/spec13-ktg-generator` | `swap/spec13-ktg-generator` | +1，基线 `f740339` | `9b30f0f` 换壳 ktg-generator | 0（idle） | **BUILD SUCCESS**（仍是旧 common；单模块） |
| 14 | `.worktrees/spec14-ktg-common` | `swap/spec14-ktg-common` | +23，基线 `upgrade/ruoyi-framework`（`0bdd2f4`） | `e9617e4` Overlay ktg-common（留 MesPermitAllProvider） | 0（idle） | **BUILD SUCCESS**（该 worktree 内聚合） |
| 15 | `.worktrees/spec15-ktg-system` | `swap/spec15-ktg-system` | +23，同 14 基线 | `1f0baaa` 换壳 ktg-system（留 selectUserByMobile） | 0（idle） | **BUILD FAILURE**（缺 common 3.9.2 API） |
| 16 | `.worktrees/spec16-ktg-framework` | `swap/spec16-ktg-framework` | +23，同 14 基线 | `3e9cadd` Overlay ktg-framework | 0（idle） | **BUILD FAILURE**（缺 common/system + Security 5.7） |
| 17 | `.worktrees/spec17-ktg-admin` | `swap/spec17-ktg-admin` | +1，基线 `f740339`（**抽出前** nested `master`） | `5411c97` Overlay ktg-admin | 0（idle） | **BUILD SUCCESS**（该基线 mes **无**抽出后 NoticeList/Minio 控制器） |

合入执行记录（相对上表库存）：`swap/spec12-16-integrate` 已 idle 于 `6f1e374`；本轮从该 HEAD 建 `swap/spec12-17-integrate`，admin 从 springboot2 `ruoyi-admin` 重拷 + keep-list（未 wholesale-merge `5411c97`），mes UNIQUE 改为 boolean 后跑 JDK 8 全量 compile。隔离树表仍只描述当时各块单独基线，**不能**代替合入树门禁。

Known gaps:

> 状态：`[ ]` 开放 · `[x]` 已收口（收口详情见 `docs/02.requirements/99.升级交付剩余缺口与收口清单.md`）

- `[x]` `12`–`17` 已在 `swap/spec12-17-integrate` @ `f58079c` 合入抽出后基线；全量 JDK 8 `ktg-admin -am` **BUILD SUCCESS**（`18a056e`）
- `[ ]` 未登录探测：`/mes/tm/tool/importData`、`/mes/sys/camera/list`、`/mobile/user/**` → 401；`/mobile/login/**`、`/loginOuth`、`/system/autocode/get/**`、`/ureport/**`、`/websocket/**` 仍可达（不写主机）
- `[ ]` 标准 `/login`、手机号登录
- `[ ]` `POST /common/uploadMinio`、本地盘 `/common/upload`
- `[ ]` `GET /system/notice/list` 已读 VO
- `[ ]` UReport 控制台、WebSocket、FieldMeta 插入填充、Godex
- `[ ]` 部署侧配置根键 `ruoyi:` 同步
- `[ ]` 阶段 2 整包升级另立项

## 5. Final Summary

Files changed:

- 本文件第 4 节回填合入树真实 JDK 8 编译 / 纯度 / 五项（外层文档未 commit）
- nested Java 在 `swap/spec12-17-integrate`（见该分支提交）

Behavior delivered:

- 阶段 1 静态门禁通过（编译 + 纯度 + 五项）；运行时清单条数未减、仍为未验证
- nested `master` 未动；父任务决定是否合入 `upgrade/ruoyi-framework`

Next development unit:

- 父任务：merge `swap/spec12-17-integrate` → `upgrade/ruoyi-framework`（不要默认合 nested `master`）
- 阶段 2 见 `19`；未做运行时则不开「测试通过」。Vue3 / spec `25` 改名仍 Out of scope
