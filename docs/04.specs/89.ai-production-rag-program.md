# Development Unit Template

Use this template before each implementation unit.

## 1. Spec

### Capability

- Name: MES AI 生产级知识解析与 Advanced RAG（总控）
- Module: 外层文档 `docs/04.specs/89`–`93`；实现落 `startech-mes-basic/backend/ktg-ai` 与 `startech-mes-basic/startech-mes-front`。**禁止**改 Vue2。
- Priority: P0（总控本身不改 Java）
- Related requirement/design documents:
  - 缺口：`docs/02.requirements/10.mes-ai-production-rag-gaps.md`
  - 设计：`docs/03.design/11.mes-ai-production-rag-plan.md`
  - 对照：[LangChain4j RAG](https://docs.langchain4j.dev/tutorials/rag)
  - A32 基线（只读）：`docs/04.specs/69.ai-rag-pipeline.md`、`docs/03.design/07.mes-ai-runtime-hardening-plan.md` §5.4
  - 知识库/向量（只读）：`docs/04.specs/53.ai-knowledge-p4.md`、`54.ai-vector-oss-p5.md`、`55.ai-rag-p6.md`、`56.ai-dashvector-p7.md`
  - A24 工厂：`docs/04.specs/61.ai-langchain4j-runtime.md`
  - 平台总控：`docs/04.specs/48.mes-ai-platform-program.md`
  - 下一编码单元：`docs/04.specs/90.ai-document-ingest.md` 与 `92.ai-knowledge-admin-ui.md`（可并行）→ `91.ai-advanced-rag.md`；平台向量集群 `93.ai-vector-cluster-ui.md`（可并行，不挡 90/91/92）

### Business Goal

工厂上传的 PDF / Office / 文本能按类型解析；对话与 Agent 知识检索从 Naive RAG 换成 LangChain4j Advanced RAG。本文件只锁定原则、阶段与决策表，不写 Java/SQL。

### Scope

In scope:

- 锁定本期目标与非目标（对齐设计第 1 节）
- 锁定已拍板决策表（不得在后续单元重开，**PR-G1、PR-G2、PR-G3 除外**）。含 **PR-D19**：切块 size/overlap 上知识库页 + `sys_config` 回落，拆除 yml `AI_RAG_*`。含 **PR-D20**（2026-09-08）：平台默认集群 + 每厂 SHARED/DEDICATED 绑定（非知识库表单、非工厂 overlay）；env 仅默认行空时回落
- 锁定 89–93 文件名与序号
- 规定模块边界、契约冻结、验证与安全底线
- 规定缺口状态只回写 `docs/02.requirements/10.mes-ai-production-rag-gaps.md`

Out of scope:

- 本总控内改 Java、SQL、Vue
- Easy RAG、内存向量库、OCR、HyDE、SQL/Web/图谱 Retriever
- 改 `/ai/chat/send` / `/ai/agent/send` / parse 的 URL 与 SSE 信封
- 换官方 EmbeddingStore、补齐 Milvus/Qdrant（仍属 V2 开放项）
- 改 Vue2、`frontend/`、pad `/platform/**`

### Acceptance Criteria

- `docs/04.specs/` 存在 89–93，章节与 `docs/01.temlplates/01.development_unit_template.md` 一致
- 每份单元 Related documents 链到设计、缺口清单、本总控、上一/下一单元
- 文档无真实地址、凭据
- 已拍板项不得在 90/91/92/93 重开；开放项保持 `[ ]` 直到产品另开轨
- 本总控第 4 节不得写成「本文件已改 Java」

### Rules and Constraints

Tenant isolation:

- 解析、embedding、压缩、展开、打分、向量读写只信 JWT / `TenantContext`
- kid 必须本租户；SQL 回表带 `tenant_id`
- 并行 Executor 拷贝租户上下文

Security and masking:

- 不扩 `MesPermitAllProvider`
- 日志 / SSE / parse_msg 不打印 Key、向量 token、内部路径

Audit and lineage:

- 新 DDL 只进 Flyway `db/migration/tenant`（切线后规则）
- 不改历史 `backend/sql/` 已发布文件
- 运行时可调 RAG 项走 `sys_config` 或知识库列，不新增 yml 产品键、不新增 rag `@Value`
- 向量集群地址/类型走平台（93：默认页 + 租户表单绑定），不走工厂 overlay `sys_config`；yml 仅默认集群回落

AI/rule-engine rules:

- Advanced RAG；禁 Easy RAG；禁 Naive 唯一路径
- Chat/Embedding/Scoring 均 A24 `(tenantId, modelId)`
- 禁止 `SqlDatabaseContentRetriever` 与任意 SQL Tool

Database rules:

- 根键仍 `ruoyi:`
- 向量按当前 `TenantContext` 解析客户端（V4：每租户一种 type，非一 JVM 一种）；改默认或该厂绑定须 evict（93 / PR-D20）

## 2. Plan

### Implementation Approach

先 90 换掉 UTF-8 伪解析（后端）并落地切块列 + `sys_config` 五键、拆除 yml rag；92 补管理页下拉/切块表单/片段/点选（可与 90 并行）；再 91 用 `DefaultRetrievalAugmentor` 替换 `retrieveContext` 内部（topK/minScore 列 → sys_config）。93 平台向量默认/专绑定可并行（不挡 90/91/92）。90/91 按当前租户解析 store。

### Affected Modules

- 文档：本文件与 90/91/92/93；缺口 10；设计 11；总控 48 索引
- 编码：`ktg-ai`、Flyway tenant 列 + master `sys_config`（90/91）；Vue3 知识库页（**92**，不在 90）；平台默认集群页 + 租户绑定 + master 表默认 NULL 行/每厂一行（**93**）

### Public Contracts

- **冻结：** `POST /ai/knowledge/attach/{id}/parse`；`POST /ai/chat/send` 可选 `knowledgeId`；`POST /ai/agent/send`；SSE `{content, done}`
- **内部：** 单一 RetrievalAugmentor facade

### Persistence Changes

- 可选 `ai_knowledge_fragment.source_ref`；**必做** tenant `ai_knowledge.chunk_size` / `chunk_overlap`；master `sys_config` 增量种子 `ai.rag.*`（切块/检索五键在 90；展开/再排序在 91）
- 不改 `ai_knowledge` 已有 `top_k` / `min_score` / `chunk_separator` 语义（空仍=回落，回落源改为 `sys_config`）
- 拆除产品配置：yml `ruoyi.ai.rag.*` / `AI_RAG_*`。向量集群 yml 占位改为 **93 默认集群启动回落**（产品面是平台页/租户绑定，不是「必须改 yml」）
- **93：** Flyway master `ai_platform_vector_store`（默认 NULL 行 + 每厂至多一行）；不进工厂 overlay `sys_config`；不改 `sys_tenant`

### Transaction Boundaries

- 解析：fragment 短事务；向量失败标附件失败可重试（54 已有）
- 检索：只读

### Risks

- Scoring/压缩增加上游调用与时延
- Weaviate score 语义与 minScore（69 已部分处理，91 不得回退）
- Tika 回退误判类型

### Verification Strategy

- 以 90/91/92/93 第 4 节为准；本总控不跑 Maven

## 3. Task Breakdown

| Task | Description | Output | Verification |
| ---- | ----------- | ------ | ------------ |
| T1 | 拍板缺口 10 / 设计 11 / TOC 89–93 | 四层文档 | 编号连续、管理页单独 92、集群页单独 93 |
| T2 | 索引 48 / 03 / 05 / 54 / 69 / 手册 | 指针 | 无「69 未编码」误导本轨 |
| T3 | 回填第 4 节 | 本文 | 不写 Java 已改 |

## 4. Verification Record

Commands run:

```text
（总控不改代码；无 Maven）
```

Result:

- 缺口 `10`、设计 `11`、单元 `89`–`93` 已落盘。本文件未改 Java/SQL/Vue。

Known gaps:

> 状态：`[ ]` 开放 · `[x]` 已收口（收口详情见 `docs/02.requirements/10.mes-ai-production-rag-gaps.md`）

- `[x]` 产品决策 PR-D1–PR-D20、PR-I1–PR-I7（本轨文档；PR-D20/I7 含 2026-09-08 两档修订）
- `[ ]` PR-X1 / PR-X2 / PR-X3 / PR-X4 编码（90 / 91 / 92 / 93 第 4 节）
- `[ ]` PR-G1 OCR、PR-G2 HyDE、PR-G3 专向量不切断公网 LLM
- `[ ]` live 向量集群

## 5. Final Summary

Files changed:

- `docs/02.requirements/10.mes-ai-production-rag-gaps.md`
- `docs/03.design/11.mes-ai-production-rag-plan.md`
- `docs/04.specs/89.ai-production-rag-program.md`（本文件）
- 编码单元：`90`、`91`、`92`、`93.ai-vector-cluster-ui.md`

Behavior delivered:

- 锁定生产级解析 + Advanced RAG 分期与红线。未改运行时。

Next development unit:

- `docs/04.specs/90.ai-document-ingest.md`（解析）与 `docs/04.specs/92.ai-knowledge-admin-ui.md`（管理页，可并行）→ `docs/04.specs/91.ai-advanced-rag.md`；平台默认集群 + 每厂绑定 `docs/04.specs/93.ai-vector-cluster-ui.md`（可并行）
