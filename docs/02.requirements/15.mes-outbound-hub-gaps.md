# MES 仓储出库 Hub 缺口清单

- 轨道：对照快格云出库 Hub（多类型一张表、详情抽屉），把**思路**接到 Startech：**共享菜单 + 现有领料/销售出库散页**，不是搬 React/Ant Design，也不是新开一套出库表。
- 来源：入库缺口 **IH-G1**（`docs/02.requirements/13.mes-inbound-hub-iqc-gaps.md`）拆出本文件。IH-G1 仍开放、**不挡 105**；细则只在本文件 **OH-***。
- 对照总览（进度面）：`docs/02.requirements/14.mes-kuaige-capability-overview.md`（KG-D；本域状态「文档已开、编码未开」，**不是**「进行中」）
- 分期：**P0** 领料 + 销售出库 Hub、详情抽屉（107）。**后续（编码未开）：** P1 其他出库+借料（112）、P2 送货单（113）；退料/退货进 Hub（114，**待确认**）。**编码未授权。** 编码门禁见 `docs/04.specs/106.mes-outbound-hub-program.md`：须入库 105 部署手验收口，或用户明示「开工出库」。
- 设计：`docs/03.design/15.mes-outbound-hub-plan.md`
- 开发单元：`docs/04.specs/106.mes-outbound-hub-program.md` → `107.outbound-hub-p0.md` → `112`–`114`（对照总控 103 只锁 TOC，不写出库算法）
- 业务代码：`startech-mes-basic`（只读核对）；本文只落在外层 `docs/`
- 对照只读：`kuaigeyun-mes/`（不合并、不改写）
- 状态：`[ ]` 开放 · `[x]` 已拍板（产品决策类在本文件拍板后标 `[x]`；实现证据标 `[x]` 时须有对应 spec 第 4 节）
- 分类：`OH-D` 产品决策 · `OH-I` 隔离/权限 · `OH-F` 前端 · `OH-X` 实现证据 · `OH-G` 开放产品缺口 · `OH-P` 后续阶段
- 编号前缀 **`OH-`**（Outbound Hub）。禁止把本域决策写成 `IH-*`（例外：指针 **IH-G1**）。
- 禁止在文档中写入真实服务地址、凭据或内部敏感路径。
- 界面范围已拍板 **UI-A**：只改本切片碰到的页（出库 Hub、详情抽屉）。不改入库 Hub / 旧领料 / 旧销售出库列表布局，不改若依顶栏/侧栏/标签页。
- **禁止打包进本缺口：** CAT-W01 看板、CAT-W04 功能业务、CAT-W05 库存管理、CAT-W06 库存报表、CAT-W07 配料（**CAT-N2**）。工单执行 / 质量其它闸 / 设备不是本刀（**CAT-N1**）。

本轮能由已拍板讨论钉死的 **OH-D1–OH-D16、OH-I1–OH-I6、OH-F1–OH-F6** 已拍板。对照清单有 **待确认（范围）1 条、待补全 2 条**；无逻辑冲突待确认。拆 spec 不等于代码完成，更不等于授权编码。

## 用户故事

仓管在 Vue3 打开「出库」Hub，可按仓库筛选，一张表看到本厂生产领料单与销售出库单；点单号打开右侧抽屉看表头、明细、状态轴、关联工单/销售单（能跳则跳）。执行出库仍走现有 PUT，库存按今日规则扣减。旧「生产领料单」「销售出库单」菜单仍在，权限不断。入库页外观与今日一致。本刀无金额列、无 Hub 打印、无 OQC 闸、无借料、无送货单。

## 现网事实（以代码为准，禁止臆造）

核对仓库：`startech-mes-basic/`（2026-09-15）、`kuaigeyun-mes/` 只读对照。

### Startech

| 项 | 现网 |
| --- | --- |
| 生产领料 | `WmIssueHeaderController` `/mes/wm/issueheader`。列表 `GET /list`（`mes:wm:issueheader:list`）。执行 `PUT /{issueId}`：有行则 `storageCoreService.processIssue` 后 `FINISHED`。**无独立 confirm**。执行后原材料低于下限可能 **自动建采购**（`addPurchase`） |
| 销售出库 | `WmProductSalseController` `/mes/wm/productsalse`。列表 `GET /list`（`@PreAuthorize` 现网注释掉）。执行 `PUT /{salseId}`：`processProductSalse` 后 `FINISHED`。有 `soCode` 时回写 `ProSalesOrderItem` 发货量/剩余量。**无独立 confirm、无 OQC 闸** |
| 生产退料 | `WmRtIssueController` `/mes/wm/rtissue`。执行 `PUT /{rtId}` → `processRtIssue`。Vue3 `views/mes/wm/rtissue`。**是否进本刀 Hub：待确认，默认不进** |
| 销售退货 | `WmRtSalseController` `/mes/wm/rtsalse`。执行 `PUT /{rtId}` → `processRtSalse`。Vue3 `views/mes/wm/rtsalse`。**是否进本刀 Hub：待确认，默认不进** |
| Vue3 页 | `views/mes/wm/issue/index.vue`、`productsalse`：若依 CRUD（筛、表、分页、`el-dialog`） |
| 单据状态 | `PREPARE` →（执行）`FINISHED`（`mes_order_status`）。领料/销售出库不是入库那套确认+执行两步 |
| 租户 | `wm_*` 已在 P5 include，`tenant_id` 失败关闭。登录后只信 JWT |
| 前端栈 | Vue 3.5 + Element Plus 2.13；壳 `layout/index.vue`。禁止第二套组件库 |
| Vue2 | `frontend/` 冻结 |
| OQC | `qc/oqc` 独立 CRUD 存在；**销售出库执行不查 OQC** |

### 快格云（对照，禁止照搬实现）

| 项 | 快格云 |
| --- | --- |
| Hub | 出库页前端聚合 6 种类型：生产领料 / 销售出库 / 委外发料 / 其他出库 / 借料 / 采购退货；详情抽屉；`capabilities` 以后端为准 |
| 闸 | 销售出库确认前 `EnsureOqcForSalesDelivery`（`can_outbound` / `oqc_required`） |
| 列表 | `outboundListAggregate.ts` 多源 `Promise.allSettled` 再切片（前端假分页） |
| 送货单 | `delivery-notices` 独立资源，**不是** Hub 的 `sales_delivery` |
| 栈 | React + Ant Design 6 + ProComponents |

## 对照清单（KG-D9）

- 制度见总览 **KG-D9**。本表是出库第一刀的能力盘点：**草稿**（2026-09-15 按对照仓 `outboundHubTypes.ts` / `outboundListAggregate.ts` / OQC 确认预览与主工程 `WmIssueHeader` / `WmProductSalse` / `WmRtIssue` / `WmRtSalse` 代码核对高价值行）。
- 完整性 = 行均已分类，**不是**把快格云列拷进 SQL。尚未核完的标 **待补全**，禁止默默删除。
- 处置：**纳入**（本刀用现网模型实现思路）· **本刀不做**（范围外，可另开缺口）· **永不做**（禁止照搬实现）· **待确认（冲突）**（升级用户，未拍板则 **挡 107 编码**）· **待确认（范围）**（默认不纳入 P0，不挡入库 105；出库编码前须写明默认不含）。
- 证据：快格云 `outboundHubTypes.ts`（6 种 `OutboundIssueType`）、`outboundListAggregate.ts`、`OutboundConfirmPreviewModal`（OQC tag）、`delivery-notices`；主工程领料 `PUT /mes/wm/issueheader/{issueId}`、销售出库 `PUT /mes/wm/productsalse/{salseId}` 均直接过账。

| 编号 | 类 | 快格云 | 主工程现状 | 处置 | 说明 |
| --- | --- | --- | --- | --- | --- |
| OUT-F01 | 字段 | Hub `outbound_type` 共 6 种（领料/销售出库/委外发料/其他出库/借料/采购退货） | 散页：`wm_issue_header`、`wm_product_salse`；另有 `wm_rt_issue`、`wm_rt_salse` | 本刀不做（整组） | 本刀 Hub 只映射领料↔`ISSUE`、销售出库↔`PRODUCT_SALSE`（OH-D2）。借料、送货单、委外、其它出库、采购退货、11 种类型 **禁止默默纳入** |
| OUT-F02 | 字段 | （Hub 无「生产退料/销售退货」类型；退货更接近入库方向） | Vue3 有 `wm/rtissue`、`wm/rtsalse` | **待确认（范围）** | 总览 CAT-W03 列过这两页。P0 **默认不进 Hub**。未拍板不得 silently include。见 **OH-G1** |
| OUT-F03 | 字段 | Hub 行：单号/状态/日期/仓/客户或工单/数量合计/操作员 | 领料头：`issueCode/Name/status/issueDate/warehouse*/workorder*`；销售头：`salseCode/soCode/client*/salseDate` | 纳入 | Hub 列表用现网列投影，不新建第三张头表 |
| OUT-F04 | 字段 | Hub `total_amount` / 行金额 | 出库头无金额列；行上是否有单价 **待补全** | **本刀不做** | 后续缺口 **OH-G5**。不绑入库 IH-G5，不绑价目 CAT-M06（CAT-N3） |
| OUT-F05 | 字段 | 行：拣货量/批次/序列号集合 | 领料行 `quantityIssued`/`batchCode`；销售行 `quantitySalse`；序列号集合未核完 | **待补全** | 本刀抽屉先展示现网已有行列。不默认为对齐入库 IH-D19 三列 |
| OUT-F06 | 字段 | 自定义字段 enrich、附件 | 若依 `attr1–4` | 本刀不做 | 不搬快格云自定义字段框架 |
| OUT-F07 | 字段 | Hub 打印、仓库筛选、深链 | 旧页或无对等 | **拆分** | **纳入**仓库筛选 + 深链（工单/销售单能跳则跳）。**打印本刀不做**，后续 **OH-G4** |
| OUT-F08 | 字段 | 送货单 `delivery-notices`（独立资源） | 无对等送货单页 | 本刀不做 | 不是 `sales_delivery`。禁止把送货单当销售出库纳入 |
| OUT-S01 | 状态机 | 待出库/待领料/已领料等中文+英文混用；`capabilities.confirm` 以后端为准 | `PREPARE` → 执行 → `FINISHED` | 纳入 | 保留现网字典与一步过账；Hub 状态轴用现网值 |
| OUT-S02 | 状态机 | 快格云「确认出库」一次可入账 | 无 confirm；`PUT /{id}` 即扣库存 | **纳入** | 保留现网一步状态机。不为本刀再造确认步 |
| OUT-A01 | 动作/按钮 | Hub 一张表 + 类型筛选 + 详情抽屉 | 两套若依 CRUD；无聚合 Hub | 纳入 | 新 Hub 页 + `el-drawer`（UI-A）；旧页保留 |
| OUT-A02 | 动作/按钮 | 销售出库确认前 OQC 复核弹窗 | 执行不查 OQC | 本刀不做 | **OH-G2**。P0 只做聚合，不加闸 |
| OUT-A03 | 动作/按钮 | 从工单/销售单 **拉单** 新建出库 | 现网手工建单 | 本刀不做 | 拉单另开缺口 |
| OUT-A04 | 动作/按钮 | `capabilities`：confirm / withdraw / print / delete / update | 执行、删、改；无「撤回已出库并回滚库存」 | 本刀不做 | 本刀不新增撤回入账；打印见 OH-G4 |
| OUT-A05 | 动作/按钮 | Hub 前端多源各拉再切片 | 各 list 服务端分页 | **永不做** | 禁止前端假分页（OH-D10）；Hub 服务端合并 |
| OUT-X01 | 跨域副作用 | （未见对等）领料执行后原材料低于下限自动建采购 | `WmIssueHeaderController.execute` → `addPurchase` | 本刀不做 | **保留现网副作用**，不改 |
| OUT-X02 | 跨域副作用 | （未见对等）销售出库回写订单发货量 | `execute` 有 `soCode` 时更新 `ProSalesOrderItem` | 本刀不做 | **保留现网副作用**，不改 |
| OUT-G01 | 校验/闸 | 销售出库 `can_outbound` 卡 OQC | 现网执行不查 OQC | 本刀不做 | OH-G2；对齐入库「第一刀 Hub、闸可后置」的出库口径（本刀用户点名只聚合散页） |
| OUT-G02 | 校验/闸 | 无行不可确认 | 执行要求有行 | 纳入 | 保持现网「无行不能执行」 |
| OUT-N01 | 永不做（实现） | React / Ant Design / `ListPageTemplate` / 应用中心 | Vue3 + Element Plus + 若依壳 | **永不做** | KG-D1 / UI-A |
| OUT-N02 | 永不做（实现） | 前端假分页聚合当正式 Hub | — | **永不做** | 同 OUT-A05 |

**待确认（冲突）汇总：** 无。逻辑与现网一步过账不冲突（OUT-S02 已纳入保留现网）。

**待确认（范围）：** OUT-F02（`rtissue` / `rtsalse` 是否进 Hub）。P0 默认不含。

**待补全：** OUT-F04（行上金额列是否已有）、OUT-F05（行批次/序列号细目）。禁止把待补全行删掉。

## OH-D — 产品决策

- [x] **OH-D1** 抄什么。**已拍板：抄思路，不抄仓。** Hub + 抽屉。不拷快格云源码、应用中心、Ant Design、Demand/MRP。指针：IH-G1 / CAT-N1。
- [x] **OH-D2** 本切片类型。**已拍板：仅 `ISSUE`（生产领料）与 `PRODUCT_SALSE`（销售出库）。** 借料、送货单、委外发料、其它出库、采购退货、快格云 6 种整组、入库 11 种口径 **不进 Hub**。`rtissue` / `rtsalse` 见 OH-G1（默认不进）。
- [x] **OH-D3** 质量闸。**已拍板：本刀不加 OQC / IQC / FQC 闸。** 销售出库 OQC 另开 **OH-G2**。成品入库 FQC 仍是入库 **IH-G2**，不进出库 TOC。
- [x] **OH-D4** 仓储其余块。**已拍板：不进本缺口（CAT-N2）。** 看板 / 盘点调拨组装拆卸 / 即时库存线边仓 / 库存报表 / 配料各自后开。
- [x] **OH-D5** 金额与价目。**已拍板：本刀无金额列。** 出库金额 **OH-G5** 后开。不绑入库 IH-G5，不绑价目 CAT-M06（CAT-N3）。
- [x] **OH-D6** 状态机。**已拍板：保留现网一步执行过账。** 不为本刀增加 confirm 步。无行不能执行（OUT-G02）。
- [x] **OH-D7** 现网副作用。**已拍板：不改。** 领料执行自动建采购、销售出库回写订单发货量均保留（OUT-X01 / X02）。
- [x] **OH-D10** Hub 列表怎么拿数据。**已拍板：附加** `GET /mes/wm/outboundhub/list`，服务端合并两张现网表并分页。**不替换** `GET /mes/wm/issueheader/list`、`GET /mes/wm/productsalse/list`。禁止前端各拉再切片当正式分页。
- [x] **OH-D11** 旧菜单。**已拍板：保留** 生产领料单、销售出库单（及现网退料/退货菜单，无论是否进 Hub）。Hub 是加法。不自动 `INSERT sys_role_menu`。
- [x] **OH-D12** 菜单号与 path。**已拍板：** 全局菜单 **9310–9319**（入库 Hub 已占 9300–9309）；页 `9310` path=`mes/wm/outboundhub` 组件 `mes/wm/outboundhub/index`；按钮 `9311+`。挂在现网仓储目录下（与领料单同级，`parent_id` 实现时读现网该目录 id）。
- [x] **OH-D13** HTTP 契约。**已拍板：** 旧 URL / method / 认证头 / `code/msg/data` 不变。执行失败仍用现有 `AjaxResult.error(消息)`。新接口本刀仅 Hub list 一条 GET（闸预览本刀不做）。
- [x] **OH-D14** UI-A。**已拍板：只改出库 Hub 页与其抽屉。** 不改 `issue` / `productsalse` / `rtissue` / `rtsalse` / 入库 Hub / `iqc` 旧页布局。不改若依壳、不改 `/index`、不改 F1、不改行业包。
- [x] **OH-D15** 触屏 / kiosk / Demand。**已拍板：不做。**
- [x] **OH-D16** Maven。**已拍板：不新建模块。** Controller 仍在 `ktg-mes` 的 `com.ktg.mes.wm`。
- [x] **OH-D8** 零 Hub 菜单。**已拍板：Hub 是加法。** 角色未勾 9310 时侧栏无出库 Hub；旧领料/销售出库路径与今日一致。不得因未勾 Hub 让 `getRouters` 500。
- [x] **OH-D9** Hub 筛选与深链 / 打印。**已拍板（OUT-F07 拆分）：** Hub list query 增加可空 `warehouseId`。深链：工单号 / 销售订单号能打开现有只读信息或旧页（能跳则跳，无页则纯展示）。**打印本刀不做**，见 **OH-G4**。

## OH-I — 隔离 / 权限

- [x] **OH-I1** 租户。**已拍板：** 只信 JWT / `LoginUser` / `TenantContext`。禁止客户端租户头。Hub 列表不得读出他厂单据。
- [x] **OH-I2** 失败关闭。**已拍板：** 无上下文不得访问 `wm_issue_header` / `wm_product_salse`（若未来纳入退料/退货表则同此）。`isAdmin()` 不得跳过拦截器。SQL 永不跳过 `tenant_id`。
- [x] **OH-I3** 权限。**已拍板：** Hub 列表 `@PreAuthorize mes:wm:outboundhub:list`。行上执行领料仍要求现网领料权限字（与旧页执行按钮同一套）。销售出库执行同旧页。无权限 403。
- [x] **OH-I4** 匿名面。**已拍板：** 不把 `/mes/wm/outboundhub/**` 写入 `MesPermitAllProvider`。
- [x] **OH-I5** 脱敏。**已拍板：** 日志 / toast / API 不打印 JDBC、cipher、JWT、内部路径。
- [x] **OH-I6** 专库。**已拍板：** 本刀无新业务列则无 tenant DDL；若后续待补全行要求加列，走 Flyway **tenant**。菜单/`sys_config` 全局行走 **master**。`DATABASE` 未追上则该厂失败关闭。

## OH-F — 前端

- [x] **OH-F1** 只改 Vue3 `startech-mes-front/`。不改 `frontend/`。不引入 Ant Design Vue / UnoCSS / hook-fetch；不升级 Vue / Vite / Element Plus；不提交 lockfile。
- [x] **OH-F2** 新页。**已拍板：** `src/views/mes/wm/outboundhub/index.vue`；API `src/api/mes/wm/outboundhub.js`；抽屉为该页子组件，不新建全局壳。
- [x] **OH-F3** 页模式。**已拍板：** 类型 / 状态 / 单号 / 日期 / **仓库**筛选 + `el-table` + 行内打开 `el-drawer`。抽屉：描述列表 + 状态轴 + 现网明细 + 关联单号深链。用 Element Plus。无 Hub 专用打印按钮。无 OQC 闸块。
- [x] **OH-F4** 请求。**已拍板：** `src/utils/request.js` Bearer；不传租户头。
- [x] **OH-F5** 旧页。**已拍板：不重排** `issue/index.vue`、`productsalse/index.vue`、`rtissue`、`rtsalse`、入库 Hub。
- [x] **OH-F6** 空态。**已拍板：** Hub 无行时 Element 空表格/空态一句「暂无出库单」。

## OH-G — 开放产品缺口

- [ ] **OH-G0** 与入库指针。**本文件即 IH-G1 的独立缺口。** 入库 13 的 IH-G1 保持 `[ ]`、later-not-now（不挡 105 编码）。关闭 IH-G1 须等本出库 P0 实现证据，不在本文提前勾掉。
- [ ] **OH-G1** 生产退料 / 销售退货是否进 Hub（OUT-F02）。**待确认（范围）。** P0 默认不含 `rtissue`/`rtsalse`。拍板前禁止编码纳入。不挡 105。门控单元：[`docs/04.specs/114.outbound-hub-returns-gated.md`](../04.specs/114.outbound-hub-returns-gated.md)（未拍板禁止编码；默认不进 107/112）。
- [ ] **OH-G2** 销售出库 OQC 闸。**later-not-now。** 不挡 105；也**不**进 107 P0 范围。本会话不另开 spec。
- [ ] **OH-G3** 委外发料 / 采购退货（残留）。**later-not-now。** 其他出库+借料已拆 **OH-P1**（112）；送货单已拆 **OH-P2**（113）。残留禁止塞进 107。
- [ ] **OH-G4** Hub 打印（OUT-F07 打印支）。**本刀不做，必须可追。** 本会话不另开 spec。
- [ ] **OH-G5** 出库金额（OUT-F04）。**本刀不做，必须可追。** 不绑 IH-G5、不绑价目 CAT-M06。本会话不另开 spec。

## OH-P — 后续阶段（文档已开、编码未开）

- [ ] **OH-P1** 其他出库 + 借料（CAT-W03 余型）。编码单元：[`docs/04.specs/112.outbound-hub-other-borrow-p1.md`](../04.specs/112.outbound-hub-other-borrow-p1.md)。现网无对等头表；编码须表结构拍板。**禁止打进 107。** 不含退料/退货、不含送货单、不含委外/采购退货。
- [ ] **OH-P2** 送货单（OUT-F08 独立资源）。编码单元：[`docs/04.specs/113.outbound-hub-delivery-p2.md`](../04.specs/113.outbound-hub-delivery-p2.md)。**不是** `PRODUCT_SALSE`。禁止打进 107。

## OH-X — 实现证据（代码完成后回写）

- [ ] **OH-X1-code** Hub 列表 + 菜单 9310 + 抽屉：代码与单测。编码未开始。
- [ ] **OH-X1** 部署手验。编码未开始。
- [ ] **OH-X2-code** 执行仍走旧 PUT、租户隔离、无假分页。编码未开始。
- [ ] **OH-X2** 部署手验。编码未开始。

## 开放实现缺口（供 spec 引用）

| 编号 | 内容 | 单元 |
| --- | --- | --- |
| OH-X1-code / OH-X2-code | P0 代码与单测 | 107（**编码门禁未开**） |
| OH-X1 / OH-X2 | 部署/手验 | 107 |
| OH-G0 / IH-G1 | 入库侧指针 | 13 保持开放直至出库 P0 证据 |
| OH-G1 | 退料/退货是否进 Hub | [114](../04.specs/114.outbound-hub-returns-gated.md)；待确认；默认不进 107/112 |
| OH-P1 | 其他出库 + 借料 | [112](../04.specs/112.outbound-hub-other-borrow-p1.md)（编码未开） |
| OH-P2 | 送货单 | [113](../04.specs/113.outbound-hub-delivery-p2.md)（编码未开） |
| OH-G2 | 销售出库 OQC 闸 | 后续另开；本 TOC 不占文件 |
| OH-G3 | 委外发料 / 采购退货（残留） | 后续另开；不进 112 |
| OH-G4 | Hub 打印 | 后续另开 |
| OH-G5 | 出库金额 | 后续另开；不绑 IH-G5 / 价目 |

## 编码门禁（本文件不授权开工）

同时满足才允许开始 107 的 Java/Vue：

1. 入库 `105.inbound-hub-iqc-p0.md` 的 IH-X1 / IH-X2 **部署手验**已收口，**或**用户明示「开工出库」/「105 已关闭」；
2. 本对照清单无未处理 **待确认（冲突）**（当前无）；
3. 开工会话点名 107，且 Git 根为 `startech-mes-basic/`。

当前：**文档已开、编码未开。** 唯一「进行中」编码切片仍是入库 105。112–114 同样未授权。
