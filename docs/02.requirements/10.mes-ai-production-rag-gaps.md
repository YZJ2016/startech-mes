# MES AI 生产级知识解析与 Advanced RAG 缺口清单

- 轨道：把现网知识库从「UTF-8 当文本 + 单次向量检索注入提示词」升级为 **LangChain4j Advanced RAG**，并按类型解析 PDF / Office / 文本。对照：[LangChain4j RAG](https://docs.langchain4j.dev/tutorials/rag)。
- 与 A32 关系：**不重开** overlap / 分隔符 / topK / minScore / 共用检索入口。A32「不做 hybrid / 不做 rerank」只约束单元 69 基线。本轨 **覆盖升级检索形态**：禁止 Naive RAG 作为唯一路径；允许 Query 变换、多 Retriever、RRF 与 Scoring 再排序。仍禁止 BM25 hybrid、仍禁止**单次检索**多向量库融合。V4（2026-09-08）允许同进程多租户各连各的 type，不是融合。
- 设计：`docs/03.design/11.mes-ai-production-rag-plan.md`
- 开发单元：`docs/04.specs/89.ai-production-rag-program.md` → `90.ai-document-ingest.md`（解析）与 `92.ai-knowledge-admin-ui.md`（知识库管理页，可并行）→ `91.ai-advanced-rag.md`；向量集群页 **`93.ai-vector-cluster-ui.md`**（可与 90/91/92 并行，不挡解析/检索）
- 平台史诗（A14/V4 以 2026-09-08 修订为准；其余 A1–A28 只读）：`docs/02.requirements/03.mes-ai-platform-gaps.md`、`docs/03.design/06.mes-ai-platform-plan.md`
- 加固轨（只读 A32 基线）：`docs/02.requirements/05.mes-ai-runtime-hardening-gaps.md`、`docs/04.specs/69.ai-rag-pipeline.md`
- 知识库现状（只读）：`docs/04.specs/53.ai-knowledge-p4.md`、`54.ai-vector-oss-p5.md`、`55.ai-rag-p6.md`
- 业务代码：`startech-mes-basic`（只读核对）；本文只落在外层 `docs/`
- 状态：`[ ]` 开放 · `[x]` 已收口（产品决策类在本文件拍板后标 `[x]`；实现证据标 `[x]` 时须有对应 spec 第 4 节）
- 分类：`PR-D` 产品决策 · `PR-I` 隔离 · `PR-F` 前端 · `PR-X` 实现证据 · `PR-G` 开放缺口
- 编号前缀 **`PR-`**（Production RAG），避免与平台 `A*`、加固 `R*`、F1 `S-*`、行业包 `IP-*` 碰撞。
- 禁止在文档中写入真实服务地址、凭据或内部敏感路径。

本轮产品决策 **PR-D1–PR-D20、PR-I1–PR-I7、PR-F1–PR-F6** 已拍板。拆 spec 不等于代码完成。**PR-G1 / PR-G2 / PR-D20（93）不挡 90/91/92**。**PR-F6 不挡编码**，但 92 第 4 节必须记录是否浏览器点选。

## 用户故事

工厂管理员在知识库页上传工艺 PDF、作业指导书 Word、点检 Excel 或 Markdown 后，点解析能抽出可读正文（不是乱码），切块带文件名与页/表来源。已登录员工在对话里挂该库、或经 Agent `searchKnowledge` 追问「上面那份 SOP 第三步是什么」时，系统先把指代压成独立问句，再按本厂知识库检索、融合排序后注入模型。不要 Easy RAG 演示件，也不要把「整文件当 UTF-8 字符串再固定窗口切」当生产方案。

## 现网事实（以代码为准，禁止臆造）

核对仓库：`startech-mes-basic/`（2026-09-08）。

| 项 | 现网 |
| --- | --- |
| 解析 | `AiKnowledgeServiceImpl.parseAttach`：`objectStore.getObjectBytes` 后 `new String(bytes, UTF-8)`，再 `AiKnowledgeChunker` 分隔符 + overlap |
| 前端允许类型 | `views/ai/knowledge/index.vue`：`doc/docx/xls/xlsx/ppt/pptx/txt/pdf/md`，20MB |
| 向量 | `VectorStoreFactory`：weaviate（默认）或 dashvector；未知 type 失败关闭；无 Milvus/Qdrant |
| 对话 RAG | `AiChatServiceImpl.attachRetrieval`：`retrieveContext(knowledgeId, content)` → 拼一条 system「参考以下知识库片段…」 |
| Agent | `TenantKnowledgeSearchTool` 调同一 `AiRetrievalService.retrieveContext`；档案 `knowledgeIds` 白名单 |
| 参数 | 知识库列已有 `top_k` / `min_score` / `chunk_separator`；**无** `chunk_size` / `chunk_overlap`。切块 size/overlap 与空列回落现走 `AiProperties` `@Value ruoyi.ai.rag.*`（yml `${AI_RAG_*}`：500 / 200 / `\n\n` / topK 5 / minScore 0）。Vue 表单已有 topK / minScore / chunkSeparator，无 size/overlap。**无** `sys_config` `ai.*` 键。本期：知识库列+表单为真相源，空则 `sys_config` `ai.rag.*`（PR-D17 / PR-D19）；yml RAG 旋钮作产品配置拆除 |
| 向量集群地址 | `AiProperties`：`@Value ruoyi.ai.vector-store.type` / `weaviate.host` / `dashvector.endpoint`（yml `${VECTOR_STORE_TYPE:weaviate}` `${WEAVIATE_HOST:}` `${DASHVECTOR_ENDPOINT:}`）及对应 api-key 占位。**不是** MinIO，**不是** chat 模型 host，**不是**知识库切块旋钮。无平台页；工厂「参数设置」可对任意 `sys_config` 键插 overlay，且 `selectConfigByKey` overlay 优先于全局行 → **不能**把集群 URL 放进该表 |
| LangChain4j | 出站 Chat/Embedding 走 A24 工厂；**没有** `RetrievalAugmentor` / `DocumentParser` / `EmbeddingStoreIngestor` |
| 对照教程 | 现网 = 教程 **Naive RAG**（embed query → vector search → inject）。不是 Easy RAG，也不是 Advanced RAG |
| embedding 列 | `ai_knowledge.embedding_model_id` 已有；解析空则取本租户第一条启用 `category=vector`。**知识库表单无下拉** |
| 片段 API | `GET /ai/knowledge/{id}/fragments`（`ai:knowledge:query`，可分页）。**Vue `knowledge.js` 无封装，页面无切块浏览** |
| 浏览器点选 | 53/54/69 均未做已登录点选验收 |

## PR-D — 产品决策

- [x] **PR-D1** 目标形态。**已拍板：LangChain4j Advanced RAG，不是 Naive RAG，不是 Easy RAG。** 检索入口为 `DefaultRetrievalAugmentor`（QueryTransformer → QueryRouter → ContentRetriever(s) → ContentAggregator → ContentInjector）。禁止把 `AiServices.contentRetriever(EmbeddingStoreContentRetriever)` 当作唯一生产路径。禁止引入 `langchain4j-easy-rag`、进程内 `InMemoryEmbeddingStore`、ONNX 默认 bge 当本厂 embedding。
- [x] **PR-D2** 覆盖 A32 的范围。**已拍板：** 保留 overlap、分隔符、topK、minScore、chat 与 `searchKnowledge` **同一检索 facade**。A32「不做 rerank」**不**阻止本轨 `ContentAggregator`（RRF + Scoring 再排序）。仍禁止：BM25 / Azure / Elasticsearch hybrid；**单次检索**融合多种 `VECTOR_STORE_TYPE`；知识库直连 MES SQL（A7）。同进程多租户各用各的 type 见 V4 修订，不是本条禁止项。
- [x] **PR-D3** 文档解析。**已拍板：按类型走 LangChain4j `DocumentParser`，禁止整文件 UTF-8 兜底当成功。** 路由：`pdf` → `ApachePdfBoxDocumentParser`；`doc/docx/ppt/pptx/xls/xlsx` → `ApachePoiDocumentParser`；`md` → `MarkdownDocumentParser`（或与 txt 相同若 markdown 模块未纳入 BOM 则 `TextDocumentParser`）；`txt` → `TextDocumentParser`。扩展名与 Content-Type 不一致时以魔数/Tika 探测再路由；探测失败或抽出文本为空 → 解析失败（附件 `parse_status=2`），不得写入乱码片段。`ApacheTikaDocumentParser` **只作探测/未知但允许类型的回退**，不得替代 PDFBox/POI 主路径。
- [x] **PR-D4** 字节来源。**已拍板：** 仍从现网 MinIO `AiObjectStore` 读流。禁止 `FileSystemDocumentLoader` 扫服务器磁盘；禁止把 MinIO 对象下载到未隔离临时目录后当「本机语料库」。
- [x] **PR-D5** 索引管线。**已拍板：** 对齐 `EmbeddingStoreIngestor` 四段：**DocumentTransformer**（写入 `tenant_id` / `knowledgeId` / `attachId` / `file_name`，去空控制符）→ **DocumentSplitter**（`DocumentSplitters.recursive` + overlap>0；知识库 `chunk_separator` 作为优先切分）→ **TextSegmentTransformer**（段首附加文件名，有页/表则附加 `source_ref`）→ embedding（A24，`EmbeddingInputType.DOCUMENT` 仅当该模型 `supportedParameters` 支持，否则不传）→ **现网 `VectorStoreService`**。禁止换官方 `langchain4j-weaviate` 为本期主存储。
- [x] **PR-D6** Query 变换。**已拍板：必须 `CompressingQueryTransformer`**（用当前对话/Agent 的 A24 `ChatModel`，把多轮指代压成独立问句）。**默认开启** `ExpandingQueryTransformer`（压缩后再展开，条数小，默认 3，可 `sys_config` 关）。禁止 HyDE 进本期（PR-G2）。变换失败：回退原始用户问句，不 500、不跨租户检索。
- [x] **PR-D7** Query 路由。**已拍板：`DefaultQueryRouter` 扇出到本请求允许的每个 knowledgeId 的 ContentRetriever。** 禁止 `LanguageModelQueryRouter`（贵且可能漏库）。chat 只带一个 `knowledgeId` 则单 Retriever；Agent 用档案 `knowledgeIds`。
- [x] **PR-D8** ContentRetriever。**已拍板：** 自写 Retriever，内部调现网向量 `search(tenantId, kid, vector, topK)` + fragment 回表。Filter 只信 `TenantContext` + 已校验 kid，禁止客户端传 `tenantId`。query embedding 走 A24；支持则 `EmbeddingInputType.QUERY`。
- [x] **PR-D9** 聚合与再排序。**已拍板：** 先 `DefaultContentAggregator` 两段 RRF；再 `ReRankingContentAggregator`。Scoring 用本厂 A24 **chat 模型实现 `ScoringModel`**（结构化打分），**不**引入 Cohere / 新 SaaS。Scoring 失败或超时 → 保留 RRF 顺序（失败打开，不空检索）。候选上限为实现 spec 写死的小倍数（如 2×topK）。
- [x] **PR-D10** 邻块扩窗。**已拍板：** 向量命中后按同一 `attach_id`、`chunk_index` ±1 从 `ai_knowledge_fragment` 回表拼接（parent / window），再交给 Injector。禁止把整份附件塞进提示词。
- [x] **PR-D11** 注入。**已拍板：** `DefaultContentInjector`（或等价）注入片段；`metadataKeysToInclude` 至少 `file_name`、`source_ref`。提示词与 SSE **不含** 对象键全路径、MinIO 内网、apiKey。无命中仍纯对话（55 行为保留）。
- [x] **PR-D12** 接线。**已拍板：** `POST /ai/chat/send` 的 `knowledgeId`、`searchKnowledge`、以及挂知识的 Agent 对话，都走同一 `RetrievalAugmentor` facade。禁止再保留一条「只 embed 原句、不压缩、不聚合」的平行生产路径。单测可保留 Fake 向量。
- [x] **PR-D13** HTTP 契约。**已拍板：不改** `POST /ai/knowledge/attach/{id}/parse`、`POST /ai/chat/send`、`POST /ai/agent/send` 的 URL / method / 认证 / SSE `{content, done}`。解析失败 `msg` 对用户可读（如「无法从 PDF 提取文本」），不回堆栈与内部路径。
- [x] **PR-D14** 明确不做。**已拍板：本期不做** Easy RAG；`SqlDatabaseContentRetriever`；Web Search；Neo4j / GraphTransformer；OCR 扫描件（PR-G1）；官方 Azure/ES hybrid；把 fragment 原文改成进向量库而不回 MySQL。
- [x] **PR-D15** 依赖。**已拍板：** `ktg-ai` 增加无 version 坐标：`langchain4j-document-parser-apache-pdfbox`、`langchain4j-document-parser-apache-poi`、`langchain4j-document-parser-apache-tika`；markdown 解析器若在 1.19.x BOM 中则加 `langchain4j-document-parser-markdown`，否则 md 走 Text。父 POM 仍只 60 的双 BOM，禁止手写 version，禁止 `langchain4j-easy-rag`。
- [x] **PR-D16** 并行检索线程。**已拍板：** `DefaultRetrievalAugmentor` 自定义 `Executor` 必须拷贝 `TenantContext`（对齐 I-A4 / I-A7）。空上下文不得检索。
- [x] **PR-D17** 可运营旋钮。**已拍板（修订）：** 运行时可调 RAG 项**不写 yml、不新增 `@Value`。** ① 租户/全局默认走系统管理「参数设置」`sys_config`（`ISysConfigService.selectConfigByKey`，键前缀 `ai.rag.`）：展开开关、展开条数、是否再排序，以及切块/检索回落默认（`chunk-size` / `chunk-overlap` / `chunk-separator` / `top-k` / `min-score`）。② 知识库级覆盖走 `ai_knowledge` 列（现列 top_k / min_score / chunk_separator，加列见 PR-D19）。空列 → `sys_config`（本厂 overlay 优先，否则全局行）。禁止 yml 与 `sys_config` 双源。现网 `ruoyi.ai.rag.*` / `AI_RAG_*` **作为产品配置拆除**（实现 90/91 去掉对应 `@Value`）。yml / 环境变量只留启动基础设施。**向量集群地址/类型不再以 yml 为产品面**（PR-D20：平台默认 + 每厂绑定，env 仅默认行空时回落）。密钥仍不进 yml，也不进 `sys_config`。种子：Flyway **master** 全局行（`tenant_id` NULL），对齐 `mes.index.layout`；现网无既有 `ai.*` 键。缺键时 Java 硬编码与种子相同的 500 / 200 / `\n\n` / 5 / 0，仅防空库单测，不是第二套产品配置。
- [x] **PR-D18** embedding 模型。**已拍板：** 知识库可绑本租户 `category=vector` 的 `embeddingModelId`；空则仍取本租户第一条启用 vector 模型。未配置 embedding → 解析失败关闭（现网已如此），不得静默跳过向量当成功。
- [x] **PR-D19** 切块 size/overlap 上知识库页。**已拍板：** 管理员在现网知识库新增/修改对话框输入切块参数，不靠运维改 yml。`ai_knowledge` 增列 `chunk_size`、`chunk_overlap`（SQL snake；Java/JSON `chunkSize` / `chunkOverlap`）；与已有 `topK` / `minScore` / `chunkSeparator` 同一表单。空 = 不覆盖，回落 PR-D17 的 `sys_config`。校验：overlap **> 0** 且 overlap **<** chunk_size（对齐 `AiKnowledgeChunker` / `AiProperties.getRagChunkOverlap`：overlap≤0 非法；overlap≥size 则夹到 `size-1`）。不改 parse / chat/send / agent/send URL。列与 ingest 读路径：90；表单：92；检索 topK/minScore 读路径：91。**不把向量集群地址放进该对话框**（PR-D20 / 93）。
- [x] **PR-D20** 向量集群地址上平台页。**已拍板（2026-09-08 修订）：**「向量集群地址」= 现网 `ruoyi.ai.vector-store.*` 的 **type**（`weaviate` \| `dashvector`；未知失败关闭）、**Weaviate host**、**DashVector endpoint**。不是 MinIO、不是 chat 模型 host、不是知识库切块/RAG 旋钮。产品面 = **平台运营**（Vue3 `/platform/**`，`isPlatformOperator`），**不是** `/ai/knowledge`，**不是**工厂「参数设置」overlay。现网工厂可对 `sys_config` 同键插 overlay 且 overlay 赢 → 禁止把集群 URL 放进该表。
  - **两档（类比 MySQL `ROW`/`DATABASE`）：** **SHARED**（无专绑定 → 平台默认集群，逻辑 `tenant_id` 过滤）；**DEDICATED**（平台绑定该厂自建集群，物理隔离）。谁部署库：租户/其运维；MES 只存连接。
  - **存储：** Flyway **master** 一张表 `ai_platform_vector_store`：`tenant_id` NULL = 平台默认一行；`tenant_id` = 工厂 = 专绑定（每厂至多一行）。不进 include / 专库；拦截 ignore。**不**加列到 `sys_tenant`（JDBC 已在该表，cipher 密钥不同）。
  - **页：** 默认集群薄页 `/platform/ai-vector`；专绑定做在 `/platform/tenant` 表单（与 JDBC 同操作面），HTTP `GET`/`PUT /platform/tenant/{id}/vector-store`。
  - **Key：** 只写加密（`AI_MODEL_CIPHER_KEY`），GET 永不回明文/cipher。**env 回落仅默认行空时**（`WEAVIATE_API_KEY` / `DASHVECTOR_API_KEY` 等）；DEDICATED 必须平台录入，禁止工厂 env。
  - **运行时：** 当前 `TenantContext`：有专行用专客户端，否则默认行，再否则 env（yml 仅启动回落）。
  - **V4 修订：** 每租户客户端一种 type，懒缓存 `(tenantId)`，平台保存 evict；同进程可混合 Weaviate 与 DashVector。不是 per-knowledge。
  - **A14 修订：** 仍是基础设施；工厂仍不可劫持 URL。原「不重开每厂 Cluster / master 一行 / 一进程一种」作废。实现：`docs/04.specs/93.ai-vector-cluster-ui.md`。

## PR-I — 隔离 / 权限

- [x] **PR-I1** 检索与写入必带当前 `tenant_id`；跨租户 kid 404，不调向量、不调压缩 LLM。
- [x] **PR-I2** Query metadata / `InvocationParameters` 可含 session 记忆 id，**禁止**含客户端 `tenantId`。
- [x] **PR-I3** `/ai/**` 必须登录；不进 `MesPermitAllProvider`。
- [x] **PR-I4** 日志 / SSE / parse_msg 不打印 apiKey、向量 token、JWT、内部绝对路径。
- [x] **PR-I5** Scoring / Compressing / Expanding 出站 Key 只来自 A24 `(tenantId, modelId)`，禁止全局 `ChatModel` Bean。
- [x] **PR-I6** 邻块回表 SQL 必须带 `tenant_id`；不得 `isAdmin()` 关拦截器。
- [x] **PR-I7** 向量集群写权限。**已拍板（2026-09-08 修订）：** 仅平台账号可写默认集群与每厂绑定（type/host/endpoint/Key、SHARED↔DEDICATED）。工厂 403：写、读默认配置、读他厂绑定。专集群工厂用户亦不得见 API Key。禁止 overlay、禁止知识库表单、禁止 pad 注册 `/platform/**`。向量读写仍带请求 `tenant_id`（I-A3）。ingest/检索按当前 `TenantContext` 解析 store（有绑定用专，否则默认）。

## PR-F — 前端

- [x] **PR-F1** embedding 下拉。**已拍板：** 只改 Vue3 知识库新增/修改表单，绑已有 `embeddingModelId`。options 走现网 `GET /ai/model/options`，前端只展示 `category=vector`（或等价）。可清空；空 = 后端仍用本租户第一条启用 vector 模型。不回显 Key。无模型 options 权限时下拉为空，不扩匿名面。实现：`docs/04.specs/92.ai-knowledge-admin-ui.md`。
- [x] **PR-F2** 解析失败展示后端 `parse_msg`；不把二进制当成功。允许类型列表与 PR-D3 对齐。实现随 92（展示）与 90（失败语义）。
- [x] **PR-F3** 不引入 element-plus-x / UnoCSS / hook-fetch；不改 Vue2；不提交 lockfile。
- [x] **PR-F4** 对话页契约不变：未选档案可带 `knowledgeId`；选档案不传 `knowledgeId`。
- [x] **PR-F5** 片段只读浏览。**已拍板：** 在知识库附件区提供切块列表，调现网 `GET /ai/knowledge/{id}/fragments`，不新开 HTTP。`knowledge.js` 补封装。只读（序号、正文、可选 `source_ref`）；禁止改/删单条 fragment。实现：92。
- [x] **PR-F6** 浏览器点选。**已拍板：** 92 验收路径冻结为：建库 →（可选）选 vector 模型 → 上传 txt/md → 解析 → 打开片段列表能看见切块。有已登录环境必须点选并写入 92 第 4 节；无环境须写「未点选」，不得称 UI 已验。PDF/Office 点选不挡 92（属 90）。

## PR-G — 开放产品缺口（不挡 90/91/92）

- [ ] **PR-G1** 扫描件 OCR。纯图片 PDF / 无文字层，本期失败并提示，不接 OCR 引擎。
- [ ] **PR-G2** HyDE / step-back。教程列为 Query 增强手段，本期不做，避免双倍 embedding 成本。
- [ ] **PR-G3** 专向量库不切断公网 LLM。DEDICATED 只隔离向量存储；切块正文仍可能经本厂 A24 embedding/chat 出站到公网模型。不在 93 扩大范围；若要「知识不出公网」须另开模型出站/专网模型约束。

## PR-X — 实现证据（代码完成后回写）

- [ ] **PR-X1** 文档解析与 ingest（90）。单元 `docs/04.specs/90.ai-document-ingest.md`。
- [ ] **PR-X2** Advanced RAG 接线（91）。单元 `docs/04.specs/91.ai-advanced-rag.md`。
- [ ] **PR-X3** 知识库管理页 embedding 下拉 + 切块/检索表单（含 chunkSize / chunkOverlap）+ 片段浏览 + 浏览器点选记录（92）。单元 `docs/04.specs/92.ai-knowledge-admin-ui.md`。
- [ ] **PR-X4** 平台默认集群页 + 每租户绑定 + 库优先/默认 env 回落 + Key 只写加密 + 按 `tenantId` evict（93）。单元 `docs/04.specs/93.ai-vector-cluster-ui.md`。不挡 90/91/92。
