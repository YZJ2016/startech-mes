# Vue3 管理端与换壳后后端 API 缺口清单

- 轨道：`startech-mes-front`（RuoYi Vue3 3.9.2 壳）对齐 `backend`（JDK 17 / Spring Boot 4.1.0 平台 + MES 五项接线）
- 原则：平台跟换壳后的若依；MES 定制走 mes；禁止用若依 3.9 公告已读 HTTP 覆盖 `GET /system/notice/list`
- 状态：`[ ]` 开放 · `[x]` 已收口（含 **已收口-不修改**：原因明确、本对齐程序不改前端/不补 Java，不是接口已通）
- 分类：`P` 平台壳（单元 28）· `M` MES 定制须保留 · `D` MES 领域 · `X` 前端多调且无 HTTP · `K` 已知保留不接
- 收口时在对应 spec 第 4 节写证据，并把本文件该项改为 `[x]`

历史 Vue3 迁移文档（`docs/superpowers/specs/2026-08-21-*`）按 3.8.2 后端写，**不再当契约**。

## P — 平台壳（`docs/04.specs/28.vue3-platform-api-align.md`）

- [x] **P1** 锁屏：`POST /unlockscreen`、`/lock`、Navbar、`permission.js`。
- [x] **P2** `getInfo` 写入 `pwdChrtype` → session `pwrChrtype`，以及 `isDefaultModifyPwd` / `isPasswordExpired`。
- [x] **P3** 铃铛改 MES `GET /system/notice/detail/message` 与 `POST /system/notice/detail`；UI 用 `readStatus` / `userId1`。
- [x] **P4** `notice.js` 已删 `listTop` / `markRead` / `markReadAll` / `readUsers`。
- [x] **P5** 上传默认 `POST /common/uploadMinio`。
- [x] **P6** `GET /loginOuth`、路由、白名单。
- [x] **P7** `GET /system/user/deptTree`；选人组件已改接。
- [x] **P8** 平台 `src/api` 主体已对照 Controller；死路径 `treeselect` 已删。残留 **X2**、**X3** 于 2026-08-21 已收口-不修改。

## M — MES 定制

- [x] **M1** `GET /system/notice/list` 仍在 mes；admin 无 `/list`。
- [x] **M2** 默认 `POST /common/uploadMinio`。
- [x] **M3** `GET /loginOuth`。
- [x] **M4** `/system/autocode/**`（含 `GET /system/autocode/get/{ruleCode}`）仍在 mes。
- [x] **M5** `/ureportM/**`；预览基址来自 `VITE_APP_UREPORT_URL`（见 34）。

## X — 前端多调，不补后端

- [x] **X1** 已删除 Vue3 对 `listTop` / `markRead` / `markReadAll` / `readUsers` 的调用。
- [x] **X2** `GET /system/post/listAll`（工位工人页；Vue2 同样；后端仅 `GET /system/post/optionselect`）。2026-08-21 已收口-不修改：本轮不对齐改前端/不补 Java；不静默改接到 `optionselect`；形状未核。
- [x] **X3** `GET /system/dict/data/tree/{dictType}`（评价页；Vue2 同样；无 Controller）。2026-08-21 已收口-不修改：本轮不对齐改前端/不补 Java；后端无同形接口。

## K — 已知保留

- [x] **K1** `SysNoticeReadService` 无 HTTP。不删、不挂 `listTop`。
- [x] **K2** `/get_post_signature_for_oss_upload` 不设为上传默认。
- [x] **K3** `GET /monitor/logininfor/unlock/{userName}` 与锁屏不是同一契约。

## D — MES 领域

- [x] **D-md** 标准 CRUD 已对齐；图纸 add/update/delete 接到 `/md/manage`。证据：`docs/04.specs/29.vue3-md-api-align.md`。已收口-不修改子项（2026-08-21）：`/md/taskTutnPlate/**` 无 Controller；`importTemplate` / `mditem` 导入模板无 mapping（均与 Vue2 相同）。本轮不补 Java。
- [x] **D-pro** `src/api/mes/pro`、`getIndexNum`、`/pro/sales/order/**` 已对齐；`holder.js` 改为 `GET /mes/tm/log/list` 与 `POST /mes/tm/log`。证据：`docs/04.specs/30.vue3-pro-api-align.md`。已收口-不修改（2026-08-21）：`/pro/sales/invoice`、`/pro/order/item`、`/mobile/pro/**` Vue2 同样未封装，不补 Java。
- [x] **D-wm** wm Controller 已对齐。证据：`docs/04.specs/31.vue3-wm-api-align.md`。已收口-不修改（2026-08-21）：`POST /mes/purchase/productionConfirm`、`GET /mes/purchase/lackList`（`itempurchase.js`；勿接到 `/mes/tm/purchase`）。打印走 `VITE_APP_PRINT_AGENT_URL`。
- [x] **D-qc** 已对齐。证据：`docs/04.specs/32.vue3-qc-api-align.md`。`/mobile/qc/**` 管理端不接。已收口-不修改（2026-08-21）：若干 `POST .../export` 后端有、管理端页面未调（Vue2 相同），非 URL 打错。
- [x] **D-cal** 已对齐。证据：`docs/04.specs/33.vue3-cal-dv-api-align.md`。已收口-不修改（2026-08-21）：planteam GET-by-id/PUT 与若干未调 export 与 Vue2 相同。
- [x] **D-dv** 已对齐（同上）。已收口-不修改（2026-08-21）：设备页 `importData`/`importTemplate` 无 HTTP，不补 Java。
- [x] **D-rest** tm/edu/report/camera/contract/sale 可对齐项已处理（评价 export POST、ureport 预览 env）。证据：`docs/04.specs/34.vue3-rest-mes-api-align.md`。已收口-不修改（2026-08-21）：`GET /jasper-report/previewPrint/{name}`、`GET /mes/report/{name}`（Vue2 同样；后端无 mapping）。

运行时登录/锁屏/上传/铃铛冒烟与 `build:prod`：仍开放（验证门禁，不是「不需要修改」）。本机后端未达，未勾通过。
