# MES 多租户 AI 平台缺口清单

- 与 Phase 1 内嵌助手的关系：`docs/02.requirements/04.mes-ai-phase1-gaps.md` 与 `docs/04.specs/57`–`59` 冻结 **对话** HTTP/表/三页（Phase 1 那三页本身无 RAG）。工厂 `ai` 许可已由 49 落地，**覆盖**原 P1-D11「不建套餐」；现行许可闸以本文件 **A9 / I-A2** 为准。`48`–`56` 是平台轨道且 **已与 57–59 同模块落地**（知识库 / RAG / 向量）。不要用本清单的 A1 去改 57–59 冻结路径；也不要把 53–56 当成尚未开工。
- 轨道：在 `feature/mes-multi-tenant` 上并行建设 AI 平台；AI 是租户许可能力（`mes_module.ai`），不是第二套认证或第二套租户
- 设计：`docs/03.design/06.mes-ai-platform-plan.md`
- 开发单元总控：`docs/04.specs/48.mes-ai-platform-program.md`（阶段单元 `49`–`56`；本清单落盘时未改 Java/SQL）
- LangChain4j 依赖管理：`docs/04.specs/60.ai-langchain4j-bom.md`（父 POM 双 BOM）
- ChatModel 工厂（A24）：`docs/03.design/06.mes-ai-platform-plan.md` §5.1（配置留 DB；`(tenantId, modelId)` 懒缓存；决策不占 spec 号）
- Agent（A25）：`docs/03.design/06.mes-ai-platform-plan.md` §5.2（当期含 Agent；一份定义 + A24；决策不占 spec 号）
- LangChain4j 运行时改写：`docs/04.specs/61.ai-langchain4j-runtime.md`（落实设计 A24+A25；第一批只读知识 Tool；代码已落地，证据见该文件第 4 节）
- 受控写 Tools / MCP / 工作流（A26–A28）：`docs/03.design/06.mes-ai-platform-plan.md` §5.3（不占 spec 号；不进 61）。A26 代码已落地（证据见 `docs/04.specs/62.ai-controlled-write-tools.md` 第 4 节）；A27 代码已落地（证据见 `docs/04.specs/63.ai-mcp-whitelist.md` 第 4 节）；A28 代码已落地（证据见 `docs/04.specs/64.ai-logged-in-workflow.md` 第 4 节）；Vue → `docs/04.specs/65.ai-mcp-workflow-ui.md`（65 代码已落地，证据见该文件第 4 节；浏览器点选未做）
- **运行时加固（A29+，不重开本文件 A1–A28）：** `docs/02.requirements/05.mes-ai-runtime-hardening-gaps.md`、`docs/03.design/07.mes-ai-runtime-hardening-plan.md`；代码 `docs/04.specs/66`–`73`（是否已编码以各 spec 第 4 节与缺口 05 的 R 项为准；**69 / R4 已收口**）
- 对照参考（只读，不合并）：`ruoyi-ai` 后端、`ruoyi-web` 用户对话端
- 状态：`[ ]` 开放 · `[x]` 已收口（收口时在对应 spec 第 4 节写证据，并把本文件该项改为 `[x]`；产品决策类可在设计「已拍板决策」表收口）
- 分类：`A` 产品决策 · `I` 集成/隔离 · `F` 前端 · `V` 向量库
- 禁止在文档中写入真实服务地址、凭据或内部敏感路径

本轮产品决策 **A1–A28、I-A1–I-A6、F-A1–F-A4、V1–V4** 已拍板。拆 spec 不等于代码完成。

## A — 产品决策

- [x] **A1** 目标形态。**已拍板：做 AI 平台**（模型、会话、知识库、后续工具），不是「MES 里嵌一个助手」的终态。第一版先交付可运营的最小平台。
- [x] **A2** 首家对话供应商。**已拍板：OpenAI 兼容**（可配 `baseUrl` + 模型名）。不在第一版并行接入智谱原生 / 百炼原生 / 仅 Ollama 专用协议。
- [x] **A3** 模型密钥存放。**已拍板：按租户加密落库。** 列表打码；API 不回传明文或 cipher。揭明文若做，仅本租户管理员、写审计且审计不记明文。配置源与出站选 Key 见 **A24**（不把模型目录搬进 yml）。
- [x] **A4** 加密主密钥。**已拍板：** 算法与 JDBC 相同（AES-256-GCM）。主密钥只来自环境变量 `AI_MODEL_CIPHER_KEY`，**不**复用 `TENANT_JDBC_CIPHER_KEY`。密钥轮换不进本期。缺密钥且已有加密模型行 → 拒启动（对齐 S6 门禁风格）。
- [x] **A5** 知识库。**已拍板：要。** 原文在 MySQL 片段表 + MinIO 附件；向量库只存向量与引用 id。
- [x] **A6** 向量库批次。**已拍板：A 方案。** 第一批 Weaviate / Milvus / Qdrant 策略工厂；紧接着第四策略 DashVector。不要四种齐头并进当同一单元。运行时「一进程只开一种」已由 **V4 2026-09-08 修订**作废（改为每租户客户端一种 type）；本条仍只约束实现批次。
- [x] **A7** Agent 与 MES 库。**已拍板：不能直连。** 禁止 JDBC、禁止内置 `execute_sql_query` / `query_all_tables`、禁止任意 SQL / SQL MCP。白名单 Tools 允许：61 第一批只读知识检索（A25）；受控写走现有 MES service（方案 **B**，见 **A26** / `docs/04.specs/62.ai-controlled-write-tools.md`，代码已落地）。Tools/Agent **不回写进 49–56**；只读 Agent 改写走 61；MCP 走 63（代码已落地）；工作流走 64（代码已落地，见第 4 节）。
- [x] **A8** 第一版前端形态。**已拍板：内嵌 `startech-mes-front`。** 不另开用户对话端；不部署、不改 `ruoyi-web` baseURL。
- [x] **A9** 分支与许可。**已拍板：** 与租户同分支 `feature/mes-multi-tenant` 并行。AI 是租户能力：`mes_module` 增加 `ai`，写入 `sys_tenant_license`。未许可拒绝 `/ai/**` 且 `getRouters` 不出现 AI 菜单。本条覆盖缺口 04 原 P1-D11「Phase 1 不建工厂套餐」；现行两闸并存（菜单权限 + 许可）。证据：`docs/04.specs/49.ai-foundation-p0.md`。
- [x] **A10** 与 ruoyi-ai 关系。**已拍板：只参考，不整仓合并。** 不引入 Sa-Token、MyBatis-Plus、第二套 JWT、`org.ruoyi` 包。
- [x] **A11** HTTP 前缀。**已拍板：一律 `/ai/**`。** 禁止复用 ruoyi-ai 的 `/system/message|session|info|attach`、`/chat/send`、`/auth/login`。
- [x] **A12** Maven 落点。**已拍板：新建 `ktg-ai`，`ktg-admin` 依赖。** 不塞进 `ktg-mes` / `ktg-system`。包名 `com.ktg.ai`。`ktg-admin` Controller 保持薄。
- [x] **A13** Embedding。**已拍板：** 与对话同一条 OpenAI 兼容协议（可另配 embedding 模型名与维度）。维度创建向量 collection 时写死。
- [x] **A14** 向量集群归属。**已拍板（2026-09-08 修订）：平台基础设施，两档隔离，类比 MySQL `ROW` / `DATABASE`。** 向量集群仍不是工厂 AI 自助（不像模型目录）。平台指定档位：① **SHARED** — 无专绑定，走平台默认共享集群，逻辑过滤 `tenant_id`；② **DEDICATED** — 平台绑定该厂自建集群，物理隔离。连接由平台写入 `/platform/**`，工厂 `/ai/**` 与「参数设置」overlay **不得**改集群 URL（**不勾掉**「工厂不能劫持」）。工厂只管本租户模型密钥。API Key 只写加密（`AI_MODEL_CIPHER_KEY`）或**仅默认集群**允许 env 回落；不进 yml、不进参数表明文。禁止知识库表单改集群。**修订前**（PR-D20 初版）冻结为「一进程一份、master 一行、不改为每厂 Cluster」——该冻结作废。证据轨：缺口 `10` PR-D20；`docs/04.specs/93.ai-vector-cluster-ui.md`。
- [x] **A15** 认证。**已拍板：复用现有 JWT / `LoginUser` / 手机号登录分支。** `/ai/**` 必须登录。禁止第二套 `SecurityFilterChain`。禁止把聊天口写进 `MesPermitAllProvider`。
- [x] **A16** 上传。**已拍板：默认仍 `POST /common/uploadMinio`。** 知识附件走 MinIO + `tenant/{id}/` 前缀（I2）。不引入 OSS 为默认上传。
- [x] **A17** SSE 契约。**已拍板：MES 自定** `POST /ai/chat/send`（SSE）。事件字段先只要 `content`（可选 `reasoning`）。不在第一版兼容 ruoyi-ai 的 MCP / `[NODE_CHUNK_*]` 工作流帧。
- [x] **A18** 独立对话端。**已拍板：v1 不做。** 以后若给进不了管理端的人用，再开端并补登录/SSO；不在 49–56 预埋第二套账号。
- [x] **A19** 游客对话。**已拍板：禁止。** 不复制 ruoyi-web「未登录可进 `/chat`」。
- [x] **A20** 模型列表 VO。**已拍板：永不下发 `apiKey` / cipher / `apiHost` 口令。** 用户可选 `GET /ai/model/options` 冻结为 `{id, modelName, providerCode, category}`（证据：`docs/04.specs/57.ai-phase1-provider-model.md`）。展示名即 `modelName`；允许 `providerCode` / `category`；**不**另收窄成「仅 id + 展示名 + 模型名」。列表/详情仍打码，禁止回 cipher。
- [x] **A21** `DATABASE` 租户。**已拍板：AI 表必须进入专库 catalog / 同一套增量。** 未迁成功则该厂 AI 失败关闭，禁止回落主库写共享库。向量**连接**不随专库 JDBC 自动切换（修订前共享向量不跟专 JDBC）；须平台按 A14 单独绑 SHARED/DEDICATED，可与 MySQL 档位不同。
- [x] **A22** 短剧/Harness/匿名工作流。**已拍板：不进 49–56，也不做下列能力：** 匿名 `/workflow/run`、内置 SQL MCP、短剧、Coding Harness。**Agent** 见 A25；**白名单 MCP** 见 A27；**已登录工作流** 见 A28。不要把 A27/A28 理解成可以搬 ruoyi-ai 匿名 runner。
- [x] **A23** LangChain4j 依赖管理。**已拍板：父 POM 双 BOM。** `dev.langchain4j:langchain4j-bom:1.19.0`（核心 `langchain4j-*`，内部管理 Boot 4 starters `1.19.0-beta29`）与 `dev.langchain4j:langchain4j-community-bom:1.19.0-beta29`（`langchain4j-community-*`：DashScope/Qwen、额外 store、boot4 starters）。双 BOM 有意并存，GA 不重叠。禁止改用过期的 `langchain4j-spring-bom`。Community **有 DashScope、无 DashVector**。本决策只覆盖 dependencyManagement。chat/RAG 出站改写见单元 61（已落地，证据见该文件第 4 节）。证据：`docs/04.specs/60.ai-langchain4j-bom.md`。
- [x] **A24** ChatModel 生命周期。**已拍板：配置留在 DB，按 `(tenantId, modelId)` 懒建缓存。** 每个租户有自己的 apiKey；出站必须用当前租户、当前 `modelId` 那一行的 Key。**不**要求租户之间密钥互不可见（那不是本条验收）。**禁止：** yml 租户模型目录、LangChain4j 全局 `chat-model.api-key` Bean、启动时按租户各造一个实例、`tenantId → 唯一 ChatModel`。改 Key/删行须 evict。A3 加密与 A20 VO 不下发明文不变。证据：`docs/03.design/06.mes-ai-platform-plan.md` §5.1（本清单本条）。代码改写见单元 61（已落地）。
- [x] **A25** Agent / AiService。**已拍板：纳入当期平台轨道。** 一份接口/Agent 拓扑/`@Tool` 单例；`ChatModel` 仍走 A24。AiService 按 `(tenantId, modelId)` 缓存或内部路由；Agent 执行时向工厂要模型（`Function<AgenticScope, ChatModel>` 或等价）。**禁止**只传 modelName 换 Key；**禁止**启动按租户全量注册 Agent。Memory 用 Provider，id 含租户+用户+会话。不进 Phase 1 的 57–59。**61 第一批 Tool 仅只读知识检索**；受控写 / MCP / 工作流见 A26–A28，不塞进 61。证据：`docs/03.design/06.mes-ai-platform-plan.md` §5.2（本清单本条）。代码见单元 61（已落地；无 Agent UI）。
- [x] **A26** 受控写 Tools（方案 B）。**已拍板：允许白名单 Tool 调用现有 MES service 做受控写**（如报工、工单状态），必须带当前 JWT、`TenantContext`、原权限字。禁止绕过 `@PreAuthorize`。禁止任意 SQL、禁止把 DataSource 交给模型。不进 61。证据：`docs/03.design/06.mes-ai-platform-plan.md` §5.3。实现单元：`docs/04.specs/62.ai-controlled-write-tools.md`（代码已落地，见第 4 节）。
- [x] **A26 代码**（登记报工 + 工单状态 Tools）。见 `docs/04.specs/62.ai-controlled-write-tools.md` 第 4 节。
- [x] **A27** 白名单 MCP。**已拍板：可作为 Tool 后端。** 配置按租户加密落库；默认不开放任意远程 MCP；必须登录。禁止 SQL MCP 跑任意语句。不进 49–56 / 57–59 / 61。证据：`docs/03.design/06.mes-ai-platform-plan.md` §5.3。实现单元：`docs/04.specs/63.ai-mcp-whitelist.md`。
- [x] **A27 代码**（租户加密白名单 MCP）。见 `docs/04.specs/63.ai-mcp-whitelist.md` 第 4 节。
- [x] **A28** 已登录工作流。**已拍板：多步编排给已登录管理端用户**，HTTP 仍 `/ai/**`。禁止匿名 `/run`。SSE 自定，不兼容 ruoyi-ai `[NODE_CHUNK_*]`（A17）。不拷 ruoyi-web 工作流 runner 组件（F-A3）；MES 自有页另开 Vue 单元 `docs/04.specs/65.ai-mcp-workflow-ui.md`。不进 61。证据：`docs/03.design/06.mes-ai-platform-plan.md` §5.3。实现单元：`docs/04.specs/64.ai-logged-in-workflow.md`。
- [x] **A28 代码**（已登录 `/ai/**` 编排）。见 `docs/04.specs/64.ai-logged-in-workflow.md` 第 4 节。

## I — 集成 / 隔离

- [x] **I-A1** 行级隔离。**已拍板：** AI 表 `tenant_id bigint NOT NULL`，加入 `tenant.line.include-tables`。无上下文访问失败关闭。INSERT 填充/校验。
- [x] **I-A2** 许可闸。**已拍板：** 除平台账号外，调用 `/ai/**` 须当前租户有效 `module_code=ai` 许可；过期视为未许可。平台模块永不许可给工厂的规则不变（F3）。与 A9 一起覆盖原 P1-D11；`@PreAuthorize` 权限字仍要，不能只靠许可或只靠菜单。
- [x] **I-A3** 向量隔离。**已拍板：** 写入/检索必须带 `tenant_id`。允许「collection/class = 前缀 + kid」但查询仍要过滤 `tenant_id`，禁止只靠 kid 防串租。
- [x] **I-A4** 异步对话。**已拍板：** SSE/embedding 线程沿用 `TenantTaskDecorator`，不用 `InheritableThreadLocal`。空上下文不得调向量库、不得写租户文件键。
- [x] **I-A5** 日志。**已拍板：** 不打印模型 Key、DashVector/Weaviate/Milvus/Qdrant token、JWT、MinIO 凭据、内部绝对路径。异常对前端不回密钥。
- [x] **I-A6** 配置根键。**已拍板：** 应用根键保持 `ruoyi:`。AI 段用占位环境变量（如 `${AI_MODEL_CIPHER_KEY:}`、`${VECTOR_STORE_TYPE:weaviate}`），不写真实值。**不**在 yml 维护每租户 `apiHost` / apiKey / 模型名（A24）。

## F — 前端

- [x] **F-A1** 只改 Vue3 `startech-mes-front/`。**不改**遗留 `frontend/`。
- [x] **F-A2** 依赖。**已拍板：第一版不引入** `vue-element-plus-x`、UnoCSS、`hook-fetch`、TypeScript 工程形态。气泡/输入用现有 Element Plus 重做。禁止为聊天升级 Vue / Vite / Element Plus，禁止提交 lockfile。
- [x] **F-A3** 交互蓝本。**已拍板：** 对照 `ruoyi-web` 的会话列表、模型下拉、SSE 气泡；不搬登录弹窗、扫码、注册、应用市场。**不拷**其 MCP 卡片、工作流 runner 组件（禁止借机引入 x/UnoCSS）。MES 自有 MCP/工作流页见 A27/A28，另开 Vue 单元 `docs/04.specs/65.ai-mcp-workflow-ui.md`，仍 Element Plus。
- [x] **F-A3 代码**（MCP/工作流 Vue3 页，非 Agent 聊天页）。见 `docs/04.specs/65.ai-mcp-workflow-ui.md` 第 4 节（`build:prod` exit 0；浏览器点选仍开放）。
- [x] **F-A4** 页面。**已拍板：** 全宽 `/ai/chat`（弱化 tagsView）+ 模型配置 + 知识库管理。均走现有 `request.js` Bearer JWT。

## V — 向量库

- [x] **V1** 策略工厂。**已拍板：** `VectorStoreService` 风格接口；`type=weaviate|milvus|qdrant|dashvector`。未知 type 失败关闭，不静默落到「不隔离的内存库」。
- [x] **V2** 第一批三种。**已拍板：** 重写适配，不复制 ruoyi-ai 的 MyBatis-Plus Mapper。本地默认建议 `weaviate`（单容器）。
- [x] **V3** DashVector。**已拍板：** 第四策略，独立单元 56。官方 SDK 或 HTTP；可作为平台默认或某厂 DEDICATED 绑定；Partition 或 Field 带 `tenant_id`。community BOM（A23）**不含** DashVector（含 DashScope）；DashVector **仍**自定义 HTTP，不要改写成不存在的 community 构件。不能出厂的租户**通常**绑自建 Weaviate（引导，非禁 DashVector）。
- [x] **V4** 客户端粒度。**已拍板（2026-09-08 修订）：** 一种 type 对应**一个租户客户端**，不是一 JVM 一种。实现可共存于代码；A 厂 Weaviate、B 厂 DashVector 须可同时存活。懒缓存键对齐 A24：`(tenantId)`（SHARED 共用默认客户端）。平台保存后 evict。未知 type 失败关闭（V3 精神）。**修订前**「一个进程只连一种」作废。单次检索仍禁止多 store 融合（缺口 10 PR-D2）。

## 运行时加固索引（A29+）

A1–A28 已收口，**不在此重开**。追加决策与实现单元见：

- 缺口：`docs/02.requirements/05.mes-ai-runtime-hardening-gaps.md`（A29–A36、I-A7–I-A8、F-R1–F-R2、R1–R8；A31 含 `knowledgeIds`/`mcpServerIds`；A35/A36 为 Agent 档案 Vue3 与对话可选档案）
- 设计：`docs/03.design/07.mes-ai-runtime-hardening-plan.md`
- 总控 TOC：`docs/04.specs/48.mes-ai-platform-program.md`（66–73）
- 代码单元 66–71 / 前端 72–73：**是否已编码以各 spec 第 4 节为准**（本索引不伪关闭）

## F1 壳层助手索引（另立单元，不重开 A1–A28）

任意页 F1 开抽屉、带着当前单说话，**不**另开对话运行时。不在此重开 A1–A28 / 57–59 冻结 path。

- 缺口：`docs/02.requirements/08.mes-ai-f1-shell-gaps.md`（S-D1–S-D12；P0=壳层，P1=摘要+工作台预填）
- 设计：`docs/03.design/09.mes-ai-f1-shell-plan.md`
- 实现：`docs/04.specs/84.ai-f1-shell-p0.md` → `85.ai-f1-summary-p1.md`
- 现网权限字是 `ai:chat:list` / `ai:chat:send`，**没有** `ai:chat` 这一档

## 生产级知识解析 / Advanced RAG 索引（另立，不重开 A1–A28 / A32 字段）

现网解析是 UTF-8 整文件；检索是 LangChain4j **Naive RAG**。升级轨另立，**不**把 Easy RAG 当生产方案。A32 的 overlap/topK/minScore/共用入口仍有效；「69 不做 rerank」只约束基线单元。A14/V4 以本文件 2026-09-08 修订为准（两档向量隔离），生产 RAG 93 为实现单元，不是「A14 不重开」。

- 缺口：`docs/02.requirements/10.mes-ai-production-rag-gaps.md`（PR-D1–PR-D20）
- 设计：`docs/03.design/11.mes-ai-production-rag-plan.md`
- 实现：`docs/04.specs/89.ai-production-rag-program.md` → `90.ai-document-ingest.md` / `92.ai-knowledge-admin-ui.md` → `91.ai-advanced-rag.md`；向量集群页 `93.ai-vector-cluster-ui.md`（可并行）
