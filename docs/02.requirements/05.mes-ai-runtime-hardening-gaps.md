# MES AI 运行时加固缺口清单

- 前提：平台轨道 A1–A28、I-A1–I-A6、F-A1–F-A4、V1–V4 已拍板且代码单元 49–65 已落地（证据见各 spec 第 4 节）。**本清单只追加 A29+，不从加固轨改写 A1–A28。** A14/V4 正文以缺口 `03` 的 **2026-09-08 修订**为准（SHARED/DEDICATED，非一 JVM 一种）。
- 设计：`docs/03.design/07.mes-ai-runtime-hardening-plan.md`
- 平台总控：`docs/04.specs/48.mes-ai-platform-program.md`（TOC 已扩展 66–73；生产级 RAG **89–94 另立**）
- 平台缺口索引（只读，不重开）：`docs/02.requirements/03.mes-ai-platform-gaps.md`
- 平台设计（只读，不重开）：`docs/03.design/06.mes-ai-platform-plan.md`
- 开发单元：66 共享运行时 → 67 记忆 → 68 Agent 配置 → 69 RAG → 70 工作流 chat 步 → 71 Tool 审计 → **72 Agent 档案 Vue3** → **73 对话可选档案**；车间只读 Tool **83**（依赖 62/68/72）
- 状态：`[ ]` 开放 · `[x]` 已收口（收口时在对应 spec 第 4 节写证据，并把本文件该项改为 `[x]`）
- 分类：`A` 产品决策 · `I` 集成/隔离 · `F` 前端（加固轨） · `R` 运行时加固
- 禁止在文档中写入真实服务地址、凭据或内部敏感路径

本轮产品决策 **A29–A36、I-A7–I-A8** 已拍板。66–73 / 83 代码已落地（证据见各 spec 第 4 节）；**浏览器未点选，不得写成 UI 已验收。** 71 落库已闭环（**R6**）。审计查询 HTTP / 管理端页见开放缺口 **R11**，不要勾成已交付，也不要把 71 标成未完成。

## A — 产品决策（运行时加固）

- [x] **A29** 共享会话准备 / SSE / 错误 / 租户上下文。**已拍板：** 从现网 `AiChatServiceImpl` 与 `AiAgentServiceImpl` 抽出统一的 session-prepare、SSE 推送、异常映射与 `TenantContext` 拷贝，供 `POST /ai/chat/send`、`POST /ai/agent/send`、`POST /ai/workflow/run` 复用。**冻结**三条 HTTP path 与既有 `{content, done}` 基础信封；扩展字段白名单与现网一致（`error` / `reasoning` / `status` / `tool` / `step`，见 A17）；不得为抽公共层而改 path 或引入 ruoyi `[NODE_CHUNK_*]`。证据：`docs/03.design/07.mes-ai-runtime-hardening-plan.md` §5.1。实现单元：`docs/04.specs/66.ai-shared-runtime-pipeline.md`。
- [x] **A30** Agent 记忆落库。**已拍板：** LangChain4j `ChatMemoryStore` 读写 `ai_message`；`memoryId` 冻结为 `{tenantId}:{userId}:{sessionId}`（字符串拼接，分隔符 `:`）。禁止全进程一份内存；禁止客户端传 `memoryId` 换租户。跨租户 session 404/403。证据：设计 07 §5.2。实现单元：`docs/04.specs/67.ai-agent-memory-store.md`。
- [x] **A31** 租户 Agent 配置与按需 Tools。**已拍板：** 学 ruoyi `agent_info`（**不要** Supervisor）：租户级 `ai_agent` 档案字段冻结为 `modelId`、`systemPrompt`、`knowledgeIds`、`enabledTools`、`mcpServerIds`；A24 工厂按档案组装一份 `MesAiAssistant`。**默认**受控写 Tools（A26）与 MCP（A27）**关闭**（无 profile 时亦同；未传 `agentId` 用默认 profile，其 seed 亦不含写/MCP）；`enabledTools` 仅白名单 Tool 名子集，**未列出者不得**出现在模型 tool list；`mcpServerIds` 为租户 MCP 白名单（A27）的**档案级子集**（非「启用 mcp 即挂本租户全部 server」）；`knowledgeIds` 绑定本租户知识库 id，Tool 调用时仍复核归属。`POST /ai/agent/send` 可选 `agentId`；未传则用租户默认 profile。管理端 CRUD 冻结为 `/ai/agent/profile`。**禁止** Supervisor 多 Agent 拓扑、禁止任意 SQL / JDBC Tool。证据：设计 07 §5.3。实现单元：`docs/04.specs/68.ai-agent-profile.md`。现行 `enabledTools` 五值见 **83**（`searchKnowledge` | `submitProductionFeedback` | `updateWorkorderStatus` | `queryWorkorder` | `listWorkorderTasks`；默认档案只读 `searchKnowledge`）。
- [x] **A32** RAG 检索管线。**已拍板：** 知识解析采用**重叠切块**（overlap）+ **分隔符**（per-knowledge 可配或租户/全局默认）；检索支持**相似度阈值**与**可配置 topK**（租户或知识库级，具体字段留给实现 spec）。`POST /ai/chat/send` 挂知识库与 Agent `searchKnowledge` **共用** `AiRetrievalService`。**69 基线不做** hybrid 检索、**不做** rerank、**不做** 多向量融合。证据：设计 07 §5.4。实现单元：`docs/04.specs/69.ai-rag-pipeline.md`。**覆盖升级（另立，不改本条字段）：** 生产级 Parser + LangChain4j Advanced RAG（含 RRF / Scoring 再排序）见缺口 `docs/02.requirements/10.mes-ai-production-rag-gaps.md`、设计 11、单元 89–94。仍禁止 BM25 hybrid 与多向量库融合。
- [x] **A33** 工作流 chat 步骤。**已拍板：** 工作流步骤允许 `toolName=chat`，经 A24 `(tenantId, modelId)` 工厂调 LangChain4j 对话；SSE 仍自定 `content` / `done` / 可选 `step`，**禁止** langgraph、**禁止** ruoyi `[NODE_CHUNK_*]`。不改 `POST /ai/chat/send` / `POST /ai/agent/send` 信封。证据：设计 07 §5.5。实现单元：`docs/04.specs/70.ai-workflow-chat-step.md`。
- [x] **A34** Tool 调用审计。**已拍板：** 每次 Tool 调用（含受控写、MCP、知识检索、工作流节点）须落审计：用户、租户、Tool 名、业务主键（若有）、耗时、token 用量（若上游返回）。**禁止**日志 / SSE / 审计行写入 apiKey、MCP token、cipher、JDBC。**禁止**引入 `ruoyi-common-trace` 或第二套链路追踪。证据：设计 07 §5.6。实现单元：`docs/04.specs/71.ai-tool-audit.md`。查询 HTTP / 管理端页不在本条，见 **R11**。
- [x] **A35** Agent 档案 Vue3 管理页。**已拍板：** 在 `startech-mes-front` 落地 `/ai/agent` **配置 CRUD**（学既有 `ai/mcp`、`ai/knowledge` 表格/对话框），**不是**独立对话 SPA、**不是** Supervisor。表单字段与 A31 / `/ai/agent/profile` 对齐：`name`、`systemPrompt`、`defaultModelId`、`knowledgeIds`、`enabledTools`（现行五值与 **83** 一致：`searchKnowledge` | `submitProductionFeedback` | `updateWorkorderStatus` | `queryWorkorder` | `listWorkorderTasks`；默认只读 `searchKnowledge`）、`mcpServerIds`、`status`、`remark`。新建 `src/api/ai/agent.js`；组件路径 `ai/agent/index`；菜单 **9107** 在页面就绪后取消隐藏（`visible='0'`；若依 `visible='1'` 为隐藏）。权限字沿用 `ai:agent:profile:*`。默认交互：写 Tool 与 MCP **默认不勾**。MCP/模型/知识库 options **不回显** cipher / apiKey。禁止改 Vue2；禁止引入 element-plus-x / hook-fetch。证据：设计 07 §5.7。实现单元：`docs/04.specs/72.ai-agent-profile-ui.md`。
- [x] **A36** 对话页可选档案走 Agent 发送。**已拍板：** 现有 `/ai/chat` 增加可选「Agent 档案」选择器；**选中档案**（含显式选默认档案）时 SSE 走 `POST /ai/agent/send`（body 可带 `agentId`）；**未选档案**时仍走 `POST /ai/chat/send`（纯对话、无 Tool；58/59 信封与 path **不变**）。**不新开**独立 Agent 聊天路由 / 菜单。发送权限复用 `ai:chat:send`。不把 chat 页改成工作流 runner。证据：设计 07 §5.8。实现单元：`docs/04.specs/73.ai-chat-agent-select.md`。

## I — 集成 / 隔离（运行时加固）

- [x] **I-A7** 三条 SSE 口共享租户装饰。**已拍板：** chat / agent / workflow run 的异步线程均走同一 `TenantTaskDecorator`（或等价）拷贝 `TenantContext`；空上下文失败关闭，不得写 `ai_message`、不得调向量、不得跑 Tool。与 A29 同一实现单元 66。
- [x] **I-A8** 审计不打密钥。**已拍板：** A34 审计字段允许 userId、tenantId、toolName、businessKey、latencyMs、promptTokens、completionTokens；**不允许** apiKey、cipher、Authorization、MCP 明文配置。异常对前端不回密钥。与 A34 同一实现单元 71。

## F — 前端（运行时加固轨；不重开 F-A1–F-A4）

- [x] **F-R1** 72 Agent 档案 Vue3 页编码（A35）。证据：`docs/04.specs/72.ai-agent-profile-ui.md` 第 4 节。**浏览器未点选，不得写成 UI 已验收。**
- [x] **F-R2** 73 对话页可选档案 + `/ai/agent/send` 接线编码（A36）。证据：`docs/04.specs/73.ai-chat-agent-select.md` 第 4 节。**浏览器未点选，不得写成 UI 已验收。**

## R — 运行时加固（依赖顺序，非产品重开）

- [x] **R1** 66 共享运行时管线编码（A29 / I-A7）。证据：`docs/04.specs/66.ai-shared-runtime-pipeline.md` 第 4 节。**浏览器未点选。**
- [x] **R2** 67 Agent 记忆 Store 编码（A30）。证据：`docs/04.specs/67.ai-agent-memory-store.md` 第 4 节。**浏览器未点选。**
- [x] **R3** 68 Agent profile + 按需 Tools 编码（A31）。证据：`docs/04.specs/68.ai-agent-profile.md` 第 4 节。**浏览器未点选。**
- [x] **R4** 69 RAG overlap / 阈值 / topK 编码（A32）。证据：`docs/04.specs/69.ai-rag-pipeline.md` 第 4 节。Vue3 知识库页已可配检索条数 / 相似度阈值 / 切块分隔符。剩余：live 向量集群。UTF-8 伪解析与 Naive RAG 已由缺口 10 / 单元 90–91 覆盖升级（Maven 证据见各第 4 节；点选/live 未做），不在本条重开。
- [x] **R5** 70 工作流 chat 步编码（A33）。证据：`docs/04.specs/70.ai-workflow-chat-step.md` 第 4 节。**浏览器未点选。**
- [x] **R6** 71 Tool 审计编码（A34 / I-A8）。证据：`docs/04.specs/71.ai-tool-audit.md` 第 4 节（落库闭环）。查询 HTTP / 管理端页不在本条，见 **R11**。**浏览器未点选。**
- [x] **R7** 72 Agent 档案 UI 编码（A35；与 F-R1 同单元，前端为主）。证据：`docs/04.specs/72.ai-agent-profile-ui.md` 第 4 节。**浏览器未点选，不得写成 UI 已验收。**
- [x] **R8** 73 对话可选档案编码（A36；与 F-R2 同单元）。证据：`docs/04.specs/73.ai-chat-agent-select.md` 第 4 节。**浏览器未点选，不得写成 UI 已验收。**
- [x] **R9** Demo A 车间生产助手只读 Tools（`queryWorkorder` / `listWorkorderTasks`）。扩展 A31 `enabledTools` 白名单；包装现有 MES list service；禁止 SQL / JDBC；默认 profile 仍仅 `searchKnowledge`、不含写 Tool。实现单元：`docs/04.specs/83.ai-workshop-demo-read-tools.md`（证据见该文件第 4 节）。**浏览器未点选。**
- [ ] **R10** 聊天/Agent 发送路径缺少报工/改状态确认闸。**现状：** 档案启用写 Tool（A26 `submitProductionFeedback` / `updateWorkorderStatus`）后，对话里模型调用即可报工或改工单状态，仍走现有 MES `@PreAuthorize`。**缺口：** 发送路径没有「确认报工/改状态？」闸（`confirmToken` 或等价 UI）。**已拍板：本期不做**（无 `confirmToken`、无二次确认按钮）。后续另开单元。不要把确认闸写成已交付。不作为 F1 新能力。
- [ ] **R11** 无 `ai_tool_audit` 查询 HTTP / 管理端页。**现状：** 工具调用已写 `ai_tool_audit`（A34 / 71 / R6）。**缺口：** 无查询 HTTP、无管理端页。**已拍板：本期不做**。后续另开单元。不要把查询页写成已交付，也不要把 71 标成未完成。

生产级 RAG **不在本清单 R1–R11 编号内**（避免与加固轨混淆）。编码证据回写 `docs/02.requirements/10.mes-ai-production-rag-gaps.md` 的 PR-X1 / PR-X2。
