# MES Flyway 后续缺口（多租户收口转出）

- 来源：多租户清单 **I10** 在租户轨已收口为「独立后续需求」，**不**阻塞租户 v1
- 设计：`docs/03.design/05.mes-flyway-integration.md`
- 租户清单（只读，不重开 I10）：`docs/02.requirements/02.mes-multi-tenant-gaps.md`
- 总控（只读）：`docs/04.specs/35.mes-multi-tenant-program.md`；专库开通：`docs/04.specs/41.tenant-dedicated-ds-pd.md`
- 状态：`[ ]` 开放 · `[x]` 已收口
- 禁止在文档中写入真实服务地址、凭据或内部敏感路径

租户轨道只拍板「要 Flyway」，不在本仓当前租户单元引入 `flyway-core`。剩余工作记在本文件；实现前另拆 `docs/04.specs/` 开发单元（复制 `docs/01.temlplates/01.development_unit_template.md`，序号接当时最大号）。本文不写 Java。

## 从租户轨转出

- [x] **I10（租户轨）** 产品已拍板：集成 Flyway。租户 v1 **不**实现 `flyway-core`。不得回写为租户 Known gap，也不得把 flyway-core 当作租户 v1 门禁。

## 待独立需求落地

- [ ] **FLY-C1** 引入 `flyway-core` + `flyway-mysql`（Spring Boot 4.1）；不引入第二套 ORM / 认证。当前仓库仍无 `flyway-core`。
- [ ] **FLY-C2** 启动时对 master（含全部 `ROW` / 平台）自动 migrate（locations = `master` + `tenant`）；失败拒绝启动。
- [ ] **FLY-C3** `DATABASE`：新开在开通步骤跑 tenant location 全链建表；已 `READY` 专库启动时用一次性 JDBC 跑增量（不建专池，并行 N≤2，失败只阻断该租户）。
- [ ] **FLY-C4** 落地前专库增量仍走 `TenantDedicatedSchema.incrementalSqlFiles`（I6）。落地后只保留 Flyway Java API 一套迁表器，不再并行旧清单。
- [ ] **FLY-C5** 不改 I5 / I6 / I7 失败关闭语义：启动只建主池；专库不可用禁止回落主库；迁表失败保留登记、可重试、不删行、不写共享主库。
